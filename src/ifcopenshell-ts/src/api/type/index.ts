// This file was generated with the assistance of an AI coding tool.
//
// Barrel for `ifcopenshell.api.type` (src/ifcopenshell-python's `ifcopenshell/api/
// type/` package). Only `unassign_type` (`./unassignType.ts`) is ported in this chunk
// -- `assign_type` and `map_type_representations` remain future, separate chunks (see
// `unassignType.ts`'s own header comment for why). Namespaced per this project's
// `util/index.ts` per-submodule convention: `api.type.unassignType`.
export { unassignType } from "./unassignType";
export type { UnassignTypeSettings } from "./unassignType";
