// This file was generated with the assistance of an AI coding tool.
//
// Barrel for `ifcopenshell.api.type` (src/ifcopenshell-python's `ifcopenshell/api/
// type/` package) -- now FULLY COMPLETE. `unassign_type` landed in an earlier chunk
// (`./unassignType.ts`); this chunk adds `assign_type`/`map_type_representations`
// (`./assignType.ts`/`./mapTypeRepresentations.ts`), completing the module -- see each
// file's own header comment for the two disclosed, load-bearing dependencies on
// entirely unported modules (`api.material`/`api.geometry`) that both throw a clear
// error rather than silently skip real logic, and `TODOS.md` for the full writeup.
// Namespaced per this project's `util/index.ts` per-submodule convention:
// `api.type.assignType`/`api.type.mapTypeRepresentations`/`api.type.unassignType`.
export { assignType } from "./assignType";
export type { AssignTypeSettings } from "./assignType";
export { mapTypeRepresentations } from "./mapTypeRepresentations";
export type { MapTypeRepresentationsSettings } from "./mapTypeRepresentations";
export { unassignType } from "./unassignType";
export type { UnassignTypeSettings } from "./unassignType";
