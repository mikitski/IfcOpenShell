// This file was generated with the assistance of an AI coding tool.
//
// Barrel for `ifcopenshell.api.attribute` (src/ifcopenshell-python's `ifcopenshell/api/
// attribute/` package) -- now FULLY COMPLETE, its one real file (`edit_attributes.py`,
// 85 lines) landed in this same chunk. See `./editAttributes.ts`'s own header comment
// for this chunk's exact scope, the correction of this chunk's own task brief (real
// Python DOES wrap this function via `wrap_usecases`, unlike `api.alignment`/
// `api.cogo`), and the ported `PredefinedType`/`ElementType`/`ObjectType` consistency
// logic. Namespaced per this project's `util/index.ts` per-submodule convention:
// `api.attribute.editAttributes`.
export { editAttributes } from "./editAttributes";
export type { EditAttributesSettings } from "./editAttributes";
