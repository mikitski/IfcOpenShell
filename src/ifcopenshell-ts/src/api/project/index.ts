// This file was generated with the assistance of an AI coding tool.
//
// Barrel for `ifcopenshell.api.project` (src/ifcopenshell-python's `ifcopenshell/api/
// project/` package). An earlier chunk ported 3 of the module's 4 real files:
// `create_file.py`, `assign_declaration.py`, `unassign_declaration.py`. This chunk adds
// the 4th and final file, `append_asset.py` (827 lines -- importing a whole asset, its
// geometry, materials, styles, and property sets from a library file into the active
// model) -- see `./appendAsset.ts`'s own header comment for the full investigation
// writeup (native EXPRESS schema attribute-type reflection turned out to be fully
// available) and the one disclosed, narrow blocker (a pre-existing, already-tracked
// primitive-layer gap, not new). `api.project` is now fully ported.
export * from "./createFile";
export * from "./assignDeclaration";
export * from "./unassignDeclaration";
export * from "./appendAsset";
