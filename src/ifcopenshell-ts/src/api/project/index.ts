// This file was generated with the assistance of an AI coding tool.
//
// Barrel for `ifcopenshell.api.project` (src/ifcopenshell-python's `ifcopenshell/api/
// project/` package). This chunk ports 3 of the module's 4 real files:
// `create_file.py`, `assign_declaration.py`, `unassign_declaration.py`. Deliberately
// NOT ported: `append_asset.py` (827 lines -- importing a whole asset, its geometry,
// materials, styles, and property sets from a library file into the active model) --
// a much larger, separate future chunk, out of scope here. Matches this project's
// established partial-completion pattern for other oversized modules (`api.type`,
// `api.pset`, `api.geometry`'s own partial `PROGRESS.md` rows).
export * from "./createFile";
export * from "./assignDeclaration";
export * from "./unassignDeclaration";
