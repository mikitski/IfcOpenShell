// This file was generated with the assistance of an AI coding tool.
//
// Barrel for `ifcopenshell.api.spatial` (src/ifcopenshell-python's
// `ifcopenshell/api/spatial/` package) -- all 4 public functions ported in this
// chunk: `assign_container`/`unassign_container` (manage the single-valued
// `IfcRelContainedInSpatialStructure`) and `reference_structure`/
// `dereference_structure` (manage the weaker, multi-valued
// `IfcRelReferencedInSpatialStructure`). See each function's own file for its
// specific porting notes and (for `assignContainer.ts` only) disclosed blockers.
// Namespaced per this project's `util/index.ts` per-submodule convention:
// `api.spatial.assignContainer`, etc.
export { assignContainer } from "./assignContainer";
export type { AssignContainerSettings } from "./assignContainer";
export { dereferenceStructure } from "./dereferenceStructure";
export type { DereferenceStructureSettings } from "./dereferenceStructure";
export { referenceStructure } from "./referenceStructure";
export type { ReferenceStructureSettings } from "./referenceStructure";
export { unassignContainer } from "./unassignContainer";
export type { UnassignContainerSettings } from "./unassignContainer";
