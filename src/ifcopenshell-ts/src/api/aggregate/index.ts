// This file was generated with the assistance of an AI coding tool.
//
// Barrel for `ifcopenshell.api.aggregate` (src/ifcopenshell-python's
// `ifcopenshell/api/aggregate/` package) -- this chunk only ports `unassign_object`
// (`./unassignObject.ts`), a direct dependency of `api.spatial.assign_container`.
// `assign_object` (the other half of this 2-function module) is NOT ported here --
// see `unassignObject.ts`'s own header comment for why. Namespaced per this project's
// `util/index.ts` per-submodule convention: `api.aggregate.unassignObject`.
export { unassignObject } from "./unassignObject";
export type { UnassignObjectSettings } from "./unassignObject";
