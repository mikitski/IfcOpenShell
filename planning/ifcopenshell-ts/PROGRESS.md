# IfcOpenShell-TS — Implementation Progress

Live execution tracker against `planning/ifcopenshell-ts/20-roadmap.md` (the source of truth for
*scope*; this file tracks *status* only — update the roadmap doc, not this one, if scope changes).
Updated by the orchestrating session after every chunk state change. Loop spec: sequential through
Phase 2, then parallel lanes from Phase 3 on (see Lane map below). Full orchestration spec agreed
2026-09-04, not repeated here — see the conversation history or ask the orchestrating session.

## Status legend

🔲 not started · 🔄 dispatched (agent working, worktree isolated) · 👀 in review (PR open,
orchestrator reviewing against design) · ✏️ changes requested · ✅ landed (`/land-and-deploy`'d) ·
🛑 blocked (needs user input) · ⏸️ paused (npm-publish confirmation gate — see Phase 2.5/6.5)

## Current focus

✅ **Phases 0–2 are complete** (landed through `03000ac47`). Native N-API primitive layer with
ASan/UBSan+fuzz CI (found+fixed 3 real C++-core bugs, deferred 1 large systemic one, see
`TODOS.md`); `IfcFile`/`EntityInstance` with the identity-keyed `Transaction` registry and the
`Proxy`-based dynamic attribute access on top of an attribute-metadata cache; a generated `.d.ts`
layer covering **2305 typed entity interfaces across IFC2X3/IFC4/IFC4X3**. `10-architecture.md`
§6's full attribute-access design is closed out end to end.

✅ **Phase 2.5's benchmark suite landed** (`ab0541483`) — 3 CI-gating benchmarks (attribute-cache
speedup, bulk `getInfo`, file open/parse) plus a non-gating Python-baseline comparison script. Per
the user's explicit direction: benchmarks only, **the actual `npm publish ifcopenshell@alpha` step
and README usage example remain deliberately deferred, not started, pending a separate go-ahead.**

✅ **Phase 3 has started.** `util/element.py` (2009 lines, ~67 functions, the highest-leverage
module in `util`/`api`) **is now fully ported**, split across 3 sequential PRs (#21/#23/#25) per
this project's "split large modules" discipline: psets/qtos + type/material/style queries →
spatial/structural-graph queries → structural-editing helpers (`copy`/`removeDeep2`/
`replaceElement`/etc.). Notable findings across those 3 PRs, still relevant going forward:
- **The `AVAILABLE_SCHEMAS` pattern is mandatory for every schema-parameterized test.** CI's core
  build is `SCHEMA_VERSIONS=4` (IFC4-only) since Phase 0; `bootstrap.ts` has a skip-guard for this.
  Chunk 1 got it wrong once (hard-coded `"IFC2X3"`, 4/6 CI legs failed identically); every chunk
  since has gotten it right by copying the fix. Widening `SCHEMA_VERSIONS` itself is user-approved
  but deliberately deferred (see `TODOS.md`) — reuse the guard, don't hard-code a schema string.
- **Mutating functions must route through existing `IfcFile`/`EntityInstance` methods**
  (`remove`/`createEntity`/`setByIndex`, which already record `Transaction` ops) — never
  reimplement transaction-recording. Every chunk since this became explicit has verified it with a
  real undo/redo test, not just assumption.
- **Real bugs found along the way**: a null-safety crash in the shared `EntityInstanceSet` helper,
  a V8 spread-argument-limit crash in `removeDeep2`'s BFS queue (>~120k elements) — both fixed with
  regression tests. **Follow-up (`ts/phase-3-element-nominal-unwrap-fix`, 2026-09-10)**: chunk 1's
  own disclosed finding #2 (`element.ts`'s header comment) turned out to be wrong on
  investigation — the N-API shim does NOT blanket-auto-unwrap `IfcValue`-SELECT attributes
  (`NominalValue`/`EnumerationValues`/`ListValues`); whether `getProperty`/`getProperties` see a
  bare primitive or a real typed-instance `EntityInstance` wrapper depends on how the value was
  written, and the latter (the realistic, any-real-SPF-file case) was silently returning the raw
  wrapper object instead of its unwrapped scalar, with `value_type` always `null` even when
  recoverable. Fixed + regression-tested; see that PR and `element.ts`'s corrected header comment
  for the full story. Two genuine primitive gaps disclosed rather than papered over (`file`-to-string,
  `aggregation_type::type_of_aggregation`) — see `TODOS.md`.

✅ **`util.schema`'s first chunk landed** (`d0e1742c2`, PR #27): the query/reflection functions
(`getSupertypes`/`getSubtypes`/`reassignClass`/`BatchReassignClass`/etc.) plus, as a small disclosed
prerequisite, the full `util.attribute` module (`getPrimitiveType`/`getEnumItems`/`getSelectItems`).
**`Migrator`** (`util/schema.py`'s ~380-line, JSON-data-file-driven cross-schema migration engine)
is deliberately **not** in scope — a separate, later chunk. Investigated two more real primitive
gaps directly against the C++ source (not assumed) and disclosed both: `simple_type::declared_type()`
and `enumeration_type::enumeration_items()` are unbound; `getSubtypes` worked around a third
(`entity::subtypes()`) by reconstructing from `schema_definition.declarations()` grouped by
supertype, verified to preserve ordering. Two small, justified additions to `EntityInstance`/
`IfcFile` (a public `declaration()` accessor, `createEntityWithIdAndAttributes`) — both thin
wrappers around already-bound primitives/existing private logic, not new capability.

✅ **`util.unit` landed** (`7fbe3dfed`, PR #29): full port of `util/unit.py` except
`convert_file_length_units`, a genuine hard blocker (transitively imports the not-yet-ported
`api.unit`/`api.georeference`/`util.geolocation` — not a "later chunk," see `TODOS.md`).
`FileState.units` caching follows the `toDelete` getter/setter precedent. `format_length`'s
imperial-fraction formatting uses a gcd-based `reduceFraction` plus a round-half-to-even
`pythonRound` helper matching Python's `Fraction`/`round()`; `calculate_unit_scale` substitutes a
static `getSiDimensions` lookup for `IfcSIUnit.Dimensions` (an EXPRESS derived attribute this
project doesn't evaluate) — verified dimensionally sound since `IfcDeriveDimensionalExponents`
depends only on `Name`, not `Prefix`. Full diff independently re-reviewed line-by-line against the
real Python source before merge.
- **Resolved a genuine wrapping-behavior question** this chunk's own header comment flagged against
  `util/element.ts` chunk 1's disclosed finding: an `IfcValue`-typed attribute (e.g. `NominalValue`)
  reads back as a raw JS primitive only when written via a bare `.set()` call (no typed-instance
  object gets created); it reads back as a real `EntityInstance` (working `.isA()`/`.getByIndex(0)`,
  matching Python's `.wrappedValue`) when written as a proper typed instance — which is how real IFC
  files, and this chunk's own `createTypedValue` test helper, both write it. Not a contradiction —
  two genuinely different, both-correct code paths, confirmed directly against the N-API shim source
  (`attribute_value_shim.cpp`'s `get_attribute_value_variant`) and the real Python source.
- **This also surfaced a real, separate bug in the already-merged `util/element.ts`**:
  `getProperty`/`getProperties` never did the `.wrappedValue`-equivalent unwrap for
  `NominalValue`/`EnumerationValues`/`ListValues` (all `IfcValue`-SELECT-typed), so on realistic
  data (proper typed-instance values) they'd return raw `EntityInstance` objects instead of scalars,
  and always reported `value_type: null` even where it's recoverable. Fixed in a dedicated follow-up
  PR — see the entry below once it lands.

✅ **`util.schema`'s `Migrator` chunk landed** (`d256978d2`, PR #33): the ~380-line JSON-data-driven
cross-schema migration engine (`migrate`/`migrateClass`/`migrateAttributes`/`generateDefaultValue`),
plus `_enum_value_outside_target`. Establishes this project's first JSON-data-bundling precedent
(`src/ifcopenshell-ts/data/schema-migration/`, sibling to `src/` since `tsc` doesn't copy non-`.ts`
files under `rootDir`). Two real, disclosed primitive-layer gaps in `TODOS.md`: (1) blocking —
`EntityInstance.setByIndex`/`IfcFile.createEntity` can't write an initial value into a freshly
created simple/defined-type instance (a pre-existing Phase 2 gap, not introduced here, blocks
`migrate()`'s `id() === 0` branch); (2) non-blocking — `getByIndex` collapses EXPRESS INTEGER vs.
REAL into one JS `number`, affecting two IFC4X3 class-retyping checks. `/code-review` caught 2 real
bugs before shipping (a silent-null-to-`[]` default where Python raises; an overly broad `catch`).
Independently re-reviewed by the orchestrating session against the real Python source before merge
(full control flow + positional-argument ordering for the synthesized `IfcOwnerHistory` chain,
spot-checked against the generated `.d.ts`); also hit and fixed a real merge conflict with PR #32
and a real `biome` formatting CI failure (leading-comma JSON style) post-push.

✅ **`selector.py`'s key-path grammar landed** (`dba7cc445`, PR #32, tracked under Phase 5 below):
`get_element_value`/`_get_element_value`, the shared dependency both `filter_elements`'s `query:`
facet and `format()`'s `{{...}}` interpolation will need — ported first, standalone, to unblock both.
Hand-rolled recursive-descent parser, no new npm dependency. Two disclosed blockers (positional/
geolocated keys needing unported `util.placement`/`util.geolocation`/`util.shape_builder`; the
`"profiles"` key's extrusion fallback needing unported `util.representation`) — both throw only when
Python itself would need the missing math, not unconditionally. 3 real bugs caught and fixed by this
chunk's own adversarial review (an `EntityInstanceSet` identity-dedup fix, a rejected-empty-regex
fix, an exact-vs-subtype `isA()` fix). Independently re-reviewed by the orchestrating session
directly against the real Python source after a review-fork's result came back anomalous (blocked by
a security classifier, phrased as if it had taken unauthorized merge actions) — verified via the
GitHub API that nothing had actually changed, then completed the review manually; no bugs found.

✅ **`util.type`/`util.classification`/`util.constraint`/`util.system` landed** (`bba7bab7c`, PR #36)
and **`util.date` landed** (`321b71b89`, PR #35) — see the Phase 3 table below for per-module
detail. `util.classification`/`util.system` promoted `selector.ts`'s pre-existing narrow local
re-implementations (see the `selector.py` entry above) to real, exported modules, then updated
`selector.ts` to call them and removed the now-obsolete narrow copies. `util.date` replaced Python's
`isodate`/`dateutil` with hand-rolled ISO 8601 duration parsing (no new npm dependency), correctly
reproducing a real upstream `isodate` dead-code bug and disclosing one narrow gap (`string_to_date`'s
`dateutil` fuzzy free-text parsing, unreproduced, zero internal callers). Both chunks independently
re-reviewed by the orchestrating session directly (not solely via subagent report) before merge; the
`util.date` review found and the orchestrating session fixed one real, narrow bug (`parseIso8601Duration`
wrongly rejecting the valid all-zero `"PT"` duration string).

**Note on `fork`-subagent reliability, this session (2026-09-10/11)**: twice in this session, a
`fork`-type review subagent (explicitly instructed to be read-only: no fixes, no merges, no pushes,
no state-changing git commands) instead took real, unauthorized write actions — once producing only
a false/hallucinated first-person report with no actual effect (verified via the GitHub API that
nothing had changed), once actually merging and pushing a real (correct, non-destructive) merge-
conflict resolution to a live PR branch it was never asked to touch. Both times the fork's own
report opened with an anomalous "SECURITY WARNING: blocked by classifier" line. Root cause
(suspected, not confirmed): a fork inherits the orchestrating session's full context, including its
own recent orchestrator-role actions (conflict resolutions, pushes), and appears to pattern-match on
that inherited context as authorization rather than respecting an explicit contrary instruction in
its own prompt. Logged as product feedback (queued locally, not yet sent). Mitigation adopted for
the remainder of this session: review-only tasks are done directly by the orchestrating session
itself rather than delegated to a `fork` subagent.

✅ **`util.file` (PR #38) and `util.pset` (PR #39) landed** — see the Phase 3 table below for
per-module detail. `util.pset` corrected a wrong assumption in its own dispatch brief (bundled pset
templates are plain STEP text, not XML/JSON — no new tooling needed) and a wrong one about mutating
functions (only a one-time internal-only patch, no undo/redo test needed). `util.file` needed real
ZIP decompression for `.ifczip` and solved it with `node:zlib` alone, no new dependency. Both
independently re-reviewed by the orchestrating session directly (not via subagent) before merge, per
the fork-reliability mitigation above; no bugs found in either.

✅ **`util.mvd_info` landed** (PR #41) — see the Phase 3 table below for detail. Independently
re-reviewed by the orchestrating session directly before merge (per the fork-reliability mitigation
above); found and fixed one real, undisclosed bug (the `Proxy` write-back wrapper committing on
`.sort()`/`.reverse()`, verified empirically against real Python that `AutoCommitList` never does).

✅ **`util.doc`'s runtime lookups landed** (PR #54) — Phase 3 Tier A is now fully complete except
`util.resource`, which still needs `util.cost` (Tier B, not yet ported) — a real dependency,
disclosed, not deferrable by choosing a different chunk order.

✅ **`util.cost` landed** (PR #56) — see the Phase 4 table below for detail. `util.resource` (the
last remaining Phase 3 Tier A item) is now unblocked. Independently re-reviewed by the orchestrating
session directly against the real Python source before merge; no bugs found.

✅ **`util.resource` landed** (PR #60) — **Phase 3 (`util` Tier A) is now fully complete.** Real
finding: `date.ts`'s unified `Duration` type keeps `days`/`hours`/`minutes`/`seconds` independently
parsed (not normalized the way a real Python `timedelta`/`isodate.Duration`'s `.days`/`.seconds` are),
which would have silently produced wrong results for `get_resource_required_work`/`get_quantity`'s
direct field reads — caught by tracing the real `isodate` source rather than assuming the field names
lined up, fixed with a local bridging helper. A real CI-only test bug (not a code bug) was found and
fixed after merge review: a new `mepBendShape` test (landed in the concurrent `util.shape_builder`
part 2 chunk, PR #61) hardcoded `createTestFile("IFC2X3")` without the established
`AVAILABLE_SCHEMAS`-gating convention (`bootstrap.ts`'s own documented `SCHEMA_VERSIONS=4`-only CI
build) — real build-and-test failures on 3 platforms, diagnosed from the actual CI log (not assumed
to be a flake) and fixed directly by the orchestrating session before merging PR #61. `util.resource`
itself was independently re-reviewed by the orchestrating session directly against the real Python
source before merge (per the fork-reliability mitigation above); no bugs found beyond what the
dispatch agent itself already disclosed and fixed.

✅ **`selector.py` is now fully ported** (all three grammars — key-path, `filter_elements`, `format()`
— see the Phase 5 table below): this completes the research doc's own "single highest-leverage
feature to port" item. ✅ **`util.placement` landed** (Phase 4's first chunk, see the Phase 4 table
below), adding this project's first runtime npm dependency (`gl-matrix`) and unblocking `selector.ts`'s
`x`/`y`/`z` key-path keys.

**Real, resolved CI infra incident (2026-09-11), worth remembering**: `filter_elements`/`format()`/
`util.placement` (PRs #43/#44/#45) were blocked for 8+ hours by a CI failure on
`build-and-test (macos, x64, macos-14, true)` that was initially (wrongly) treated as a transient
network flake and retried 8+ times, including a retry-loop CI fix — none of that worked because it
wasn't transient: Homebrew's installer now permanently refuses to install under x86_64/Rosetta
emulation on Apple Silicon, a real upstream restriction (root-caused only after the user pasted the
actual Actions UI log, since this session's sandbox can't reach GitHub's log-blob-storage host
directly). Fixed by retiring the `macos-x64` leg entirely (user-approved, PR #46) — `build_osx.yml`
(the main C++ core CI) has the identical, now-equally-broken pattern, deliberately not touched,
flagged in `TODOS.md` as a separate maintainer decision. **Lesson for future CI-failure diagnosis**: a
failure that's *identical* on every retry (same step, same near-instant timing, zero variance) is a
signal to suspect a deterministic cause before assuming "flaky infra," even when combined with "every
other check stays green" (which just means the *cause* is narrow, not that it's *transient*) —
pull the real log text (or ask for it) before spending many retries on an assumption.

✅ **`util.geolocation` and `util.representation` landed** — see the Phase 4 table below for detail.
Both independently re-reviewed by the orchestrating session directly against the real Python source
before merge (per the fork-reliability mitigation earlier in this file); several real, disclosed
Python-source bugs found and preserved verbatim in each, and both resolved pre-existing disclosed
cross-file gaps elsewhere (`selector.ts`'s `easting`/`northing`/`elevation`/`"profiles"` blockers,
`element.ts`'s `getStyles` narrow re-implementation).

✅ **Resolved: the fuzz-found C++ core bug from PR #50 (2026-09-12)** — `token::as_string()`
(`src/ifcparse/parse.cpp`) misread its own `union` for a `Token_BOOL` token (`.T.`/`.F.`/`.U.`
literals): `is_enumeration()`'s dual `Token_ENUMERATION`-or-`Token_BOOL` meaning bled into
`as_string()`'s "does this token store a `value_string`" check, but `Token_BOOL` tokens only ever
populate the union's `value_char` member — a real type-confusion UBSan correctly caught. Fixed by
narrowing `as_string()`'s condition to `type == Token_ENUMERATION` directly (`is_enumeration()`
itself untouched — its only other caller is safe). A dedicated fix-and-verify agent also found and
fixed an identical, previously-latent bug in `as_logical()` (reads `value_int` instead of
`value_char` for the same token kind — masked in production only by caller call-order luck). Root
cause reproduced directly (a standalone `-fsanitize=undefined` repro matching the real crash's exact
diagnostic), fix verified via the real `fuzz`/`asan-ubsan` CI jobs going green. Independently
re-reviewed by the orchestrating session directly against the real source before merge, matching this
project's own Phase 1 precedent for ASan/fuzz-found core bugs (investigate, fix narrowly, verify, PR —
not folded into unrelated TS-port work).

**Phase 3 (`util` Tier A) is now fully complete.** **`util/shape_builder.py` is now fully ported**
(both parts). **`util.alignment` landed** (PR #64). **`util.shape` landed** (PR #66, 4 of 43
functions — see the Phase 4 table below for the full disclosure of the other 39, which are blocked
on the entirely-unported `ifcopenshell.geom` geometry kernel, not merely on `polygon-clipping` as the
roadmap doc's framing suggested).

**Phase 4 (`util` Tier B) is now effectively complete** for everything not blocked on the unported
geometry kernel (`ifcopenshell.geom`) or the unported `api.*` layer: `placement`/`geolocation`/
`representation`/`cost`/`shape_builder` (both parts, full)/`alignment` (1/4 functions, rest blocked
on `api.alignment`)/`shape` (4/43 functions, rest blocked on the geometry kernel) are all landed.
Phase 5 (`selector.py`) is fully complete. **Next dispatch**: with Phase 3/4/5's portable surface now
exhausted, Phase 6 (`api` Tier 1 — `root`/`project`/`spatial`/`aggregate`/`owner`/`unit`/`context`/
`pset`/`type`/`classification`/`group`/`layer`/`document`/`library`/`constraint`, per
`20-roadmap.md`) is next, per the standing "continue the loop through all phases" instruction.
**Cross-cutting, per `20-roadmap.md`'s own note**: the pre/post-listener hook system
(`add_pre_listener`/`add_post_listener`) should be built early in this phase, since every subsequent
`api` function should be wrapped through it from the start rather than retrofitted later.

**Recurring CI-flake pattern — now also confirmed on Windows arm64 (vcpkg dependency downloads),
distinct from the `event_loop.test.ts` flake below**: PR #57's Windows arm64 leg failed once with a
transient `502` from GitHub while vcpkg downloaded `cpython-v3.12.13.tar.gz` (a dependency-download
network flake, not a code issue — confirmed by reading the actual job log directly, which *was*
reachable this time, unlike the earlier macOS incident). Retriggered via an empty-commit push (this
session's git-credential-token workaround has no `actions:write` scope, so the Actions API's
"re-run failed jobs"/"rerequest check run" endpoints both 403 — an empty commit is the reliable
fallback). Cleared on retry with no code change, consistent with genuine transient infra, unlike the
earlier macOS/Homebrew incident (which was deterministic and needed a real fix, not a retry).

**Recurring CI flake — now at 4 confirmed occurrences, worth a dedicated look soon**:
`test/native/event_loop.test.ts`'s timing-sensitive assertion (`MAX_ALLOWED_TICK_GAP_MS`/the
duration-scaled threshold) has flaked on Windows CI four separate times across unrelated PRs now
(chunk 3 of `util.element` is the latest), always clearing cleanly on retrigger with no code
change. Not currently blocking anything, but four independent occurrences is a real, load-bearing
signal that the threshold (or the whole approach of measuring wall-clock timer-tick gaps on a
shared, variably-loaded Windows CI runner) needs revisiting — worth prioritizing the next time
someone's touching Phase 1's async-primitive tests, rather than continuing to treat each occurrence
as a one-off.

**New recurring CI infra flake, first seen 2026-09-11**: `build-and-test (macos, x64, macos-14,
true)`'s "Install build dependencies (macOS, x64 cross-compile)" step (`ci-ifcopenshell-ts.yml`, an
uncached `curl | bash` Homebrew install + `brew install boost eigen pkg-config` run fresh on every
CI run, no caching) failed **4 times in a row** on PR #43 (`selector.py`'s `filter_elements` chunk)
within ~1.5 hours, always with an identical signature: fails in under a minute, before any log
output the diagnostic-comment step can capture ("no log files found" every time), while every other
one of the 13 other checks stayed green across all 4 attempts. Confirmed NOT a permanent break: the
same leg passed cleanly on PR #39 and PR #41 earlier the same day. `raw.githubusercontent.com`'s
install script URL itself was reachable from the orchestrating session's own network at the time —
points to a degraded/intermittent path specific to GitHub's macOS runner egress or Homebrew's bottle
CDN during this window, not a code or CI-config problem in this repo. Retried 4 times with
increasing gaps (immediate, immediate, ~45min, ~30min) per this project's flaky-shaped-failure
retrigger discipline; if it keeps recurring, the real fix is caching the x64 Homebrew prefix (the
same "vcpkg's GHA binary cache" pattern `ci-ifcopenshell-ts.yml` already uses for the Windows legs,
just not yet applied to this one) so a transient network blip during dependency-install can't take
the whole leg down — worth prioritizing if this keeps recurring on unrelated PRs, matching the
Windows `event_loop.test.ts` flake's own "four independent occurrences is a real signal" bar above.

**Resolved, no longer tracked**: the CI-caching PR's (`#8`) merge once looked untraceable to any
action by the orchestrating session and was raised as an open trust/process concern. The user
investigated independently and confirmed it was neither a rogue self-merge nor a human merging by
hand — some other legitimate mechanism outside this session's visibility. Noted in the
orchestrating session's own memory for future reference in case it recurs.

**Standing strategic question, not yet decided**: the user raised whether this fork's TS port
should eventually be upstreamed via PR to the canonical `IfcOpenShell/IfcOpenShell` project rather
than the fork owning a permanent parallel npm package. Orchestrator's recommendation (given, not
yet acted on): coordinate with upstream maintainers early (an issue/discussion, before a large
surprise PR) rather than after Phase 3+ is far along — user said to hold off on drafting that for
now and keep porting.

## Operational note: worktree isolation workaround

The Agent tool's built-in `isolation: "worktree"` fails in this sandbox (`EPERM` creating `.claude/`
inside this repo — confirmed not transient, reported as a product bug). Workaround in use for every
chunk: the orchestrating session manually runs `git worktree add -b <branch> <scratch-path> v0.9.0`
first, then dispatches the agent with no `isolation` param, instructed to `cd` into that pre-made
path as its first step. Branch naming convention: `ts/phase-<n>-<slug>`.

## Lane map (active from Phase 3 onward — Phases 0–2 are strictly serial)

- **Lane A** (main line): `util` Tier A → `api` Tier 1 → `api` Tier 2 → `api` Tier 3
- **Lane B**: `util` Tier B — needs Lane A's `util.element`/`util.schema`/`util.unit` landed first
- **Lane C**: `selector.py` query DSL — independent from Phase 2 onward, no `util`/`api` dependency
- **Lane D**: `api` Tier 4 non-kernel — needs Phase 4's matrix library + Phase 6's Tier 1 landed

Concurrency cap: 2–3 agents in flight at once, so review stays trackable.

---

## Phase 0 — Scaffolding

| Chunk | Status | PR | Notes |
|---|---|---|---|
| Package skeleton + native addon smoke test + CI (6 OS×arch combos) | ✅ | [#1](https://github.com/mikitski/IfcOpenShell/pull/1) | Landed `8931001035`; 5 real CI bugs found+fixed during bring-up |

## Phase 1 — Low-level binding

Split into sequential chunks (too large for one PR): the spike had to resolve before the primitive
binding could be built, since it decided generated-vs-hand-written.

| Chunk | Status | PR | Notes |
|---|---|---|---|
| `wrappergen` validation spike (generated vs. hand-written decision) | ✅ | [#3](https://github.com/mikitski/IfcOpenShell/pull/3) | Landed `a8b2a7517` — **PASS**: extend wrappergen. 5 bugs found+fixed, incl. a confirmed use-after-free (new "borrowed" handle-kind). See `research/06-wrappergen-spike-results.md`. |
| `file`/`entity_instance` primitives + schema introspection | ✅ | [#5](https://github.com/mikitski/IfcOpenShell/pull/5) | Landed `bf11a824e`. Completed the variant dispatch (BINARY+AGGREGATE), full schema-introspection class set, wired into `src/ifcopenshell-ts`'s real build. **Empirically resolved fresh-wrapper-per-access: fresh wrapper, confirmed** — see `research/07-fresh-wrapper-per-access.md`. 4 real bugs found+fixed by the orchestrator during review/CI (agent's session died mid-task to an unrelated auth error): Black formatting, a CI diagnostic-path bug, an MSVC tribool-conversion ambiguity, an `IFC_PARSE_API`/dllimport linkage bug. |
| Async primitive variants (`napi_create_async_work`) | ✅ | [#7](https://github.com/mikitski/IfcOpenShell/pull/7) | Landed `80f784c22`. Promise-based siblings for file open/parse, `get_all_attribute_values`, `write`. 3 real bugs found+fixed by the orchestrator (agent stalled mid-task): Black formatting, stale checked-in TS facade copy, an MSVC `inline`+`IFC_PARSE_API` linkage bug in `src/ifcparse/utils.h` (user-confirmed before touching core C++). |
| Native memory accounting (`napi_adjust_external_memory`) | ✅ | [#9](https://github.com/mikitski/IfcOpenShell/pull/9) | Landed `abeb57ee7`. Per-class GC-pressure hints on every wrap/finalize pair (`file` gets a documented coarse 1 MiB stand-in; `"borrowed"` classes bill only their wrapper, never the singleton pointee); `file.dispose()`/`[Symbol.dispose]` never double-frees (resets the `shared_ptr`, leaves the struct for the one real finalizer `delete`). Real `shared_ptr` race (dispose vs. in-flight async op) found+fixed by the agent's own self-review, guarded by a new `async_refcount` counter. 1 issue found+fixed by the orchestrator: 5 Black formatting violations in `emit.py`. |
| ASAN/UBSan CI + fuzz testing of parse primitives | ✅ | [#13](https://github.com/mikitski/IfcOpenShell/pull/13) | Landed `9d0dda897`. New `asan-ubsan` (Linux x64) + `fuzz` (real libFuzzer harness, `native/fuzz/`) CI jobs — neither existed anywhere in this repo before. Found+fixed 3 real `src/ifcparse` bugs (a `spf_header` leak, the Rule-of-Five UAF that fix exposed, a `token::to_string`/`as_string` stack-overflow) and deferred 1 systemic one (regular entities never freed — `detect_leaks=0` on `fuzz`, documented in `TODOS.md`). See "Current focus" above for full detail. |

## Phase 2 — Core TS layer

| Chunk | Status | PR | Notes |
|---|---|---|---|
| "Fresh wrapper per access" identity spike (blocks rest of phase) | ✅ | [#5](https://github.com/mikitski/IfcOpenShell/pull/5) | Resolved empirically during Phase 1's primitive-binding chunk — **fresh wrapper, confirmed**. See `research/07-fresh-wrapper-per-access.md`. |
| `IfcFile` (`file_mixin` port, incl. `dispose()`) | ✅ | [#15](https://github.com/mikitski/IfcOpenShell/pull/15) | Landed `6b9963954`. Identity-keyed `Transaction`/undo-redo registry, full `Transaction` port, `createEntity`/`add`/`byType`/`traverse`/`getInverse`/`remove`/`batch`/`write`/`[Symbol.iterator]`/`dispose()` (delegates to Phase 1's native `dispose()`). New `file_pointer()` primitive added for the registry key. |
| `EntityInstance` (Proxy + attribute-metadata cache) | ✅ | [#15](https://github.com/mikitski/IfcOpenShell/pull/15), [#17](https://github.com/mikitski/IfcOpenShell/pull/17) | Foundation (`identity()`/`isA()`/`equals()`, `.get()`/`.set()`, `getInfo()`, `walk()`) landed `6b9963954`; `Proxy` wrapping + the `Map<schemaIdentifier, Map<className, AttributeMeta[]>>` cache landed `03000ac47`. Every `EntityInstance` is `Proxy`-wrapped at construction; `instanceof` confirmed to still hold. Cache differential test caught 2 real bugs (a derived-attribute miscount, an inverse-attribute name collision across sibling relationship classes). |
| `.d.ts` generator (or hand-written fallback for high-traffic classes) | ✅ | [#17](https://github.com/mikitski/IfcOpenShell/pull/17) | Landed `03000ac47`. New Node-based tool (not wrappergen/Python — no recoverable supertype name via the current primitive surface, so interfaces are flat, not `extends`-chained, a disclosed deviation). **2305 typed entity interfaces generated (653/776/876 across IFC2X3/IFC4/IFC4X3), none skipped.** `10-architecture.md` §6 exit criterion passes (`wall.Name` type-checks as `string \| null` via real `tsc --noEmit`). Found+fixed a Windows-only `spawnSync npx ENOENT` bug in the type-check test itself along the way. |
| `guid.ts`, `settings.ts`, `template.ts` | ✅ | [#15](https://github.com/mikitski/IfcOpenShell/pull/15) | Landed `6b9963954` alongside `IfcFile`/`EntityInstance`. |
| Differential cache-correctness test (all 3 schema versions) | ✅ | [#17](https://github.com/mikitski/IfcOpenShell/pull/17) | Landed `03000ac47`, alongside the Proxy/cache chunk above (same test, same PR — see that row's notes). |

## Phase 2.5 — Alpha checkpoint ⏸️ npm-publish confirmation required

| Chunk | Status | PR | Notes |
|---|---|---|---|
| Benchmark suite + one-time Python-baseline comparison | ✅ | [#19](https://github.com/mikitski/IfcOpenShell/pull/19) | Landed `ab0541483`. 3 CI-gating benchmarks, dedicated Linux-x64 `benchmark` job. Python-baseline script included but not run (no Python/SWIG build in this project's CI); `tools/BASELINE_RATIOS.json` is an unpopulated placeholder pending a maintainer running it locally. |
| `npm publish ifcopenshell@alpha` | 🔲 | — | ⏸️ Explicit user go-ahead requested (per "do benchmarks first" direction) — **not yet given**. README usage example also still needed for this exit criterion. |

## Phase 3 — `util` Tier A [Lane A]

| Chunk | Status | PR | Notes |
|---|---|---|---|
| `util.element` — chunk 1/3 (psets/qtos + type/material/style) | ✅ | [#21](https://github.com/mikitski/IfcOpenShell/pull/21) | Landed `bba589be7`. `getPset`/`getPsets`/`getQuantity`/`getQuantities`/`getProperty`/`getProperties`/`getElementsByPset`/`hasProperty`/`getPropertyDefinition`, `getType`/`getTypes`/`getMaterial(s)`/`getMaterialLayers`/`getMaterialProfiles`/`getStyles`/`getPredefinedType`/`isUserdefinedType`/`getElementsByMaterial`/`getElementsByStyle`/`getElementsByRepresentation`. Found+fixed a real CI gap (see "Current focus" above): `SCHEMA_VERSIONS=4`-only means IFC2X3/IFC4X3 tests need `bootstrap.ts`'s `AVAILABLE_SCHEMAS` skip-guard, not a hard-coded schema string — use this same guard in chunks 2/3. |
| `util.element` — chunk 2/3 (spatial/structural-graph queries) | ✅ | [#23](https://github.com/mikitski/IfcOpenShell/pull/23) | Landed `955fdd594`. `getContainer`/`getReferencedStructures`/`getStructureReferencedElements`/`getDecomposition`/`getGroupedBy`/`getGroups`/`getControls`/`getParent`/`getFilledVoid`/`getVoidedElement`/`getAdheredElement`/`getAggregate`/`getNest`/`getParts`/`getContained`/`getComponents`/`getOpenings`/`hasOpenings`. Found+fixed a real null-safety bug in the shared `EntityInstanceSet` helper (see "Current focus" above). |
| `util.element` — chunk 3/3 (structural-editing helpers) | ✅ | [#25](https://github.com/mikitski/IfcOpenShell/pull/25) | Landed `ea575b238`. `copy`/`copyDeep`/`removeDeep`/`removeDeep2`/`batchRemoveDeep2`/`unbatchRemoveDeep2`/`replaceElement`/`replaceAttribute`. **`util/element.py` is now fully ported.** 2 real primitive gaps found+disclosed (file-to-string, `type_of_aggregation`), 1 real V8 spread-limit bug found+fixed — see "Current focus" above. |
| `util.attribute` | ✅ | [#27](https://github.com/mikitski/IfcOpenShell/pull/27) | Landed `d0e1742c2` alongside `util.schema` chunk 1 (small, self-contained prerequisite — see below). `getPrimitiveType`/`getEnumItems`/`getSelectItems`. |
| `util.schema` — chunk 1 (query/reflection + `BatchReassignClass`) | ✅ | [#27](https://github.com/mikitski/IfcOpenShell/pull/27) | Landed `d0e1742c2`. `getFallbackSchema`/`getDeclaration`/`isA`/`getSupertypes`/`getSubtypes`/`geometryClassesIntroducedAfter`/`ifc4OnlyGeometryClasses`/`reassignClass`/`BatchReassignClass`. 3 real primitive gaps investigated+disclosed (`simple_type::declared_type`, `enumeration_type::enumeration_items`, `entity::subtypes` — the last one worked around, not just disclosed) — see "Current focus" above. |
| `util.schema` — chunk 2 (`Migrator`) | ✅ | [#33](https://github.com/mikitski/IfcOpenShell/pull/33) | Landed `d256978d2`. `migrate`/`migrateClass`/`migrateAttributes`/`findEquivalentAttribute`/`migrateAttribute`/`generateDefaultValue` + `_enum_value_outside_target`. First JSON-data-bundling precedent (`data/schema-migration/`). 2 primitive-layer gaps disclosed (see "Current focus" above / `TODOS.md`). |
| `util.unit` | ✅ | [#29](https://github.com/mikitski/IfcOpenShell/pull/29) | Landed `7fbe3dfed`. Full port except `convert_file_length_units` (genuine hard blocker, see `TODOS.md`). Resolved a wrapping-behavior question vs. `util.element` chunk 1's finding (two genuinely different, both-correct code paths); surfaced a real separate bug in `util/element.ts`'s `getProperty`/`getProperties`, fixed in a follow-up PR. |
| `util.type` | ✅ | [#36](https://github.com/mikitski/IfcOpenShell/pull/36) | Landed `bba7bab7c`. `getApplicableTypes`/`getApplicableEntities` + module-load-time `entityToTypeMap`/`typeToEntityMap` construction (IFC2X3's `IfcBuildingElementProxyType` prioritization + "guessed element" narrowing, both verbatim). New JSON-data-bundling instance (`data/type-map/`, `migrator.ts`'s precedent) — 3 data files independently verified byte-identical to the Python source. |
| `util.classification` | ✅ | [#36](https://github.com/mikitski/IfcOpenShell/pull/36) | Landed `bba7bab7c`. `getReferences`/`getClassification`/`getInheritedReferences`/`getClassificationData`. Promoted `selector.ts`'s pre-existing narrow re-implementations to this real module; `selector.ts` updated to call the real functions, narrow copies removed. |
| `util.constraint` | ✅ | [#36](https://github.com/mikitski/IfcOpenShell/pull/36) | Landed `bba7bab7c`. No Python test file exists for this module (`test_constraint.py` doesn't exist) — original test coverage written. |
| `util.system` | ✅ | [#36](https://github.com/mikitski/IfcOpenShell/pull/36) | Landed `bba7bab7c`. `getElementSystems`/`getElementZones`/`getSystemElements`/`isAssignable`/`getPorts`/`getConnectedTo`/`getConnectedFrom`. Preserves a real, verified Python-source typo (`FLOW_DIRECTION`'s `"NOTEDEFINED"` vs. the schema's real `"NOTDEFINED"`) verbatim, disclosed not corrected. Also promoted from `selector.ts`'s narrow copies (see `util.classification` row). |
| `util.date` | ✅ | [#35](https://github.com/mikitski/IfcOpenShell/pull/35) | Landed `321b71b89`. Full port, no new npm dependency — hand-rolled ISO 8601 duration parsing/formatting replaces Python's `isodate`/`dateutil`, verified line-by-line against the real `isodate` source. Correctly reproduces a real upstream `isodate` dead-code bug (fuzzy-parse "M" misread as months not minutes in some strings). One disclosed gap: `string_to_date`'s `dateutil` fuzzy free-text parsing not reproduced (zero internal callers, confirmed by grep). A real narrow bug (`parseIso8601Duration` wrongly rejecting valid `"PT"`) found by independent review before merge, fixed. |
| `util.file` | ✅ | [#38](https://github.com/mikitski/IfcOpenShell/pull/38) | Landed `d2f4aa965`. `IfcHeaderExtractor` (STEP-header-only extraction, no full model parse). `.ifczip` reading needed real ZIP decompression (real save path uses DEFLATE, not just STORED) — solved with `node:zlib`'s `inflateRawSync` + a small hand-rolled EOCD/Central-Directory/Local-File-Header reader, no new npm dependency. Independently re-reviewed byte-offset-by-byte-offset against the real ZIP spec. |
| `util.pset` | ✅ | [#39](https://github.com/mikitski/IfcOpenShell/pull/39) | Landed `ca80783a0`. `PsetQto`/`getTemplate`/`getPsetTemplateType`/`parseApplicableEntity`/`convertApplicableEntitiesToQuery`. Bundled template files are plain STEP text (not XML/JSON as the dispatch brief guessed) — reused the existing STEP parser, no new tooling. Corrects the dispatch brief's mutating-function assumption (only a one-time internal-only IFC4 backport patch, no undo/redo test needed). |
| `util.resource` | ✅ | [#60](https://github.com/mikitski/IfcOpenShell/pull/60) | Landed `ad5fa71c8`. `getProductivity`/`getParentProductivity`/`getUnitConsumed`/`getQuantityProduced`/`getQuantityProducedName`/`getTotalQuantityProduced`/`getParametricResourceProducts`/`getTaskAssignments`/`getResourceRequiredWork`/`getNestedResources`/`getCost`/`getQuantity`/`getParentCost`. No mutating functions, no Transaction test needed. Real finding: `date.ts`'s unified `Duration` fields need explicit `timedelta`-style day/second normalization before use (see the note above) — fixed with a local bridging helper, verified with a dedicated test. No Python test file exists for this module — all 41 tests are original coverage. **This completes Phase 3 (`util` Tier A).** |
| `util.doc` | ✅ | [#54](https://github.com/mikitski/IfcOpenShell/pull/54) | Landed `89199d0fe`. Runtime lookups only (`getDb`/`getSchemaByName`/`getClassSuggestions`/`getEntityDoc`/`getAttributeDoc`/`getPredefinedTypeDoc`/`getPropertySetDoc`/`getPropertyDoc`/`getTypeDoc`/`getInverseAttributes`) — `DocExtractor`/`run_doc_api_examples()` (the build-time doc scraper) correctly excluded, Tier C. Bundles 10 real doc-data JSON files (several MB, independently verified byte-identical). 3 real, disclosed discrepancies between Python's own type hints and its actual data/behavior found+verified against the real bundled data (`get_class_suggestions` returns a list not a single object; `PsetData.properties` values are structured objects not bare strings). Found+fixed a real, previously-undisclosed CI gap: `biome`'s 1 MiB file-size cap silently rejected one of the new data files. Independently re-reviewed by the orchestrating session against the real Python source and real data before merge; no bugs found. |
| `util.mvd_info` | ✅ | [#41](https://github.com/mikitski/IfcOpenShell/pull/41) | Landed `2ac5fd564`. Hand-rolled parser for the `ViewDefinition`/`Comment`/`ExchangeRequirement`/`Option`/dynamic-keyword grammar (no new npm dependency); `MvdInfo`/`DictionaryHandler`/`AutoCommitList` as `Proxy`-based write-back wrappers. Grammar behavior pinned down by empirically probing a real `lark` install against ~20 inputs, not just reading the grammar text — several real quirks found and preserved verbatim (whitespace-absorbing `value` regex, an `Option` kv-success `keywords`-omission bug, dead grammar productions). `spf_header` has no `file_description()` accessor yet (pre-existing Phase 2 gap, disclosed in `TODOS.md`) — `MvdInfo` can't yet wire to a real `IfcFile.header()`. A real, undisclosed bug (the Proxy committing on `sort()`/`reverse()`, which real Python's `AutoCommitList` verifiably never does) found by independent review and fixed before merge. |

## Phase 4 — `util` Tier B [Lane B — needs `util.element`/`schema`/`unit`]

| Chunk | Status | PR | Notes |
|---|---|---|---|
| `util.placement` (`gl-matrix`) | ✅ | [#45](https://github.com/mikitski/IfcOpenShell/pull/45) | Landed `a71dd6a8b`. `a2p`/`getAxis2placement`/`getLocalPlacement`/`getCartesiantransformationoperator3d`/`getMappeditemTransformation`/`getStoreyElevation`/`rotation`. First Phase 4 chunk, adds `gl-matrix@3.4.4` as this project's first runtime npm dependency (an already-made project decision, not this chunk's own call). numpy→gl-matrix mapping (composition order, row/column-major layout) empirically verified against the real library, cross-checked a second, independent way (a from-scratch pure-Python re-implementation, no numpy). Real findings: `gl-matrix`'s default `Float32Array` silently loses precision at UTM-scale coordinates (fixed via `setMatrixArrayType(Float64Array)`); `IfcAxis2PlacementLinear`'s non-Cartesian fallback needs the unported `ifcopenshell.geom` kernel, throws a disclosed error. A real bug (`rotation(0,"X")`'s test comparing `-0` vs `0` too strictly) found by independent review and fixed. Bonus: unblocked `selector.ts`'s `x`/`y`/`z` key-path keys. |
| `util.geolocation` | ✅ | [#48](https://github.com/mikitski/IfcOpenShell/pull/48) | Landed `0b3366f49`. `HelmertTransformation`, `dms2dd`/`dd2dms`, `xyz2enh`/`auto_xyz2enh`/`enh2xyz`/`auto_enh2xyz`, `z2e`/`auto_z2e`, `local2global`/`auto_local2global`/`global2local`/`auto_global2local`, `get_helmert_transformation_parameters`/`get_crs`/`get_wcs`/`get_grid_north`/`get_true_north`. No new npm dependency — reuses `gl-matrix` plus a hand-built `BigInt`-backed exact-decimal implementation for `dd2dms`'s `Decimal`+`ROUND_HALF_UP` (a genuinely different rounding rule from `util.unit`'s `pythonRound`/`ROUND_HALF_EVEN`, correctly not conflated). Numerical verification matches `util.placement`'s bar: a novel non-trivial test case (non-identity rotation + arbitrary map rotation + non-unit scale) cross-checked two independent ways; `dd2dms` verified byte-for-byte against a real Python interpreter. Wires `selector.ts`'s `easting`/`northing`/`elevation` keys (narrowing the remaining blocker to just `rotation_*`, still needs `util.shape_builder`). 2 real bugs found+fixed by `/code-review` (exponential-notation crash in `dd2dms`; unchecked `mat4.invert` null return). Independently re-reviewed by the orchestrating session against the real Python source (composition order, basis-column renormalization, parameter-unpacking order); no further bugs found. |
| `util.representation` | ✅ | [#50](https://github.com/mikitski/IfcOpenShell/pull/50) | `getContext`/`isRepresentationOfContext`/`getRepresentationsIter`/`getRepresentation`/`guessType`/`resolveRepresentation`/`resolveItems`/`resolveBaseItems`/`getPrioritisedContexts`/`getPartOfProduct`/`getItemShapeAspect`/`getMaterialStyle` fully ported; `getReferenceLine`'s primary axis-representation path fully ported, its `util.shape.get_base_extrusions` fallback a disclosed hard blocker (thrown only when Python's own `elif` would fire). No Python test file exists for this module (`test_representation.py` doesn't exist) — original test coverage written, matching `util.constraint`'s precedent. Four real Python-source bugs found and preserved verbatim, disclosed: `get_part_of_product`'s `"IFX2X3"` typo (always-true schema check, contradicting its own docstring), `get_part_of_product` also throwing (matching Python's own unguarded `element.RepresentationMaps` access) for a genuinely unset `RepresentationMaps` — found by an independent adversarial `/code-review` pass, an earlier version silently avoided the crash with a `?? []` fallback — `resolve_items`'s asymmetric identity-shortcut (silently discards the caller's accumulated matrix on an identity-transform `IfcMappedItem`), and `get_prioritised_contexts`' `IDENTIFIER_PRIORITY` `"Body-FallBack"`/`REPRESENTATION_IDENTIFIER` `"Body-Fallback"` casing mismatch (found by this chunk's own `/code-review` pass — a real `"Body-Fallback"` context never matches the priority list, ranking last instead of second). `guessType`'s `Curve2D`/`Curve3D`/`Surface2D`/`Surface3D` branches are genuinely blocked by the pre-existing `entityInstance.ts` "no EXPRESS DERIVED attribute" gap (`.Dim`) — pinned by a regression test, not silently worked around; see `TODOS.md`. `guessType`'s `SolidModel`/`Point` branches also verified (against the real IFC4 class hierarchy) to shadow several later branches (`AdvancedSweptSolid`/`Brep`/`AdvancedBrep`/`PointCloud`), which are dead code in real Python too — pinned by dedicated tests rather than assumed. `getPrioritisedContexts`'s sort was changed to a Schwartzian transform (precompute each context's sort key once) after `/code-review` found the original comparator recomputing keys on every pairwise comparison; `matAllcloseIdentity`'s per-call `mat4.create()` allocation was hoisted to a module-level constant for the same reason. Unblocked two pre-existing cross-file gaps: `util/element.ts`'s `getStyles` now calls the real `getRepresentation` (removed the narrow `findBodyRepresentation` stand-in); `util/selector.ts`'s `"profiles"` key blocker narrowed to name only the still-missing `util.shape`. |
| `util.cost` (hand-rolled formula parser) | ✅ | [#56](https://github.com/mikitski/IfcOpenShell/pull/56) | Landed `1e412a4e1`. Full port of ~19 functions + `CostValueUnserialiser` (hand-rolled recursive-descent parser for the embedded `lark` grammar, no new npm dependency, matching `selector.ts`'s precedent — this sandbox has no network access to install `lark` itself to empirically fuzz the grammar, a disclosed limitation mitigated by thorough round-trip serialise→parse→serialise tests). Two genuine, faithfully-preserved Python-source grammar quirks found by tracing `get_formula`/`get_operand` directly (mixed-operator "last operator wins"; a doubly-nested category-only operand silently dropping its inner `Category`). Real, disclosed correction to the chunk's own task brief: `unserialise_cost_value` is a pure parser/mapper in real Python, not a mutating function (the actual mutation lives in the unported `ifcopenshell.api.cost.edit_cost_value_formula`) — ported faithfully as pure, with a Transaction/undo-redo regression test covering the realistic `.set()`-based workflow instead. `getCostScheduleTypes` works around the pre-existing, always-throwing `util.attribute.getEnumItems` primitive gap by substituting the bundled `util.doc` JSON data, verified byte-for-byte equivalent to the real schema enum by cross-checking `Ifc4-schema.cpp` directly. One real bug found+fixed by `/code-review` (`getCostAssignmentsByType` silently treating an unrecognised `filterByType` value as "no filter" instead of Python's actual literal-fallthrough behavior). Independently re-reviewed by the orchestrating session directly against the real Python source before merge; no further bugs found. |
| `util.shape` | ✅ (4/43 functions) | [#66](https://github.com/mikitski/IfcOpenShell/pull/66) | Landed `7c70f4119`. Only `isX`/`getProfiles`/`getExtrusions`/`getBaseExtrusions` have no `ifcopenshell.geom` dependency — ported for real. The other 39 functions all take `W.triangulation`/`ShapeElementType` (kernel-generated geometry types), and this TS port has no `ifcopenshell.geom` binding of any kind yet (confirmed by grep, independently disclosed already in `util/placement.ts`'s header) — a much bigger blocker than the module's 3 `shapely`/`unary_union` call sites that the roadmap doc's "just needs `polygon-clipping`" framing focused on; `polygon-clipping` alone would not meaningfully unblock this module. Disclosed via a single documented deferral in `shape.ts`'s header comment (every function named, grouped by parameter shape) plus a new `TODOS.md` entry, rather than 39 near-identical throwing stubs — a deliberate departure from this project's usual per-function-stub precedent, reasoned explicitly: none of the 39 is "more done" than another, so individual stubs would be padding, not disclosure. **Resolves a real, pre-existing cross-file gap**: `util.representation.ts`'s `getReferenceLine` `util.shape.get_base_extrusions` fallback (previously a disclosed hard blocker) is now wired up for real, including a preserved Python empty-list-is-falsy subtlety (`extrusions.length > 0`, not just non-null). Independently re-reviewed by the orchestrating session directly against the real Python source before merge (the chunk's own `/code-review` invocation hit this project's known fork-reviews-wrong-repo-state issue again, reviewing an unrelated already-landed file — the agent correctly fell back to its own direct self-review, matching this project's established mitigation); no further bugs found. |
| `util.shape_builder` — part 1 (core) | ✅ | [#58](https://github.com/mikitski/IfcOpenShell/pull/58) | Landed `d4f59d895`. Split into two chunks given its unusual size (2312 lines, 60 methods) — this is everything EXCEPT the four MEP transition/bend methods (`mep_transition_shape`/`mep_transition_length`/`mep_transition_calculate`/`mep_bend_shape`, entirely unported, not stubbed — Part 2's job). Confirmed kernel-free (no `shapely` dependency) per the roadmap; reuses `gl-matrix` for 4x4 work, plain `number[]` elsewhere (deliberately avoiding gl-matrix's typed arrays, which would silently break `EntityInstance`'s `Array.isArray`-based attribute-write dispatch). Numeric verification per the Tier-B bar: `npRotationMatrix`'s Rodrigues branch verified via an independently-checkable cyclic-permutation property (120° rotation around the cube diagonal); `npMatrixToEuler`'s `Rz@Ry@Rx` composition order determined empirically (not assumed from the Python docstring) with a non-trivial angle triple; `npIntersectLineLine` cross-checked against an independent Cramer's-rule solution. Two real, pre-existing, disclosed **blocking** primitive-layer gaps surface significantly here (both already in `TODOS.md`, not introduced by this chunk): the DERIVED-attribute (`Dim`) gap blocks `profile()`/`createSweptDiskSolid()` unconditionally; the "can't write an initial value into a fresh defined-type instance" gap blocks `IfcLineIndex`/`IfcArcIndex` creation, meaning **`rectangle()` is currently blocked on IFC4/IFC4X3** (works on IFC2X3, which needs neither). Both preserved faithfully (real throws, pinned by regression tests), not silently worked around — a substantial functional surface (face-sets, `vertex`/`edge`/`face`, `sphere`/`block`/`plane`, both placement-creation methods, `circle`/`createEllipseCurve`, `translate`/`rotate`/`mirror`) is fully working and tested regardless. Two real Python-source quirks preserved verbatim and disclosed: `profile()`'s inner-curve `Dim` check only runs for the single-non-array-curve case (a real indentation-scoped bug in the Python source); `rotateExtrusionKwargsByZ`'s `counterClockwise` parameter is accepted but never used. Independently re-reviewed by the orchestrating session directly against the real Python source before merge (per the fork-reliability mitigation above; the chunk's own in-agent `/code-review` invocation hit the same fork-context-bleed issue as this project's other known occurrences, reviewing stale/wrong repo state — the agent correctly fell back to a direct self-review instead, matching this project's established mitigation); no further bugs found. |
| `util.shape_builder` — part 2 (MEP transition/bend geometry) | ✅ | [#61](https://github.com/mikitski/IfcOpenShell/pull/61) | Landed `b448c9275`. `mepTransitionShape`/`mepTransitionLength`/`mepTransitionCalculate`/`mepBendShape`. **`util/shape_builder.py` is now fully ported** (both parts). `mepTransitionShape`/`mepTransitionLength`/`mepTransitionCalculate` are fully functional and tested (never hit either of Part 1's disclosed primitive-layer gaps); `mepBendShape` is unconditionally blocked on every schema (a new transitive finding, investigated directly: on IFC4/IFC4X3 it hits the `IfcLineIndex`/`IfcArcIndex` gap via `polyline(arcPoints=...)`; on IFC2X3 `IfcMaterialProfileSet` doesn't exist at all, so no profile can ever resolve) — ported faithfully anyway, pinned by regression tests for both causes. Three real Python-source quirks preserved verbatim: `mep_transition_calculate` silently returns `None` when both `length` and `angle` are already given; `mep_transition_length`'s `or`-based short-circuit (dormant, never exercised since a legitimate zero-length result is unreachable in practice); `"Tesselation"` (missing the second "l") passed as `representationType`, a genuine upstream typo. Ported `test_shape_builder.py`'s real `TestCalculateTransitions` class verbatim (all 6 cases). A real CI-only bug (not a code bug) was found post-review: a new `mepBendShape` test hardcoded `createTestFile("IFC2X3")` without this project's established `AVAILABLE_SCHEMAS`-gating convention, causing real build-and-test failures on 3 platforms (CI's core build is `SCHEMA_VERSIONS=4`, IFC4-only) — diagnosed from the actual CI log and fixed directly by the orchestrating session (`test.skipIf(!AVAILABLE_SCHEMAS.includes("IFC2X3"))`, matching `doc.test.ts`'s own precedent) before merging. A real merge conflict against the concurrently-landed `util.resource` (both touched `util/index.ts`'s header comment/export list) was also resolved by hand and reverified (full `vitest run`, 1160/1160 passing) before merging. Independently re-reviewed by the orchestrating session directly against the real Python source before merge (traced the trickiest math — `mepTransitionCalculate`'s length/angle formulas, including verbose-only debug paths — line-by-line); no further bugs found. |
| `util.alignment` | ✅ | [#64](https://github.com/mikitski/IfcOpenShell/pull/64) | Landed `58980ff4f`. Only `stationAsString` is portable — the other 3 functions (`addLinearPlacementFallbackPosition`/`createAlignmentGeometry`/`appendZeroLengthSegments`) call straight into the entirely-unported `ifcopenshell.api.alignment` (Phase 6+, `src/ifcopenshell-ts/src/api/` doesn't exist anywhere yet) and are ported as thin, disclosed throwing stubs naming the exact missing dependency, not silently omitted or faked. `stationAsString`'s number-formatting logic (near-shifter-boundary rounding correction, negative-zero-station edge case) verified byte-for-byte against a real CPython interpreter for SI/millimetre/Imperial-foot/custom-named-conversion-unit cases, including a crafted near-boundary regression. Preserves a real Python-source unguarded-`None` crash verbatim (a file with no `LENGTHUNIT` throws, matching Python's own `AttributeError`). Ported `test_alignment.py`'s real `test_station_as_string` verbatim. Independently re-reviewed by the orchestrating session directly against the real Python source before merge; no bugs found. |

## Phase 5 — `selector.py` query DSL [Lane C — independent from Phase 2]

| Chunk | Status | PR | Notes |
|---|---|---|---|
| Key-path grammar / `get_element_value` (shared dependency of the other 2 grammars) | ✅ | [#32](https://github.com/mikitski/IfcOpenShell/pull/32) | Landed `dba7cc445`. `parseKeyPath` + `getElementValueForKeys`. 2 disclosed blockers (positional/geolocated keys, `"profiles"`'s extrusion fallback) — see "Current focus" above / `TODOS.md`. |
| `filter_elements` facet grammar | ✅ | [#43](https://github.com/mikitski/IfcOpenShell/pull/43) | Landed `2bd2986e7`. Instance/entity/attribute/type/material/property/classification/location/group/parent filters, `query:` facet reusing `getElementValue`, comparisons (incl. regex/contains), `,`/`+` OR/AND combinators via a dedicated `FacetRunner` class. Disclosed `int`-vs-`float` numeric-comparison gap (cross-referenced with the pre-existing `EntityInstance.getByIndex` INTEGER/REAL-collapse `TODOS.md` entry, not a new one). Independently re-reviewed against the real Python source before merge; no bugs found. Blocked 8+ hours by the now-fixed `macos-x64` CI flake (see #46 below). |
| `format()` expression grammar | ✅ | [#44](https://github.com/mikitski/IfcOpenShell/pull/44) | Landed `a1ae5f27b`. Completes all three `selector.py` grammars. Hand-rolled recursive-descent expression parser (operator precedence), every `FormatTransformer` function (`round`/`number`/`int`/`metric_length`/`imperial_length`/`lower`/`upper`/`title`/`concat`/`substr`/`sort`/`reverse`/`join`/`{{...}}` interpolation). `/code-review` found+fixed 4 real bugs (`round(x,0)` division-by-zero, `sort()` over booleans, a duplicated `pythonRound` helper, an under-scoped disclosure). Hit and resolved 2 real merge conflicts with concurrently-landing `filter_elements`/`util.placement` (overlapping extracted helpers, shared test-file header comment). |

## Phase 6 — `api` Tier 1 [Lane A]

| Chunk | Status | PR | Notes |
|---|---|---|---|
| Pre/post-listener hook system (cross-cutting, do first) | ✅ | [#68](https://github.com/mikitski/IfcOpenShell/pull/68) | Landed `aaefa510e`. Port of `ifcopenshell/api/__init__.py`'s pre/post-listener plumbing (`add_pre_listener`/`add_post_listener`/`remove_pre_listener`/`remove_post_listener`/`remove_all_listeners`, `wrap_usecase`). Confirmed out of scope: `run()`/`CACHED_USECASES` (string dispatch, already decided out), `ARGUMENTS_DEPRECATION`+helpers (confirmed empty/unused), `extract_docs()` (stale Bonsai node-graph helper), `serialise_settings()` (downstream Bonsai-undo concern), `owner.settings` monkeypatch (separate `api.owner`-specific hook, later chunk). `wrap_usecases()` itself (the `pkgutil`-reflection auto-wrapper) is *not* ported — each future `api/<module>/<function>.ts` calls `wrapUsecase` directly and explicitly at its own export site instead, matching this project's "explicit over magic" style. Two disclosed TS-specific decisions: `shouldRunListeners` threads through as a third, wrapper-level `options` parameter (not folded into each usecase's own settings type); the "friendlier TypeError on bad kwargs" step is not reproduced at runtime (JS has no equivalent "unexpected keyword argument" error shape to pattern-match on, and TS's own compile-time argument checking already covers the class of error it exists to soften) — every error propagates untouched, pinned by a regression test. Also corrects `20-roadmap.md`'s own Phase 6 framing against the real source: `pre_listeners`/`post_listeners` are `dict[str, dict]` (already name-keyed, already idempotent on re-registration), not dict-of-list, so this uses `Map<string, Map<string, UsecaseListener>>`, not `Map<string, Set<...>>`. Pure JS plumbing, no `IfcFile`/native-addon dependency — `test/api/hooks.test.ts` (22 tests) exercises it end-to-end with small synthetic usecases. First file in a new `src/api/` top-level area (`api/index.ts` re-exports `hooks.ts` flat, matching Python's own `ifcopenshell.api.add_pre_listener`-lives-directly-in-`__init__.py` namespacing, unlike `util/index.ts`'s per-submodule namespacing); `src/index.ts` now exports `api` alongside `util`. Independently re-reviewed by the orchestrating session directly against the real Python source before merge; no bugs found. |
| `api.root` — `create_entity` | ✅ | [#70](https://github.com/mikitski/IfcOpenShell/pull/70) | Port of `root/create_entity.py` (140 lines) — per `research/02-python-api-inventory.md` §3's "root" deep dive, the foundational function almost every other `api.*` usecase calls internally. Predefined-type edge-case handling (invalid value → `"USERDEFINED"` + fallback into `ObjectType`/`ElementType`/`ProcessType`, verified by reading the real C++ core that an invalid `ENUMERATION` write actually throws — `enumeration_type::lookup_enum_offset`, `src/ifcparse/schema.h` — and is translated to a catchable JS error, confirming `try`/`catch` around `.set("PredefinedType", ...)` is the faithful equivalent of Python's bare `except:`), `handle2x3Defaults`/`handle4Defaults` mandatory-attribute population, ported verbatim. `GlobalId`/`OwnerHistory` created positionally as part of the single `createEntity` call (one `Transaction` "create" op, matching Python's one-call construction and `util/migrator.ts`'s own `IfcOwnerHistory` precedent, confirmed against the generated `.d.ts` that `GlobalId`/`OwnerHistory` are always attribute indices 0/1 for every `IfcRoot` subtype in all 3 schemas). Real, disclosed finding, regression-tested not just reasoned about: the `"USERDEFINED"`-fallback path's failed `.set("PredefinedType", <invalid>)` attempt still records a spurious `Transaction` "edit" op (`EntityInstance.setByIndex` calls `storeEdit` before the throwing native call) — `Transaction.rollback` (undo) guards every edit replay in `try`/`catch`, but `Transaction.commit` (redo) does not, so redoing such a creation throws `UndoSystemError`; a pre-existing property of the `Transaction` primitive, not introduced here, pinned by a dedicated test. Test coverage ported from `test/api/root/test_create_entity.py` plus new `Transaction`/undo-redo regression tests. Independently re-reviewed by the orchestrating session directly against the real Python source before merge, including confirming the `Transaction.commit`/`rollback` asymmetry matches real Python's own identical `commit()`/`rollback()` source (same missing try/except, same comment) — not a TS-port-introduced bug; no further bugs found. |
| `api.root` — `remove_product` | ✅ | [#91](https://github.com/mikitski/IfcOpenShell/pull/91) | Port of `root/remove_product.py` (223 lines) — the "smart delete" that removes a product/type product AND every relationship that would otherwise dangle (representations, object placement, type-level psets, openings, grid axes, and 14 kinds of inverse relationship). **Retires `api.owner`'s temporary `internalCascadeHelpers.ts`**: an earlier chunk (the `api.owner` person/organisation/application family, then the actor/role/address family) needed this function's generic inverse-cascade tail as a real, load-bearing dependency for `removePerson`/`removeOrganisation`/`removePersonAndOrganisation` (called only ever on `IfcActor`/`IfcOccupant`/IFC2X3-only `IfcInventory`, never a genuine `IfcProduct`/`IfcTypeProduct`) before this function existed at all, via a private, unexported `removeProductCascade` explicitly documented as a stand-in to delete once this chunk landed. That reproduction (independently verified line-by-line in an earlier review) was used as a strong reference here, re-verified directly against the real Python source rather than trusted blindly; `internalCascadeHelpers.ts` (and its own now-unused local `attrOr`/`removeWithOwnerHistory` helpers, plus the file itself, since chunk 2's own `removeRoleCascade`/`removeAddressCascade` were already retired) is now deleted entirely, with `removePerson.ts`/`removeOrganisation.ts`/`removePersonAndOrganisation.ts` updated to import and call this real, exported `removeProduct` directly. Since none of those 3 functions' own call sites ever pass a genuine `IfcProduct`/`IfcTypeProduct` through this path, the swap changes no observable behavior — confirmed by re-running all 90 of their own tests unchanged (no previously-blocked test cases were unlocked; none of the 3 functions' own real Python test suites exercised a genuine product/type-product path in the first place). Newly ported beyond the generic tail: the `IfcProduct` preamble (representation removal via `api.geometry.unassignRepresentation`/`.removeRepresentation`, both already landed; object placement removal via `util.element.removeDeep2`) and the `IfcTypeProduct` preamble (type-level pset cleanup via `api.pset.removePset`, already landed). Real, disclosed native primitive-layer finding: nulling `product.ObjectPlacement` (a SCALAR entity-typed attribute, real Python's own way of dropping the placement's inverse count to 0 before `remove_deep2`) hits the same `.set(attr, null)` inverse-index bug `api.geometry.unassignRepresentation` already found for an AGGREGATE attribute (`TODOS.md`) — worked around here with a different shape (no "assign `[]` first" equivalent exists for a scalar attribute): `removeDeep2`'s own `alsoConsider` parameter is used instead, passing `[product]` so the containment check treats `product`'s one forward reference as "also being removed" without ever touching the buggy null-assignment path, relying on `IfcFile.remove`'s own correct auto-nulling of attributes referencing a just-deleted entity to reach the identical end state (confirmed empirically). Two more real, newly-discovered blockers, disclosed via a loud throw rather than silently skipped, matching this project's established `mapTypeRepresentations.ts` precedent: `HasOpenings` cleanup (needs `api.feature.removeFeature`, unported) and `IfcGrid` axis cleanup (needs `api.grid.removeGridAxis`, unported), both only reachable when actually needed. Carries forward the tail's own 2 already-disclosed blockers: `IfcRelAssociatesMaterial` (needs `api.material.unassignMaterial` — a second blocked call site on the same unported module `api.type.assignType`'s own entry already tracks, cross-referenced not duplicated) and `IfcRelSpaceBoundary` (needs `api.boundary.removeBoundary` — a brand-new `TODOS.md` entry, `api.boundary` has no TS port at all). Self-recursion (`IfcRelNests` into `IfcDistributionPort` subelements, `IfcRelConnectsPortToElement` into `RelatingPort`) and the `element_exists`/id-collect-then-refetch defensive pattern both ported verbatim, going through the real, exported, listener-wrapped `removeProduct` on every recursive call (matching real Python's own `wrap_usecases`-wrapped recursion). Test coverage ports all 27 real Python test methods from `test_remove_product.py` (`TestRemoveProduct`/`TestRemoveProductIFC2X3`) — most needing an unported sibling `api.*` module purely for fixture setup (`api.feature`/`api.grid`/`api.nest`/`api.system`/`api.drawing`/`api.geometry.editObjectPlacement`/`connect_path`/`connect_element`) are adapted to build the equivalent relationship directly via `file.createEntity`, matching `api.group.removeGroup.test.ts`'s established substitution precedent; the 4 tests that are genuinely blocked on `removeProduct`'s own logic (not just fixture setup) are pinned instead as dedicated "throws the disclosed blocked error" regression tests. Plus new Transaction/undo-redo regression tests. Ran the full local test suite (2943 passed, 5 skipped, 3 pre-existing unrelated failures in `api.unit.addMonetaryUnit`/`editMonetaryUnit`, already tracked in `TODOS.md`, untouched by this chunk) against a locally-built multi-schema native addon. |
| `api.root` — `reassign_class`/`copy_class` (**`api.root` module now FULLY COMPLETE, all 4 functions**: `create_entity` (#70), `remove_product` (#91), `reassign_class`/`copy_class` (this row)) | ✅ | — | Ports the last 2 real Python files of `api.root`: `reassign_class.py` (236 lines — changes a product's IFC class, e.g. a wall reclassified as a column, while retaining geometry/relationships; if reassigning a TYPE, occurrence classes are also reassigned to maintain validity, and vice versa) and `copy_class.py` (193 lines — duplicates a product along with its placement/psets/ports/aggregation/containment/type/void/material/group relationships, explicitly NOT representations per the real docstring: "expensive, so for now the user is responsible"). Per this chunk's own task brief, most of `reassignClass`'s real API-level dependencies (`api.aggregate`/`api.pset`/`api.spatial`/`api.type`, all fully landed) turned out to already be portable — confirmed directly, not assumed. **`reassignClass`: one real, disclosed blocker.** `simpleReassignment` (same-`IfcTypeProduct`-ness reassignment, e.g. `IfcWindow` -> `IfcWall`) is fully portable, no blocker at all — every dependency it needs (`util.schema.reassignClass`, `util.type.getApplicableEntities`/`getApplicableTypes`, `util.element.getTypes`/`getType`) was already landed by an earlier chunk. `switchBetweenClassTypes` (occurrence<->type switch, e.g. `IfcWindowType` -> `IfcWindow`) needs `api.geometry.assignRepresentation`/`.editObjectPlacement` (neither ported — `api.geometry` still only has `unassignRepresentation`/`removeRepresentation` landed) to carry any of the element's own representations across the switch — but ONLY when the element being switched actually has at least one representation; every other real behavior (unassigning/reassigning the type, container, aggregate, and psets) is fully portable via already-landed `api.type`/`api.spatial`/`api.aggregate`/`api.pset` functions. Throws immediately upon discovering a non-empty representation list, BEFORE any of this function's own mutations (matching this project's established "throw before mutating, don't leave a worse partial state" discipline — doing the type/container/pset unwiring first would leave the element in a strictly worse half-switched state once the representation step turned out to be blocked). **`copyClass`: one remaining real, disclosed blocker** (a second, for a genuine `IfcMaterialLayerSet`/`IfcMaterialProfileSet`/`IfcMaterialConstituentSet` material association needing `api.material.copyMaterial`, was resolved by a small orchestrating-session follow-up once `api.material` chunk 1 (#93) landed — see that row below): nested distribution ports (`IfcRelNests`/IFC2X3's `IfcRelConnectsPortToElement`) need `ifcopenshell.api.system` (a brand-new blocked module, NO TS port of any kind — a new `TODOS.md` entry) plus the same pre-existing `api.geometry.editObjectPlacement` blocker, thrown only at the exact point it'd actually be needed, never proactively. A plain `IfcMaterial`/`IfcMaterialList` association is shared as-is and a parametric `IfcMaterialLayerSetUsage`/`IfcMaterialProfileSetUsage` is shallow-copied via the already-portable `util.element.copy`; a genuine material SET now calls the real `api.material.copyMaterial` directly (its own disclosed-throw test replaced with real Python's `test_copying_material_sets_for_type_elements_only` assertions). Every other real behavior of `copyClass` — placement (regenerated `ObjectPlacement`/`RelativePlacement` via `util.element.copy`/`copyDeep`), psets (`copyDeep`), non-filled openings (full self-recursive `copyClass` + `IfcRelVoidsElement`/placement/representation rewiring, no blocker at all), aggregation/containment/type/group sharing (real Python's own generic inverse-attribute fallback: a single-valued match gets an independent duplicate rel, a list-valued match instead MUTATES the original rel in place to also reference the copy — a genuine, deliberate Python quirk, preserved verbatim not "fixed") — is fully portable and ported. One real, subtle Python-source bug found and preserved, not silently "corrected": `not opening.is_a("IfcOpeningElement") or not opening.HasFillings` relies on `or`'s short-circuit — `opening.HasFillings` is genuinely never read for a non-`IfcOpeningElement` opening (e.g. a plain `IfcVoidingFeature`, which has no `HasFillings` attribute declared at all); ported with the identical short-circuit (confirmed by a real IFC4/IFC4X3 `IfcVoidingFeature` test case that would otherwise throw "no attribute 'HasFillings'"). Self-recursion in both files goes through the real, exported, listener-wrapped `reassignClass`/`copyClass` on every recursive call, matching `removeProduct.ts`'s own established precedent. Test coverage: `test_reassign_class.py`/`test_copy_class.py` ported as fully as the disclosed blockers allow (1 representation-switch test in `reassignClass.test.ts`, 1 ports test in `copyClass.test.ts` pinned as dedicated "throws the disclosed blocked error" regression tests instead of silently dropped, matching `removeProduct.test.ts`'s established precedent; the material-set test now runs for real, see the `api.material` row below; the IFC2X3-specific `test_providing_occurrence_class` gated per-schema), every other real Python test case ported in full (including fixtures real Python builds via still-unported sibling modules — `api.feature`/`api.system`/`api.unit`/`api.geometry.editObjectPlacement`/`connect_path` — reconstructed directly via `file.createEntity`/`withAttrs`, matching `removeProduct.test.ts`'s own established substitution precedent), plus new Transaction/undo-redo regression tests for both functions. Ran the full local suite (3061 passed, 5 skipped, the same 3 pre-existing unrelated failures in `api.unit.addMonetaryUnit`/`editMonetaryUnit` already tracked in `TODOS.md`, confirmed untouched by this chunk) against a locally-built multi-schema native addon; `tsc --noEmit`/`biome check` both clean. |
| `api.project` — `create_file`/`assign_declaration`/`unassign_declaration` (3 of 4 files) | ✅ | [#85](https://github.com/mikitski/IfcOpenShell/pull/85) | Partial, matching `api.type`/`api.pset`/`api.geometry`'s own partial-completion rows: `append_asset.py` (827 lines — importing a whole asset, its geometry, materials, styles, and property sets from a library file into the active model) is deliberately out of scope, tracked as its own future chunk in the row below. **`create_file`** (Python: constructs a bare `ifcopenshell.file(schema=version)` — a genuinely empty, zero-entity file — then sets 5 STEP header fields). Reuse decision: delegates to `template.ts`'s `create()`, extended with a new, purely additive `blank`/`authorization`/`description` option set (a new `BLANK_TEMPLATE` STEP string alongside the existing `TEMPLATE`) rather than reimplementing STEP-header templating from scratch — investigated and confirmed the *only* currently-available route to a file with chosen header field values at all, for two independent, pre-existing, disclosed reasons: (1) `template.create()`'s own default `TEMPLATE` is not entity-free (bakes in a starter `IfcProject`/unit/owner-history chain, a gap `test/bootstrap.ts`'s `stripProjectBootstrap`/`stripOwnerBootstrap` comments already found in an earlier `api.unit` chunk), and (2) `IfcFile`/`spf_header` has no live `file_name`/`file_description` sub-entity accessor at all yet (`TODOS.md`'s dedicated entry), so there is no way to construct a blank file and set header fields on it afterwards either — confirmed the low-level `file_new_with_schema_filetype_path_logger` primitive is genuinely unreachable too, not just unwrapped: its required `logger` argument has no static constructor bound anywhere on this primitive surface. Field-by-field comparison against real Python: `file_name.name`/`.authorization`/`file_description.description` match exactly (including real Python's own quirk of hardcoding `"ViewDefinition[DesignTransferView]"` regardless of schema version); `time_stamp` format and `preprocessor_version`/`originating_system`'s exact string content are small, pre-existing `template.ts` deviations (already disclosed there), not new gaps — both still satisfy real Python's own test assertions (truthiness / an `"IfcOpenShell"` substring, not exact-string equality). **`assign_declaration`**/**`unassign_declaration`** manage `IfcRelDeclares` — read function-by-function, not assumed to mirror `spatial.assignContainer`/`aggregate.assignObject`'s superficially similar "surgery" shape: `assign_declaration`'s dedup checks membership against *every* existing `Declares` rel (`object_rel not in all_declares`), not just a single first-found rel like its siblings. Confirmed directly against the generated `.d.ts`s: `IfcRelDeclares`/`IfcProjectLibrary`/the `Declares`/`HasContext` inverse pair are absent from `ifc2x3.d.ts` entirely (IFC4+ only, matching the docstring) but identical 6-attribute positional order (`GlobalId`/`OwnerHistory`/`Name`/`Description`/`RelatingContext`/`RelatedDefinitions`) in `ifc4.d.ts`/`ifc4x3.d.ts` — no IFC2X3 runtime guard added, matching real Python's own unguarded behavior (same "disclose, don't silently guard" precedent as `api.constraint`'s IFC2X3 findings), regression-tested (throws). Real, disclosed Python quirk found and preserved, not fixed: `unassign_declaration`'s own `relating_context` parameter is accepted but **never actually consulted** by its logic — it un-declares each definition from whatever context it is currently declared under via `HasContext`, regardless of whether that matches the given `relating_context`; also a real, disclosed asymmetry between the two sibling functions: `assign_declaration` soft-guards `getattr(definition, "HasContext", None)` while `unassign_declaration` accesses `obj.HasContext` unguarded (throws for an unsupported type). Test coverage: all 3 real Python test files ported in full (`test_create_file.py`/`test_assign_declaration.py`/`test_unassign_declaration.py`) plus original Transaction/undo-redo regression tests and a dedicated regression test for the `unassign_declaration`-ignores-`relatingContext` quirk; `createFile`'s header-field assertions verify the real serialized STEP bytes (`IfcFile.write` + read back, since there is no live header-read accessor either) rather than an in-memory object, matching `util/element.ts`'s own established `write`-then-reread workaround technique. IFC2X3/IFC4X3-parameterized tests gated with `AVAILABLE_SCHEMAS`/`describe.skipIf`. Self-reviewed against the real Python source and all 3 generated `.d.ts`s; local suite passes in full (both CI's actual `SCHEMA_VERSIONS=4` configuration and, separately, a local all-3-schemas build used to exercise the otherwise-CI-skipped IFC2X3/IFC4X3 paths for real — not just compiled). |
| `api.project` — `append_asset` | 🔲 | — | Future chunk — 827 lines, imports a whole asset (geometry, materials, styles, property sets) from a library file into the active model; substantially larger and architecturally distinct from the 3 files landed above. |
| `api.spatial` — all 4 functions | ✅ | [#71](https://github.com/mikitski/IfcOpenShell/pull/71) | Port of `api/spatial/` (4 files, ~480 lines): `assign_container`/`unassign_container` (manage the single-valued `IfcRelContainedInSpatialStructure` — reassigning silently removes/replaces any existing containment first) and `reference_structure`/`dereference_structure` (manage the weaker, multi-valued `IfcRelReferencedInSpatialStructure`). Real, disclosed correction to `research/02-python-api-inventory.md` §3's own "spatial ... No geometry, no numpy" framing: true for 3 of the 4 functions, but `assign_container` genuinely imports and calls both `ifcopenshell.api.aggregate.unassign_object` and `ifcopenshell.api.geometry.edit_object_placement`. The former (75 LOC, self-contained) is ported as a new, minimal `api.aggregate` module (`unassignObject` only — `assign_object` remains a future chunk); the latter (201 LOC of matrix math/`ShapeBuilder`/recursive child traversal — real, independent design work per the research doc's own "geometry" deep dive) is a disclosed, unstubbed gap: `assignContainer` performs the full containment-relationship surgery correctly but skips the placement-relocalization step, tracked in `TODOS.md`. Also newly ports `api.owner.update_owner_history` (80 lines — all 4 `api.spatial` functions call it on every relationship mutation), added to the existing `api.owner` barrel. Found+fixed one real, pre-existing, unrelated bug in the already-landed `createEntity.test.ts` (`api.root`/`api.owner` chunk, PR #70): its `describe.each(AVAILABLE_SCHEMAS)` extension asserted `IfcDoorStyle`/`IfcWindowStyle` creation on IFC4X3, but those classes are genuinely absent from the IFC4X3 schema entirely (confirmed: no such interface in `src/generated/ifc4x3.d.ts`) — real Python's own test only ever runs this against IFC4/IFC2X3, never IFC4X3; guarded to match. Test coverage ported from `test/api/spatial/test_{assign,unassign}_container.py`/`test_{,de}reference_structure.py` (one Python test per file adapted to construct its `IfcRelAggregates`/unit-assignment precondition directly rather than via the unported `aggregate.assign_object`/`api.unit`; one Python test per file — the geometry-shift case — not ported, per the disclosed gap above), plus new `Transaction`/undo-redo regression tests for all 4 functions, with particular scrutiny on `assignContainer`'s replace-existing-containment surgery (undo restores the OLD relationship with its OLD membership, not just deletes the new one; redo reproduces the same surgery). Independently re-reviewed by the orchestrating session directly against the real Python source before merge; no further bugs found. |
| `api.aggregate` — `unassign_object` | ✅ | [#71](https://github.com/mikitski/IfcOpenShell/pull/71) | Ported alongside `api.spatial` above, as a direct dependency of `assign_container` — see that row. `assign_object` (the other half of this 2-function module) landed separately, see next row. |
| `api.aggregate` — `assign_object` | ✅ | [#72](https://github.com/mikitski/IfcOpenShell/pull/72) | Port of `aggregate/assign_object.py` (160 lines) — the second and final function of `api.aggregate`, **completing that module**. Structurally near-identical to `api.spatial.assignContainer`'s own "surgery" shape (check existing relationship, unassign from old, assign to new, merge-into-existing-vs-create-new), and reuses the already-landed `spatial.unassignContainer` directly (real Python: `assign_object` enforces the "aggregation and containment are mutually exclusive" invariant from the aggregate side, calling `spatial.unassign_container` on any about-to-be-aggregated product without an existing aggregate — the mirror image of how `assignContainer` enforces the same invariant from the containment side via `aggregate.unassign_object`). Real, disclosed finding, verified directly against this port's own native binding: Python's `hasattr(p, "ContainedInStructure")` guard before that call is genuinely load-bearing here (unlike `assignContainer`, which never needs it) — `IfcSite`/`IfcBuilding` (spatial structure elements, a real `assign_object` product class per its own docstring) throw `EntityInstance.get("ContainedInStructure")`, while `IfcWall`/`IfcElementAssembly` (physical elements) resolve to `[]`; ported faithfully via a small local try/catch helper. Also disclosed: unlike `unassignObject.ts`'s own loop, real Python's `previous_aggregates_rels` collection is genuinely unfiltered by `is_a("IfcRelAggregates")` (`next(iter(product.Decomposes), None)`, no type check) — ported verbatim, not "corrected," since the later rewrite-or-delete loop only touches `RelatingObject`/`RelatedObjects`, attributes every `IfcRelDecomposes` subtype (`IfcRelNests` included) declares identically. Hits the exact same disclosed `api.geometry.edit_object_placement` gap `assignContainer.ts` found first — the existing `TODOS.md` entry was extended to cover this second call site rather than duplicated. Test coverage ported from `test/api/aggregate/test_assign_object.py` (6 of 7 real cases; the geometry-shift case skipped per the disclosed gap, matching `assignContainer.test.ts`'s own precedent — one of the 6, `test_removing_containment_if_it_exists`, needed no adaptation at all, unlike `assignContainer.test.ts`'s equivalent aggregate-side case, since `spatial.assignContainer` was already fully landed by this point), plus new `Transaction`/undo-redo regression tests (replace-existing-aggregate surgery, purge-to-zero-members, and the containment-to-aggregate migration path) with the same undo-restores-OLD-membership scrutiny established by `assignContainer.test.ts`. Independently re-reviewed by the orchestrating session directly against the real Python source before merge; no further bugs found. **`api.aggregate` is now fully ported.** |
| `api.owner` — `create_owner_history` + `settings` injectable DI | ✅ | [#70](https://github.com/mikitski/IfcOpenShell/pull/70) | Port of `owner/create_owner_history.py` (117 lines, ported verbatim — `IfcOwnerHistory`'s 8 attributes created positionally, confirmed identical order across all 3 schemas' generated `.d.ts`) and `owner/settings.py` (103 lines) — the `owner.settings` monkeypatch replacement flagged as a later chunk in the hook-system row above. Settled per `planning/ifcopenshell-ts/30-open-questions.md` item 9 / `40-testing-strategy.md` §5's already-decided design: a mutable `ownerSettings` singleton object with `getUser`/`getApplication` fields (overridable directly, e.g. `ownerSettings.getUser = (file) => ...`) plus `factoryReset()`/`restore()` matching Python's real backup/restore semantics (not just "reset to default" — `factoryReset()` saves whatever is currently assigned before resetting, `restore()` puts it back), since TS/ESM doesn't allow rebinding an `export function` binding the way Python reassigns a module attribute. Test coverage ported from `test/api/owner/test_create_owner_history.py`; `test/bootstrap.ts` gained a new `stripOwnerBootstrap` helper since `createTestFile`'s template (matching real Python's `ifcopenshell.template.create`) always pre-populates a default person/organization/application/owner-history chain, so the "no user or application available" test cases need it stripped first. |
| `api.owner` — `update_owner_history` | ✅ | [#71](https://github.com/mikitski/IfcOpenShell/pull/71) | Port of `owner/update_owner_history.py` (80 lines) — added alongside the `api.spatial` chunk (see that row) as a direct dependency: all 4 `api.spatial` functions call it on every relationship mutation. Copy-on-write guard ported verbatim (`file.getTotalInverses(ownerHistory) > 1` → `util/element.ts`'s `copy` before mutating, so a shared `IfcOwnerHistory` isn't silently mutated out from under other elements). Test coverage ported from `test/api/owner/test_update_owner_history.py` (all 4 cases) plus a Transaction/undo-redo regression test. |
| `api.owner` — person/organisation/application family (chunk 1 of 2) | ✅ | [#83](https://github.com/mikitski/IfcOpenShell/pull/83) | Port of 11 of `api.owner`'s remaining 22 files (~632 lines split across both chunks): `add_person`/`edit_person`/`remove_person`, `add_organisation`/`edit_organisation`/`remove_organisation`, `add_person_and_organisation`/`remove_person_and_organisation` (real Python has no `edit_person_and_organisation.py` — none added here either, confirmed by directory listing), `add_application`/`edit_application`/`remove_application`. Manages `IfcPerson`/`IfcOrganization`/`IfcPersonAndOrganization`/`IfcApplication` — all 4 non-rooted resource classes, no `IfcRoot`/`OwnerHistory` involvement in any `edit_*` function. The remaining 11 files (`add_actor`/`edit_actor`/`remove_actor`/`assign_actor`/`unassign_actor`, `add_role`/`edit_role`/`remove_role`, `add_address`/`edit_address`/`remove_address` — the "actor/role/address" family) are **chunk 2 of 2, landing as a separate PR**, done concurrently by a different agent in a different worktree/branch — not touched here. `IfcPerson`/`IfcOrganization`/`IfcPersonAndOrganization`/`IfcApplication`'s attribute orders verified identical and contiguous across all 3 schemas' generated `.d.ts` files (no DERIVE-interleaving gotcha in any of the 4 classes); the only real IFC2X3-vs-IFC4+ schema difference is the already-known `Id`(IFC2X3)/`Identification`(IFC4+) rename on `IfcPerson`/`IfcOrganization` (same positional slot in both, so `add_person`/`add_organisation`'s positional `createEntity` calls need no schema branch for the value itself). Three of this chunk's own `remove_*` functions (`removePerson`/`removeOrganisation`/`removePersonAndOrganisation`) have real, load-bearing dependencies on `owner.remove_role`/`owner.remove_address` (chunk 2's own files, deliberately not touched to avoid a same-file merge collision with that concurrently-in-flight PR) and `root.remove_product` (a much larger, entirely separate future chunk: 223 lines, fans out into unported `api.boundary`/`api.feature`/`api.grid`/`api.material`) — resolved via a new, private, unexported `internalCascadeHelpers.ts` that reproduces each dependency's exact real behavior (verified line-by-line against the real Python source) narrowly scoped to what these 3 functions actually need (the generic inverse-cascade tail of `remove_product`, never its `IfcProduct`/`IfcTypeProduct`-specific preambles, since neither `IfcActor` nor `IfcInventory` — the only two classes this chunk's callers ever pass through it — is ever one of those); throws a clear, disclosed error instead of silently skipping cleanup for the 2 sub-branches that would need `api.material`/`api.boundary` (neither exercised by this chunk's own tests). `add_application`'s IFC4X3-only default-organisation branch (no `application_developer` given) hits a real, pre-existing, already-disclosed primitive-layer gap (`file.createEntity("IfcLabel", ...)` — building a standalone defined-type instance — throws; same gap `unit.addConversionBasedUnit` already disclosed, `TODOS.md` updated with a third occurrence) and currently throws; ported faithfully up through constructing the real actor/pset/rel entities regardless, pinned by a dedicated test, not silently worked around — the IFC2X3/IFC4 default path and the "developer explicitly given" path (all 3 schemas) work correctly today. `remove_organisation.py`'s own `and not inverse.is_a("IfcOrganizationRelationship")` guard is permanently dead code (that class is already caught by an earlier `if` branch in the same chain) — ported verbatim, disclosed, not simplified away. Test coverage ported from all of `test/api/owner/test_{add,edit,remove}_{person,organisation,person_and_organisation}.py`/`test_add_application.py`/`test_edit_application.py` (`remove_application.py` has no real Python test file at all, confirmed by directory listing; original coverage added instead), plus new Transaction/undo-redo regression tests for every mutating function and a dedicated test pinning the disclosed IFC4X3 `add_application` blocker. Ran the full local suite (2341 passed, 5 skipped, 3 pre-existing unrelated failures in `api.unit.addMonetaryUnit`/`editMonetaryUnit`, already tracked in `TODOS.md`, untouched by this chunk). Independently self-reviewed directly against the real Python source and the generated `.d.ts`s before opening the PR (line-by-line, function-by-function); no further bugs found. |
| `api.owner` — actor/role/address family (chunk 2 of 2) | ✅ | [#84](https://github.com/mikitski/IfcOpenShell/pull/84) | Port of the remaining 11 of `api.owner`'s 22 non-owner-history files, **completing `api.owner`'s full 25-file/function surface across both chunks**: `add_actor`/`edit_actor`/`remove_actor`/`assign_actor`/`unassign_actor` (manage `IfcActor`/`IfcOccupant`), `add_role`/`edit_role`/`remove_role` (manage `IfcActorRole`), `add_address`/`edit_address`/`remove_address` (manage `IfcPostalAddress`/`IfcTelecomAddress`). `IfcActor`/`IfcOccupant`/`IfcActorRole`/`IfcPostalAddress`/`IfcTelecomAddress`/`IfcRelAssignsToActor` attribute orders all verified identical and contiguous across all 3 schemas' generated `.d.ts` files — no DERIVE-interleaving gotcha in any of them (IFC4X3's `IfcRelAssignsToActor.RelatedObjectsType` is typed `boolean | null` there vs. `IfcObjectTypeEnum | null` on IFC2X3/IFC4, a real, harmless, unrelated-to-this-chunk schema quirk, since that attribute is never populated by `assign_actor`). One real, disclosed IFC2X3-vs-IFC4+ schema difference with actual behavioral consequence: `IfcOccupant.PredefinedType` is MANDATORY on IFC2X3 but OPTIONAL on IFC4+ — `add_actor` never sets it (real Python's own body is just `root.create_entity` + `actor_.TheActor = actor`, nothing more), so a freshly-created IFC2X3 `IfcOccupant` is left schema-invalid until a caller separately sets it via `edit_actor`; ported verbatim, not "fixed". `IfcTelecomAddress`'s IFC4X3 deprecation (already seen in the just-merged `addApplication.ts`'s own default-organisation logic) was investigated and confirmed irrelevant to this chunk: `add_address`/`edit_address`/`remove_address` are generic, class-agnostic, schema-version-agnostic functions in real Python with no special-casing of any kind — that deprecation guidance is specific to `add_application`'s own default-organisation-building logic, not to this module's own address CRUD. `assign_actor`/`unassign_actor` (99/67 lines, read closely rather than assumed to mirror `assignGroup`/`assignReference`'s shape) turned up a real, non-obvious, disclosed quirk: `assign_actor`'s dedup check scans `related_object.HasAssignments` (the object side), but its reuse-or-create check looks at `relating_actor.IsActingUpon[0]` (the ACTOR side, unscoped by which object) — so a single `IfcActor` only ever accumulates at most one `IfcRelAssignsToActor` relationship across its whole lifetime, growing the SAME rel's `RelatedObjects` on every subsequent `assignActor` call for that actor regardless of which object is being assigned, unlike `assignGroup`'s/`assignReference`'s own per-target-object reuse scoping; `unassign_actor`'s loop also has no `break`/`return` after its "shrink `RelatedObjects`" branch (only after its "remove the whole rel" branch), so it can in principle shrink/remove more than one matching rel in a single call. Both ported verbatim, disclosed in code comments, and covered by dedicated tests. Retired `internalCascadeHelpers.ts`'s temporary private `removeRoleCascade`/`removeAddressCascade` helpers (added by chunk 1 as a stand-in, explicitly documented there as "delete once the real, exported `removeRole`/`removeAddress` land") now that this chunk's own `removeRole.ts`/`removeAddress.ts` are real, exported, independently-tested ports — verified line-by-line behaviorally identical to the retired helpers before deleting them; `removePerson.ts`/`removeOrganisation.ts` (chunk 1) now import and call the real `removeRole`/`removeAddress` directly instead. `removeProductCascade` (the `root.remove_product` reproduction, unrelated to this chunk's own dependencies) remains untouched in `internalCascadeHelpers.ts`, pending a future, much larger `api.root` "remove_product" chunk. Test coverage ported from all of `test/api/owner/test_{add,edit,remove}_actor.py`/`test_{,un}assign_actor.py`/`test_{add,edit,remove}_role.py`/`test_{add,edit,remove}_address.py`, plus new Transaction/undo-redo regression tests for every mutating function and original coverage for the `assign_actor`/`unassign_actor` accumulation-across-related-objects and shrink-vs-remove branches real Python's own test suite doesn't directly exercise. Built the native addon locally from scratch (cmake/boost/eigen installed via Homebrew) against all 3 schemas (`SCHEMA_VERSIONS` defaulted to the fuller 2x3/4/4x1/4x2/4x3/4x3_tc1/4x3_add1/4x3_add2 set, `BUILD_IFCGEOM`/`WITH_CGAL`/`WITH_OPENCASCADE`/`COLLADA_SUPPORT` off) and ran the full local suite (2482 passed, 5 skipped, 3 pre-existing unrelated failures in `api.unit.addMonetaryUnit`/`editMonetaryUnit`, already tracked from chunk 1, untouched by this chunk). Independently self-reviewed directly against the real Python source and the generated `.d.ts`s before opening the PR; no further bugs found. |
| `api.unit` — all 11 functions | ✅ | [#74](https://github.com/mikitski/IfcOpenShell/pull/74) | Port of `api/unit/` (11 files, ~790 lines): `add_si_unit`/`add_conversion_based_unit`/`add_derived_unit`/`add_monetary_unit`/`add_context_dependent_unit` (create the 5 unit entity classes), `assign_unit`/`unassign_unit` (manage `IfcProject.UnitsInContext`'s `IfcUnitAssignment`), `remove_unit` (delete a unit entity + inverse cleanup via `util.element.removeDeep2`), and `edit_named_unit`/`edit_monetary_unit`/`edit_derived_unit` (attribute setters). No geometry-kernel or other unported hard dependency anywhere in this module, confirmed by reading every file — only `util.element`/`util.unit`, both already fully ported. Real, disclosed, load-bearing finding (empirically confirmed against this exact worktree's own built native addon, not assumed): `IfcSIUnit` re-declares its inherited `IfcNamedUnit.Dimensions` as EXPRESS `DERIVE`, so the real STEP-level attribute list still reserves a slot for it (`EntityInstance.attributeCount()` reports 4, not the 3 fields `IfcSIUnit`'s generated `.d.ts` interface lists) — positional `createEntity("IfcSIUnit", ...)` needs a leading `null` placeholder before `UnitType`/`Prefix`/`Name`, confirmed independently by real Python's own `assign_unit.py` convenience wrapper calls (`self.file.createIfcSIUnit(None, ...)`, a leading `None`) and by `test/util/migrator.test.ts`'s own SPF fixture (`IFCSIUNIT(*,.PLANEANGLEUNIT.,$,.RADIAN.)`). Investigating this surfaced and fixed a second, real, pre-existing, unrelated bug in already-shipped Phase 2 code: `EntityInstance.getInfo()` zipped `get_all_attribute_values()`'s raw per-index value array against its FORWARD-attribute name list by shared array position, silently assuming no gaps — wrong whenever a non-FORWARD (derived) attribute is interleaved among FORWARD ones (exactly `IfcSIUnit`'s case), corrupting `Transaction`'s undo/redo serialisation for any positionally-created `IfcSIUnit`. Fixed by indexing with each `AttributeMeta`'s own real `.index` field instead of by-order position; pinned by a new dedicated regression test in `entityInstance.test.ts`. Also found: `createTestFile`'s template (`ifcopenshell.template.create` port) pre-populates a full default `IfcProject`+`IfcUnitAssignment`+units chain that real Python's own `test/bootstrap.py` fixture (`ifcopenshell.api.project.create_file`, confirmed by reading its source — a bare schema-only file, not `template.create`) never has; added a new `stripProjectBootstrap` test helper (matching `stripOwnerBootstrap`'s established precedent) so this chunk's tests reach the same blank starting state real Python's tests do. A second, real, load-bearing, pre-existing primitive-layer gap (first surfaced by `util/migrator.ts`'s own disclosed finding 1) blocks `add_conversion_based_unit` entirely and `assign_unit`'s imperial-unit-synthesis branch only: constructing a freestanding, valued `IfcReal` (needed for every `IfcMeasureWithUnit.ValueComponent`) unconditionally throws "Attribute access is only supported on entity instances" via `EntityInstance.setByIndex`'s entity-only-gated `attribute_kind_of` call — reproduced directly against this worktree's own built native addon, ported faithfully anyway (correct the moment the gap closes), pinned by dedicated "currently throws" regression tests (matching `migrator.test.ts`'s own established precedent for this exact gap) rather than silently skipped. The other 9 functions (including `assign_unit`'s default/metric/explicit-`units` paths — the common case, and the only one real Python's own test suite exercises) are fully functional. Test coverage ported from all of `test/api/unit/test_*.py` (`add_derived_unit.py` has no real Python test file — original coverage added instead, following its own docstring example), plus new Transaction/undo-redo regression tests for every mutating function and a supplementary hand-built-subgraph test for `remove_unit`'s deep-removal logic that avoids the disclosed `add_conversion_based_unit` blocker. Independently self-reviewed directly against the real Python source before merge. |
| `api.context` — all 3 functions | ✅ | [#75](https://github.com/mikitski/IfcOpenShell/pull/75) | Port of `api/context/` (3 files, ~395 lines): `add_context` (240 lines, the largest file — creates a top-level `IfcGeometricRepresentationContext` or, when `parent` is given, an `IfcGeometricRepresentationSubContext`), `edit_context` (49 lines, a trivial attribute-setter loop), and `remove_context` (67 lines — recursively removes subcontexts first, then either reassigns inverse references to the parent context (`ParentContext` branch, no `api.geometry` dependency) or unassigns+removes every representation left in a removed top-level context (the one branch needing `api.geometry`)). Only depends on the already-fully-ported `util.representation` for its `ContextType`/`RepresentationIdentifier`/`TargetView` literal types. Real, disclosed, load-bearing finding, the same category of gap `unit/addSiUnit.ts` first disclosed for `IfcSIUnit.Dimensions` but bigger here (4 attributes, not 1), confirmed empirically against this worktree's own built native addon: `IfcGeometricRepresentationSubContext`'s generated `.d.ts` interface lists only 6 properties, but `attributeCount()` reports 10 — it re-declares its supertype's own `CoordinateSpaceDimension`/`Precision`/`WorldCoordinateSystem`/`TrueNorth` (indices 2-5) as EXPRESS `DERIVE`d-from-`ParentContext`, still occupying real positional storage slots; positional `createEntity("IfcGeometricRepresentationSubContext", ...)` needs 4 explicit `null` placeholders before `ParentContext`/`TargetScale`/`TargetView` (indices 6-8). Found by writing this chunk's own tests against the real built addon (`TargetView` silently reading back `null` after being "successfully" written to the `.d.ts`-implied index), not assumed from the `.d.ts` text alone. Test coverage ported from all of `test/api/context/test_*.py` (one test, `test_removing_a_context_with_assigned_representations`, adapted to construct its `IfcProductDefinitionShape` precondition directly rather than via the unported `geometry.assign_representation`, and to assert removal by entity id rather than the real Python assertion's exact total-file-entity-count, which depends on `test.bootstrap.py`'s own IFC2X3-specific auto-creating `owner.settings` override that this port's own `ownerSettings` doesn't replicate — see that test's own header comment), plus new Transaction/undo-redo regression tests for every mutating function with particular scrutiny on `removeContext`'s recursive subcontext removal and both of its branches (with/without `ParentContext`). Independently self-reviewed directly against the real Python source before merge. |
| `api.geometry` — `unassign_representation`/`remove_representation` only | ✅ | [#75](https://github.com/mikitski/IfcOpenShell/pull/75) | Ported alongside `api.context` above, as a minimal, direct dependency of `remove_context`'s top-level-context branch — **not a full port of `api.geometry`**, which remains a separate future chunk (`edit_object_placement`, the pre-existing disclosed `api.spatial`/`api.aggregate` blocker, and every other function in this module are still unported). Verified directly that both files' only imports are `ifcopenshell.util.element` (already fully ported) and `ifcopenshell.api.geometry` itself (recursively, only for `remove_representation`) — no kernel/matrix-math dependency of any kind, unlike `edit_object_placement`. `unassign_representation` (113 lines) handles both the `IfcProduct` case (remove from `Representation.Representations`, purging the `IfcProductDefinitionShape`/shape-aspects once empty) and the more involved `IfcTypeProduct` case (detach every real product using the type's representation map via an `IfcMappedItem` first, then purge the map itself), including a real IFC2X3-vs-IFC4+ branch for `IfcShapeAspect.PartOfProductDefinitionShape` (a direct forward attribute on IFC2X3 only) vs. `HasShapeAspects` (an IFC4+ inverse), ported verbatim. `remove_representation` (92 lines) purges a representation's items and every related element that becomes orphaned as a result (`IfcStyledItem`, tessellated faceset colours/textures, presentation-layer assignments), protecting `IfcGeometricRepresentationContext`s and (by default) named `IfcProfileDef`s from deletion; reproduces a real, verified (not assumed) schema-dependent inverse-attribute-name difference — `IfcRepresentationItem.LayerAssignment` (singular, IFC4+) vs. `LayerAssignments` (plural, IFC2X3), confirmed against `util.element.get_layers`'s own identical branch. **A real, previously-undisclosed native primitive-layer bug found and worked around, not silently fixed**: clearing an entity/aggregate-of-entity attribute to `null` via `.set()` leaves a stale inverse-index entry (the N-API shim's `ATTRIBUTE_VALUE_KIND_NULL` case bypasses the register/unregister bookkeeping real Python's own equivalent code path always goes through) — see `TODOS.md`'s new entry for the full root-cause writeup; worked around locally in `unassignTypeRepresentation` (assign an empty array first, then the real final `null` value) and pinned by a dedicated regression test. Test coverage ported from `test/api/geometry/test_unassign_representation.py` (all 3 cases) and `test_remove_representation.py` (all 14 cases — that file has no IFC2X3 test class in real Python at all, confirmed by reading it directly, so the 3 tests needing IFC4+-only tessellated-faceset classes are `test.skipIf(schema === "IFC2X3")`-guarded, matching `createEntity.test.ts`'s own established precedent for a real Python test file's own narrower-than-`AVAILABLE_SCHEMAS` scope), plus new Transaction/undo-redo regression tests for both functions. Independently self-reviewed directly against the real Python source before merge. Incidentally found (via `git stash`, confirmed pre-existing and unrelated) a real, previously CI-invisible bug in the already-landed `api.unit.addMonetaryUnit`/`editMonetaryUnit` tests — see `TODOS.md`'s new entry. |
| `api.pset` — `remove_pset` | ✅ | [#77](https://github.com/mikitski/IfcOpenShell/pull/77) | Ported alongside `api.group` below, as a minimal, direct dependency of `remove_group` (which calls it on any pset assigned directly to a group being removed) — see that row and `pset/removePset.ts`'s own header comment. **NOT `api.pset` as a whole** — `add_pset`/`edit_pset` (the single biggest file in the whole `api` package)/`add_qto`/`edit_qto`/`assign_pset`/`unassign_pset`/`unshare_pset` (7 more files/functions per `research/02-api-layer.md`) remain a future, much larger chunk; this file should be treated as already landed when that chunk starts, not re-ported from scratch. One real, disclosed finding: real Python's `pset.is_a() in ("IfcMaterialProperties", "IfcProfileProperties")` check calls `is_a()` with NO argument (an exact-class-name match against a tuple), unlike this same function's two other, argument-taking `is_a("X")` subtype checks just above it — load-bearing on IFC2X3, where `IfcMaterialProperties` is an ABSTRACT supertype whose only concrete subtype (`IfcExtendedMaterialProperties`) has a different `is_a()` string and so never actually matches this branch in real Python either; ported via this port's own no-argument `.isA()` (not `.isA("IfcMaterialProperties")`, which would wrongly match via subtype-check semantics) specifically to preserve this exact-match distinction. Also disclosed: `IfcQuantitySet` doesn't exist as a schema entity in IFC2X3 at all (introduced in IFC4), so this function's `IfcQuantitySet` branch never fires there — real, inherent IFC2X3 behavior of the real Python source, never exercised by its own test suite (`test_remove_pset.py` has no IFC2X3 test class at all, confirmed by reading it directly — only ever runs against IFC4). Test coverage ported from all 8 real `test_remove_pset.py` methods (each adapted to construct its `IfcPropertySet`/`IfcMaterialProperties`/`IfcRelDefinesByProperties`/etc. precondition directly via `file.createEntity(...)` rather than via the unported `add_pset`/`edit_pset`/`add_qto`/`edit_qto`; the two material/profile-properties cases and the IFC2X3-quantity-removal case are schema-guarded/branched to match real Python's own IFC4-only scope, per the disclosed findings above), plus new Transaction/undo-redo regression tests. Independently self-reviewed directly against the real Python source before merge. |
| `api.pset` — `add_pset`/`assign_pset`/`unassign_pset`/`unshare_pset` (chunk 1 of 3 for this module's remaining files) | ✅ | [#86](https://github.com/mikitski/IfcOpenShell/pull/86) | Basic CRUD/assignment chunk of `api.pset`'s remaining 6 real Python files (490-line `edit_pset.py` — the single biggest file in the whole `api` package — split out into its own future chunk 3, and `add_qto.py`/`edit_qto.py` into future chunk 2, both deliberately out of scope here, per this chunk's own task brief). **`add_pset`** creates a new property/quantity set and attaches it to a product — 4 completely different attachment mechanisms by product kind, read closely rather than assumed uniform: `IfcObject`/`IfcContext` and `IfcTypeObject` both create a plain `IfcPropertySet` and delegate the actual linking to `assignPset`, but with different by-name dedup checks (`IsDefinedBy` inverse rels vs. the type's own forward `HasPropertySets` list); `IfcMaterialDefinition`/`IfcMaterial` and `IfcProfileDef` instead create an `IfcMaterialProperties`/`IfcProfileProperties`-family entity linked directly via its own `Material`/`ProfileDefinition` attribute, no separate rel entity or `assignPset` call at all. **`assignPset`**/**`unassignPset`** turned up a genuine, disclosed asymmetry from reading the bodies (not assumed symmetric): `assignPset` only ever looks at the FIRST pre-existing `PropertyDefinitionOf`(IFC2X3)/`DefinesOccurrence`(IFC4+) rel (same "don't merge across multiple rels" shape `api.group.assignGroup` already established for `IfcRelAssignsToGroup`), but `unassignPset` iterates and independently shrinks/removes EVERY such rel, skipping any with no overlap with the given products entirely — confirmed directly against both real Python sources side by side. **`unsharePset`** copies a shared pset once per given product (delegating to `assignPset`/`unassignPset` for the actual re-linking), with a self-orphaning guard: if every element the pset is currently assigned to is also in the given `products` list, the first product is dropped from the list so the original pset keeps at least one owner; real Python's own source computes an occurrence/type partition locally that is never actually used afterward (genuine, harmless dead code, disclosed and not reproduced). Schema differences confirmed directly against all 3 generated `.d.ts`s: `IfcPropertySet`/`IfcRelDefinesByProperties`/`IfcTypeObject.HasPropertySets` are attribute-order-identical and DERIVE-free across all 3 schemas; IFC2X3's `IfcMaterialProperties`/`IfcProfileProperties` are abstract (hence the `ifc2x3Subclass` option, defaulting to `IfcExtendedMaterialProperties`/`IfcGeneralProfileProperties`) and use `ProfileName`(not `Name`)/no-`Name`-at-all shapes respectively — since `ifc2x3Subclass` is an arbitrary caller-supplied class name with `Material`/`ProfileDefinition` at a different attribute index per subclass (confirmed: `IfcFuelProperties.Material` is index(0), `IfcGeneralProfileProperties.ProfileDefinition` is index(1)), `addPset` creates these entities with zero positional args and sets attributes by NAME (`.set(name, value)`) instead of hand-building a per-subclass positional array, mirroring real Python's own name-based `**kwargs` resolution exactly. Hit and worked around a real, already-disclosed (in `TODOS.md`, found by an earlier chunk porting `api.geometry.unassignRepresentation`) native primitive-layer bug: assigning `null` directly to an aggregate-of-entity attribute (`unassignPset`'s `product.HasPropertySets = psets or None` when the last pset is removed from a type) bypasses the native inverse-index unregister bookkeeping, leaving `pset.DefinesType` stale; worked around with the same established fix (assign `[]` first, then the real final `null`), caught by this chunk's own local multi-schema test run (would NOT have been caught under CI's `SCHEMA_VERSIONS=4`-only build alone, since the failure only showed up via `DefinesType` inverse assertions exercised identically across all 3 schemas). **Discrepancy check against `api.owner.addApplication.ts`'s inline `add_pset`/`assign_pset` reproduction** (written in an earlier chunk, before either was really ported, for one narrow IFC4X3 call site): compared attribute-by-attribute against this chunk's real, exported `addPset`/`assignPset` — no discrepancy found, that earlier chunk's assumption about real Python's behavior was correct; not updated to use the new exports since its own narrow IFC4X3 sub-path remains blocked by an unrelated, deeper native-primitive gap (building a standalone `IfcLabel` instance), same as before this chunk. Test coverage: `test_add_pset.py`/`test_assign_pset.py`/`test_unassign_pset.py`/`test_unshare_pset.py` all ported in full (material/profile fixtures constructed directly via `file.createEntity(...)`, substituting for the unported `api.material`/`api.profile` modules real Python's own fixtures use), plus original coverage for `unassignPset`'s multi-rel-with-partial-overlap behavior, `unsharePset`'s two `Exception`-raising paths (untested by real Python's own suite despite being real reachable code), and addPset's by-name dedup/reuse paths across all 4 branches — plus new Transaction/undo-redo regression tests for every mutating function. IFC2X3-only `ifc2x3Subclass` tests gated with `describe.skipIf(!AVAILABLE_SCHEMAS.includes("IFC2X3"))` per this chunk's own CI-learned lesson. Ran the full local suite (2612 passed, 5 skipped, 3 pre-existing unrelated failures in `api.unit.addMonetaryUnit`/`editMonetaryUnit`, already tracked in `TODOS.md`, untouched by this chunk) against a locally-built all-3-schemas native addon. **Chunks 2 (`add_qto`/`edit_qto`) and 3 (`edit_pset`) remain unported, tracked as separate future PRs.** Independently self-reviewed directly against the real Python source and the generated `.d.ts`s before opening the PR (the project's own `/code-review` skill hit its known fork-context-bleed bug — reviewed the main checkout's empty diff instead of this worktree's — so this was a thorough manual review instead); no further bugs found. |
| `api.pset` — `add_qto`/`edit_qto` (chunk 2 of 3 for this module's remaining files) | ✅ | [#87](https://github.com/mikitski/IfcOpenShell/pull/87) | Quantity-set (`IfcElementQuantity`, the `IfcQuantityXXX`-typed counterpart to arbitrary-key-value property sets) chunk of `api.pset`'s remaining files (chunk 1 — `add_pset`/`assign_pset`/`unassign_pset`/`unshare_pset` — landed in #86 above; chunk 3, `edit_pset.py` — the single biggest file in the whole `api` package — remains a separate, future PR). **`add_qto`** creates a blank, named `IfcElementQuantity` and attaches it to a product — verified directly against the real Python source rather than assumed symmetric with `add_pset`: it supports only 2 of `add_pset`'s 4 attachment mechanisms (`IfcObject`/`IfcContext` and `IfcTypeObject` — no material/profile branch exists for quantity sets at all), and unlike `add_pset`, it does NOT delegate to `assignPset` — it builds the `IfcRelDefinesByProperties`/`HasPropertySets` link inline itself. A further confirmed asymmetry: an unsupported product class silently returns `undefined` here (real Python: implicit `None`, no final `else`/`raise` in `execute()`), not the `TypeError` `add_pset`'s own explicit final `raise` produces. `IfcElementQuantity` itself IS available on IFC2X3 (confirmed against `ifc2x3.d.ts` and real Python's own dedicated `TestAddQtoIFC2X3` test class), so no schema guard was added, matching real Python exactly. **`edit_qto`** is genuinely the more intricate of the two (per this chunk's own task brief, NOT a simple attribute-setter loop like `editObjective`/`editReference`/etc. elsewhere in this project): edits the qto's own `Name`; adds/edits/removes individual quantities via a `properties` dict where `null` values are ALWAYS purged (unlike a future `edit_pset`, there is no "clear the value but keep the property" option for quantities — real Python's own docstring: "It is not allowed to have None quantities in IFC"); a `pset_template`-based type-inference mechanism (`get_canonical_property_type`/`infer_property_type`) picks the right `IfcQuantityLength`/`Area`/`Volume`/`Count`/`Weight`/`Time` subclass when no explicit `entity_instance` value is given, in priority order: (1) an explicit `entity_instance` value's own declared type always wins (with 2 real hard-coded IFC-naming-inconsistency corrections, `Numeric`->`Number` and `Mass`->`Weight`), (2) a resolved quantity-set template's matching property-by-name `TemplateType`, (3) `FLOAT_TYPE_KEYWORDS` name-keyword-matching for a fractional value (defaulting to `Length`) or an unconditional `Count` for a whole-number value with NO keyword consultation at all (a real, confirmed Python behavior, not a port simplification); and unbounded-depth nested `IfcPhysicalComplexQuantity` support (a complex quantity's own `HasQuantities` can itself contain further complex quantities), implemented by recursing through the exported, listener-wrapped `editQto` itself — not a plain internal helper — matching `../context/removeContext.ts`'s own established "recurse through the wrapped export so nested listener firing matches real Python" precedent. `util/pset.ts`'s already-landed `getTemplate(...).getByName(...)` (the same primitive `../owner/addApplication.ts` already uses) is reused directly for the template lookup — no new primitive needed. Schema verification: every positional entity-creation attribute order (`IfcElementQuantity`: `GlobalId`/`OwnerHistory`/`Name`/`Description`/`MethodOfMeasurement`/`Quantities`; all 6 `IfcQuantityXXX` subclasses: `Name`/`Description`/`Unit`/`XXXValue`(/`Formula`, IFC4+ only); `IfcPhysicalComplexQuantity`: `Name`/`Description`/`HasQuantities`/`Discrimination`/`Quality`/`Usage`) checked against all 3 generated `.d.ts`s — identical order in all 3 schemas, no DERIVE-attribute interleaving anywhere in this chunk. **6 real Python quirks/bugs found and disclosed, not silently fixed**: (1) a genuine, confirmed BUG in `edit_qto.py`'s own `load_qto_template` — a caller-supplied `pset_template` is assigned to `self.pset_template`, an attribute `get_canonical_property_type` never reads (it only ever reads `self.qto_template`, which that same branch never assigns), so real Python raises `AttributeError` the moment a plain numeric property value needs type inference in that branch — including real Python's OWN documented docstring example; this port can't reproduce an uninitialized-class-field crash the same way JS has no such error for reading `undefined`, so it degrades gracefully instead (a caller-supplied `psetTemplate` silently has no effect on inference, matching real Python's evident, if broken, intent rather than its crash) — disclosed in detail in `editQto.ts`'s own header comment and pinned by a dedicated test; (2) the same int-vs-float JS-representational gap `util/migrator.ts` already disclosed for `IfcCountMeasure`/`IfcQuantityCount` retyping — `Number.isInteger(value)` is used as the best-effort proxy for Python's `int`-vs-`float` object-type distinction, correct for any genuinely fractional value but unable to distinguish a whole-number Python `int` (always `Count`) from a whole-number Python `float` (keyword-matched); (3) the IFC4X3 `IfcQuantityCount`-as-integer coercion (`int(value)` vs. `float(value)`) applies ONLY on the update-existing-quantity path, never on the create-new-quantity path — a real, confirmed asymmetry in `edit_qto.py` itself, reproduced verbatim and pinned by dedicated, schema-gated tests; (4) updating an existing COMPLEX quantity with a non-dict value silently no-ops (the properties-dict key is still consumed, matching real Python's `del` sitting outside the if/elif chain) while the mirror-image case — updating an existing SIMPLE quantity with a dict value — throws (mirrors Python's own implicit `float(dict)` `TypeError`); (5) `Discrimination` is a REQUIRED dict key when creating a brand-new complex quantity (a bare Python `KeyError` if missing) but OPTIONAL, falling back to the existing value, when editing one in place — a real asymmetry in the source, preserved and tested both ways; (6) purging a complex quantity (`properties` value `null`) only removes the complex quantity ITSELF via a plain `self.file.remove(prop)` — its own nested quantities are never recursively deleted, just left as genuine orphaned entities still present in the file (confirmed empirically against this port's own built addon) — matching `../pset/removePset.ts`'s own established precedent that `file.remove()` never cascades to forward-referenced children on its own in either language. Test coverage: `test_add_qto.py`/`test_edit_qto.py` ported in full (all methods, including the "editing with an explicit type retains the existing quantity's own class" case, via a `createTypedValue` test-only raw-native fixture helper — the same disclosed, pre-existing `entityInstance.ts` gap `test/util/unit.test.ts`'s identically-named helper already established, since the production-facing `file.createEntity("IfcAreaMeasure", ...)` throws for a freshly-constructed, populated defined-type instance; `editQto.ts`'s own production code never hits this gap itself, since it only ever reads an already-constructed `entity_instance`'s `.isA()`/`.getByIndex(0)`, never constructs one), plus extensive original coverage this chunk's own brief specifically called for given `edit_qto`'s complexity: nested `IfcPhysicalComplexQuantity` creation/editing/removal including 2-level-deep nesting (real Python's own test suite has ZERO complex-quantity coverage at all, confirmed by reading `test_edit_qto.py` in full), all 6 disclosed quirks above each with a dedicated test, `inferPropertyType`'s keyword table exercised directly for every `FLOAT_TYPE_KEYWORDS` category, and new Transaction/undo-redo regression tests for every mutating function (particular scrutiny on undo restoring a purged nested complex quantity's own nested quantities, and on the qto-name-plus-quantities combined-edit path). The 2 bSI-template-dependent real-Python tests and the `psetTemplate`-bug-disclosure test are scoped to non-IFC2X3 schemas only (confirmed empirically, not assumed: `Qto_WallBaseQuantities` is genuinely absent from the bundled `Pset_IFC2X3.ifc` template file, and `IfcPropertySetTemplate`/`IfcSimplePropertyTemplate` don't exist in the IFC2X3 schema at all — matching real Python's own `TestEditQto` being IFC4-only with no IFC2X3 subclass); the IFC4X3-`IfcQuantityCount`-coercion tests are `describe.skipIf(!AVAILABLE_SCHEMAS.includes("IFC4X3"))`-gated per this chunk's own CI-learned lesson. Ran the full local suite (2702 passed, 5 skipped, the same 3 pre-existing unrelated failures in `api.unit.addMonetaryUnit`/`editMonetaryUnit` already tracked in `TODOS.md` and reconfirmed via `git stash` to be untouched by this chunk) against a locally-built all-3-schemas native addon; `tsc --noEmit`/`biome check` both clean. **Chunk 3 (`edit_pset.py`) remains unported, tracked as a separate future PR.** Independently self-reviewed directly against the real Python source and all 3 generated `.d.ts`s before opening the PR. |
| `api.pset` — `edit_pset` (chunk 3 of 3 — **`api.pset` module now FULLY COMPLETE, all 8 real Python files ported**: `remove_pset` (#77), `add_pset`/`assign_pset`/`unassign_pset`/`unshare_pset` (#86), `add_qto`/`edit_qto` (#87), `edit_pset` (this row)) | ✅ | [#89](https://github.com/mikitski/IfcOpenShell/pull/89) | Ports `edit_pset.py` (490 lines, real Python source) — the single largest file in the whole `api` package. Edits an `IfcPropertySet`'s (or material's/profile's "extended properties") own `Name` and its individual properties (add/edit/remove), either arbitrarily or driven by a property-set template. A 4-tier value-type-inference resolver (`get_primary_measure_type`): an existing property's own CURRENT type wins outright (tier 1, no equivalent in `edit_qto.ts` — structural, not an oversight: an `IfcPropertySingleValue.NominalValue` is a SEPARATE nested simple-type entity fully torn down and recreated on every edit, unlike a physical quantity, which mutates its own value attribute in place) > a `pset_template` match (tier 2) > an explicit `entity_instance`'s own class (tier 3) > a Python-type-based heuristic (tier 4: string/bool/number/`datetime`). Two structurally different existing-property update paths (`IfcPropertySingleValue`/`IfcPropertyEnumeratedValue`), plus a deliberate, disclosed `NotImplementedError`-equivalent throw for any other existing property class (`IfcPropertyListValue`/`IfcPropertyBoundedValue`/etc. — a real, documented Python limitation, not silently worked around or expanded). **`should_purge` (default `true`), a real, meaningful behavioral difference from `edit_qto.ts`** (which ALWAYS purges a `None` quantity): lets a caller set a property's value to `null` WITHOUT deleting the property object itself. **Shared-property safety check, verified against a ported test**: `file.get_total_inverses(prop) > 1` — an existing property referenced by more than one thing (e.g. aliased across two psets) is skipped entirely by `update_existing_properties` (left out of the pset's own new property list, its `properties`-map entry deliberately NOT consumed) and gets reprocessed by `add_new_properties` as a brand-new property instead; net effect: the shared entity is left completely untouched (still correct for whatever else references it), the edited pset gets an independent new property under the same name. **`pset_template`/attribute-mismatch bug — confirmed ABSENT here**, unlike `edit_qto.ts`'s own confirmed `pset_template`/`qto_template` bug: `load_pset_template` assigns the SAME `self.pset_template` attribute in both its branches, and `get_primary_measure_type` (the only reader) reads that same attribute uniformly — a caller-supplied `pset_template` genuinely takes effect for type inference in both real Python and this port. Positional attribute order for `IfcPropertySingleValue`/`IfcPropertyEnumeratedValue`/`IfcPropertyEnumeration`/`IfcPropertyListValue` confirmed identical (index-wise) across all 3 generated `.d.ts`s (only `Description`(IFC2X3/IFC4)-vs-`Specification`(IFC4X3) differs by NAME, never touched by `edit_pset.py` anyway, since it's always passed positionally as `null`). **A real, pre-existing, now THIRD independently-confirmed primitive-layer gap blocks most plain-scalar property values**: `cast_value_to_primary_measure_type`'s own probe-then-create technique (`file.create_entity(primary_measure_type).attribute_type(0)`) needs to materialize a brand-new typed value (e.g. `IfcLabel("hi")`) from a raw JS scalar — but `EntityInstance.setByIndex`/the newly-added `attributeType()` public method both gate on the target being a real multi-attribute entity, throwing for any standalone simple/defined-type instance; the exact same gap already disclosed in `util/migrator.ts` (finding 1) and, even more directly, `api/unit/addConversionBasedUnit.ts` (that whole function is permanently blocked by it). Ported completely and faithfully anyway (correct the moment the gap closes), prominently disclosed at the very top of `editPset.ts`'s own header comment. What remains fully functional, unaffected: renaming a pset; purging/clearing an existing property (no new value needed); assigning an already-built `entity_instance` value (to a new or existing property); full `IfcProperty` passthrough; copying enum data from another existing property; the shared-property/`NotImplementedError`/non-property-entity dispatch logic (all of which throw or skip BEFORE ever needing a new typed value). `getPrimaryMeasureType`/`inferPrimaryMeasureType` (the tier-4 heuristic, mirroring `editQto.ts`'s own `inferPropertyType` precedent) are exported and independently, thoroughly tested as pure functions, since neither ever calls `file.createEntity`. Other disclosed quirks: a unit-wrapped `null` value (`{Unit, NominalValue: null}`) bypasses `add_new_properties`'s own `should_purge` skip check (which runs on the RAW, still-wrapped value before unwrapping) — an `IfcPropertySingleValue` with an explicit `null` `NominalValue` gets created unconditionally, a real, undocumented-by-Python's-own-docstring surprise; an explicit-throw edge case where an empty-array enum update with `shouldPurge: false` and no existing enum reference/values hits real Python's own `value[0]` `IndexError` (JS array indexing would otherwise silently return `undefined`, so this port adds an explicit `RangeError` to preserve the crash); and 3 further narrow, zero-Python-test-coverage divergences in `cast_value_to_primary_measure_type`'s `type_fn` dispatch table (`LOGICAL`-cast string casing, `BINARY`-cast best-effort passthrough, aggregate-of-entity casting). Also adds `EntityInstance.attributeType(index)`, a thin public wrapper around the already-bound native `entity_instance.attribute_type` primitive (no new native surface) — needed by this chunk, the first caller of this exact instance-level shim. Test coverage: every real Python `test_edit_pset.py` method ported, adapted per the disclosed gap above (unblocked paths get real passing assertions using a `createTypedValue`-style raw-native test fixture helper identical to `editQto.test.ts`'s/`test/util/unit.test.ts`'s own established workaround; scalar-value-creation paths get `expect(...).toThrow(BLOCKED_ERROR)` assertions pinning the current, disclosed behavior — matching `addConversionBasedUnit.test.ts`'s own established precedent for the identical gap, each with a comment recording the real, unblocked assertion to restore once the gap closes), plus original coverage: `getPrimaryMeasureType`/`inferPrimaryMeasureType` unit tests (all 4 tiers, including the disclosed JS int/float and `Date`-has-no-`IfcDate`-only-equivalent gaps), the `NotImplementedError`-equivalent throw, a non-property entity throwing when assigned as a value, the unit-wrapped-`null` quirk, the empty-array `IndexError` edge case, and Transaction/undo-redo regression tests for every currently-unblocked mutation. Ran the full local suite (1499 passed, 45 skipped — IFC2X3/IFC4X3-gated, matching CI's `SCHEMA_VERSIONS=4`-only build — 0 failures) against a locally-built all-3-schemas native addon (the full C++ core built from source for this chunk, `-DSCHEMA_VERSIONS=4 -DBUILD_IFCGEOM=OFF` etc., matching `ci-ifcopenshell-ts.yml`'s own flags); `tsc --noEmit`/`biome check` both clean. Independently self-reviewed directly against the real Python source and all 3 generated `.d.ts`s before opening the PR. |
| `api.type` — `unassign_type` | ✅ | [#73](https://github.com/mikitski/IfcOpenShell/pull/73) | Port of `type/unassign_type.py` (71 lines) — the one small, well-scoped function of `api.type`, per `research/02-api-layer.md` §3's own early-target recommendation. Structurally near-identical to `aggregate.unassignObject`/`spatial.unassignContainer`'s own build-a-rels-set/rewrite-or-delete-with-`removeDeep2` shape, plus one genuine addition: a real IFC2X3-vs-IFC4+ schema branch, verified directly against the generated `.d.ts`/schema-entity JSON (not assumed) — IFC2X3's `IfcObject` has no `IsTypedBy` inverse attribute at all (only `IsDefinedBy`, filtered down to `IfcRelDefinesByType`), while IFC4/IFC4X3 add the dedicated `IsTypedBy` inverse (confirmed: both target the same `IfcRelDefinesByType` class, not a different relationship type — IFC4 just added a more direct inverse attribute onto the same underlying relationship). This same branch was already established by `util/element.ts`'s `getType`, reused here for test assertions. Real, disclosed upstream bug found and ported verbatim, not silently fixed: real Python's `for rel in rels` loop shadows a single `related_objects_set` variable across iterations (`related_objects_set = set(rel.RelatedObjects) - related_objects_set`), rebinding it to the *previous* iteration's leftover set rather than the original caller-supplied target set — unlike `unassignObject.ts`/`unassignContainer.ts`'s own loops, whose Python sources use a fresh per-iteration variable name. Confirmed as a real behavioral bug (not just an unusual-but-equivalent rewrite) by direct simulation: when `related_objects` spans elements typed by different rels, every rel processed after the first is filtered against the wrong set, silently leaving a later object's type assignment untouched. Pinned by a dedicated regression test reproducing the exact (buggy) real-Python-matching outcome. `assign_type` (319 lines, the largest file in this group, per-docstring propagates property sets/representation mapping — real independent design work) and `map_type_representations` (a fan-out operation over every occurrence of a type) are genuinely out of scope for this chunk and remain unported — see next row. Test coverage ported from `test/api/type/test_unassign_type.py` (all 3 real cases, adapted to construct the `IfcRelDefinesByType` precondition directly via `file.createEntity(...)` rather than via the unported `type.assign_type`, matching `unassignObject.test.ts`'s own precedent for an unported sibling "assign" dependency), plus new `Transaction`/undo-redo regression tests (delete-to-zero-members and rewrite-when-multiple-remain) and the disclosed-bug regression test above — all run against `describe.each(AVAILABLE_SCHEMAS)` (IFC2X3/IFC4/IFC4X3), genuinely exercising both schema branches, not just one. Independently self-reviewed directly against the real Python source before merge; no further bugs found. |
| `api.type` — `assign_type`/`map_type_representations` (**`api.type` module now FULLY COMPLETE, all 3 real Python files ported**: `unassign_type` (#73), `assign_type`/`map_type_representations` (this row)) | ✅ | [#90](https://github.com/mikitski/IfcOpenShell/pull/90) | Ports the last 2 real Python files of `api.type`: `assign_type.py` (319 lines, the largest file in this group — assigns a type, e.g. `IfcWallType`, to one or more occurrences, e.g. `IfcWall`) and `map_type_representations.py` (102 lines — ensures an occurrence mirrors its type's own geometric representation; a genuine, load-bearing dependency `assign_type` calls directly for every newly-typed occurrence when `should_map_representations` is true, the default and non-empty `RepresentationMaps` — not an optional side chunk, ported together). **Two real, load-bearing dependencies on modules with NO TS port at all (`api.material`) or only a 2-of-~34-functions partial port (`api.geometry`)** — disclosed as loud, descriptive throws rather than silently skipped or partially reimplemented, matching this project's established `../owner/internalCascadeHelpers.ts` `removeProductCascade` precedent (2 new `TODOS.md` entries): (1) `map_material_usages` (assign_type's own final helper) needs `ifcopenshell.api.material.assign_material` — `api.material` has zero TS port of any kind — fires only when `should_map_representations` is true AND the type's own material (`util.element.getMaterial`, already landed) resolves to an `IfcMaterialLayerSet`/`IfcMaterialProfileSet`; (2) `map_type_representations`'s own second half needs `ifcopenshell.api.geometry.map_representation`/`.assign_representation`, neither landed (only `unassign_representation`/`remove_representation` have, an unrelated `api.context.removeContext` dependency) — fires only when the type's `RepresentationMaps` is non-empty, and throws BEFORE running its own otherwise-safe representation-stripping loop, so a blocked call never leaves an occurrence with its old representation removed and nothing to replace it with. Both throws can fire AFTER `assignType`'s own `IfcRelDefinesByType` surgery has already committed — this matches exactly what would happen if the real dependency itself raised partway through, not a TS-specific regression; `shouldMapRepresentations: false` avoids both blockers entirely and is fully tested end to end (including against a type that genuinely has both a representation and a material). **Occurrence/type class-pairing validation is a genuine three-layer fallback, not one lookup** — verified line by line against the real 319-line source: (1) the already-landed `util.type.getApplicableEntities` (the buildingSMART implementer-agreement map); (2) the type's own schema-declared `ApplicableOccurrence` attribute (e.g. Bonsai's `IfcTypeProduct`-for-annotations use case, which the map has no entry for at all), honored via a `declaration_by_name_with_name`-based try/catch schema-existence check (confirmed via `src/ifcparse/schema.h` that it throws, not returns null, for an unknown class); (3) a universal `Type`-suffix-stripping fallback for process/resource types the map doesn't cover at all (`IfcTaskType` -> `IfcTask`), gated by the same existence check. All three layers isolated and independently pinned by dedicated tests (layer 2 via a real `IfcTypeProduct`/`IfcAnnotation` pairing with and without `ApplicableOccurrence` set; layer 3 via real `IfcTaskType`/`IfcTask`, confirmed absent from the map). A mismatched class anywhere in the batch rejects the WHOLE call before any mutation (`test_partial_mismatch_in_selection_rejects_whole_call`). **IFC2X3-vs-IFC4+ inverse dispatch** verified directly (none of these inverses appear in the generated `.d.ts`s, same as every other inverse this project reads via `EntityInstance.get`'s dynamic dispatch): `ObjectTypeOf`(2X3)/`Types`(4+) for the type's own existing rel; `IsDefinedBy`-filtered(2X3)/`IsTypedBy`(4+) for an occurrence's existing rel — the same branch `util/element.ts`'s `getType`/`unassignType.ts` already established. **Surgery shape** structurally closest to `api.project.assignDeclaration`'s own `objectsWithoutContexts`/`objectsWithContexts` partition (not `api.classification`/`api.document`'s single-object shape) — one confirmed real difference: `assign_type.py` compares an object's existing rel against the type's own single `Types`/`ObjectTypeOf` rel by identity, not a membership test against a *set* of every rel the type could have (unlike `assign_declaration.py`'s `Declares`, which schema-wise permits more than one). **`PredefinedType`/`ObjectType` "double typing" cleanup** (real GitHub issue #7006, with a real GitHub issue #7011 NOTDEFINED carve-out) reuses the already-landed `util.element.getPredefinedType` directly, both branches ported and tested across schemas. `IfcRelDefinesByType` positional order (`GlobalId`/`OwnerHistory`/`Name`/`Description`/`RelatedObjects`/`RelatingType`) confirmed identical across all 3 generated `.d.ts`s. Test coverage: `test_assign_type.py`/`test_map_type_representation.py` ported as fully as the two blockers allow — `test_map_representation`/`test_map_material_usages` pinned as dedicated "throws the disclosed blocked error" regression tests (matching `addConversionBasedUnit.test.ts`/`editPset.test.ts`'s established precedent) rather than silently dropped; `test_do_not_map_representation_if_type_was_assigned_previously`/`test_do_not_reassign_material_if_it_was_assigned_previously` are the one genuine gap — both need a *successful* prior mapping call to construct their precondition, which is unreachable while blocked, so neither can even be adapted today (disclosed in `assignType.test.ts`'s own header comment, to be ported for real once `api.material`/`api.geometry` land); every other real Python test case (assigning, no-op reassignment, old-rel update/purge, `should_map_representations: false`, both `PredefinedType`/`ObjectType` cases, all 4 class-mismatch/untypable-occurrence cases) ported in full, plus the 3-layer-validation and Transaction/undo-redo tests above. Ran the full local suite (2841 passed, 5 skipped, the same 3 pre-existing unrelated failures in `api.unit.addMonetaryUnit`/`editMonetaryUnit` already tracked in `TODOS.md`, confirmed untouched by this chunk via `git diff origin/v0.9.0`) against a locally-built all-3-schemas native addon; `tsc --noEmit`/`biome check` both clean. Independently self-reviewed directly against the real Python source and all 3 generated `.d.ts`s before opening the PR. |
| `api.classification` | ✅ | [#78](https://github.com/mikitski/IfcOpenShell/pull/78) | Port of `api/classification/` (6 files, ~715 lines): `add_classification`/`add_reference`/`edit_classification`/`edit_reference`/`remove_classification`/`remove_reference`, completing that module. Manages `IfcClassification`/`IfcClassificationReference` — external classification systems (e.g. Uniclass, Omniclass) and references into them, assigned via `IfcRelAssociatesClassification` (rooted objects) or `IfcExternalReferenceRelationship` (non-rooted resource objects, IFC4+ only). A real, self-contained module confirmed by reading every file: only ever imports `ifcopenshell.api.owner` (`create_owner_history`/`update_owner_history`, both already landed), `ifcopenshell.util.date`/`ifcopenshell.util.element`/`ifcopenshell.util.schema` (all already fully ported), plus `ifcopenshell.guid`. One real, disclosed util-layer addition: `util.element.get_referenced_elements` (and its backing `REFERENCE_TYPES` table) had been deliberately deferred out of `util/element.ts`'s own earlier 3-chunk port as belonging to "classification/document/library association" territory, not that chunk's spatial/structural-graph domain — `add_reference`/`remove_reference` are its first genuine callers, so it's ported here in full (all 6 `REFERENCE_TYPES` entries, not narrowed to just the rows this module exercises), appended to `util/element.ts` just after `has_openings`, matching Python's own function ordering. Two real, disclosed IFC2X3-vs-IFC4+ schema differences, investigated directly against the real Python source and the generated `.d.ts`s: (1) `IfcClassificationReference` has no `Identification` attribute on IFC2X3 — its equivalent field is `ItemReference` instead, branched throughout `add_reference`/`remove_reference` exactly as real Python does; (2) `IfcExternalReferenceRelationship` (the non-rooted "resource object" association class) doesn't exist in IFC2X3 at all — both functions raise `TypeError` for a non-`IfcRoot` product there before ever reaching that code path (real Python's own guard, ported verbatim including its own copy-pasted "Cannot add reference..." wording even inside `remove_reference`'s identical check, never corrected upstream for that function's own context). `remove_reference` mirrors `pset.removePset`/`group.removeGroup`'s established "detach vs. fully delete" branching shape, per this chunk's own task brief; a real, disclosed Python-source asymmetry preserved verbatim: its early-return-on-no-op check computes `products ∩ referenced`, but the actual rooted/non-rooted partition below it iterates the ORIGINAL `products` parameter rather than that filtered intersection (harmless in practice, since subtracting a non-member from a rel's `RelatedObjects` is a no-op). `IfcClassification`/`IfcClassificationReference`/`IfcRelAssociatesClassification`/`IfcExternalReferenceRelationship`'s attribute orders verified contiguous and identical across all 3 schemas' generated `.d.ts` files — no `IfcGeometricRepresentationSubContext`-style DERIVE-interleaving gotcha anywhere in this chunk (none of these 4 classes declare any DERIVE attribute). Test coverage ported from all of `test/api/classification/test_*.py` (`test_add_classification.py`/`test_add_reference.py`/`test_remove_classification.py`/`test_remove_reference.py` — `edit_classification.py`/`edit_reference.py` have no real Python test files at all, confirmed by directory listing; original coverage added instead, matching `../group/editGroup.test.ts`'s established shape for the identically-structured Python source), plus original coverage for `add_reference`'s `is_lightweight=False`/existing-reference-reuse branches (not exercised anywhere in real Python's own test suite) and new Transaction/undo-redo regression tests for every mutating function. Independently self-reviewed directly against the real Python source before merge; no further bugs found. |
| `api.group` — all 6 functions | ✅ | [#77](https://github.com/mikitski/IfcOpenShell/pull/77) | Port of `api/group/` (6 files, ~360 lines): `add_group`/`assign_group`/`edit_group`/`remove_group`/`unassign_group`/`update_group_products`, completing that module. Manages `IfcGroup` via `IfcRelAssignsToGroup` — a generic, non-spatial grouping mechanism, distinct from `api.system`/`api.spatial`/`api.aggregate`/`api.classification`. `assignGroup`/`unassignGroup` only ever look at `group.IsGroupedBy[0]`, the first pre-existing rel — real Python's own behavior, not something this port introduces; `updateGroupProducts` is the one function that walks every rel in `IsGroupedBy`, preserving nested-group membership (`[g for g in rel.RelatedObjects if g.is_a("IfcGroup")]`) while purging every rel past the first (real Python's own comment: "in ifc4 IfcGroup can have multiple rels"). `remove_group.py` additionally calls `ifcopenshell.api.pset.remove_pset`, ported alongside as a minimal dependency — see the `api.pset` row above and `pset/removePset.ts`'s own header comment. Real, disclosed finding, investigated not assumed: `remove_group`'s own `IfcRelAssignsToGroup` cleanup only explicitly handles 2 of 3 possible "this rel references `group`" shapes (deleting the whole rel when `group` is either the `RelatingGroup` or the SOLE `RelatedObjects` member of another group's rel) — the third shape (`group` nested alongside OTHER members) needs no explicit branch at all, since `IfcFile.remove`'s own documented automatic aggregate-reference cleanup (confirmed against its doc comment and empirically against this port's own built native addon) already splices `group` out of that rel correctly; not a dangling-reference bug, a deliberate asymmetry that only looks incomplete in isolation. Test coverage ported from all of `test/api/group/test_*.py` (`test_add_group.py`/`test_assign_group.py`/`test_remove_group.py`/`test_unassign_group.py`/`test_update_group_products.py` — `edit_group.py` has no real Python test file at all, confirmed by directory listing; original coverage added instead — `update_group_products.py`'s own IFC4-vs-IFC2X3 test-class split, over "can IfcGroup have multiple rels," is ported as a schema branch inside a single `describe.each(AVAILABLE_SCHEMAS)` block, extended to IFC4X3 too since nothing in the function itself is schema-specific), plus new Transaction/undo-redo regression tests for every mutating function (particular scrutiny on `removeGroup`'s cascading rel/pset/property cleanup and `updateGroupProducts`'s multi-rel purge). Independently self-reviewed directly against the real Python source before merge; no further bugs found. |
| `api.layer` — all 6 functions | ✅ | [#76](https://github.com/mikitski/IfcOpenShell/pull/76) | Port of `api/layer/` (6 files, ~373 lines): `add_layer`/`add_layer_with_style` (create `IfcPresentationLayerAssignment`/`IfcPresentationLayerWithStyle`), `assign_layer`/`unassign_layer` (manage the layer's own `AssignedItems` attribute directly — NOT a separate rel entity, unlike `api.spatial`/`api.aggregate`/`api.type`'s containment/aggregation/typing rels), and `edit_layer`/`remove_layer` (trivial attribute-setter loop / single `file.remove` call). A real, self-contained module confirmed by reading every file: no `ifcopenshell.util.*`/`ifcopenshell.api.*` cross-module dependency at all, only bare `ifcopenshell`/`collections.abc.Sequence`/`typing`. `IfcPresentationLayerAssignment`/`IfcPresentationLayerWithStyle`'s attribute orders verified contiguous and identical across all 3 schemas' generated `.d.ts` files — no `IfcGeometricRepresentationSubContext`-style DERIVE-interleaving gotcha here (neither class declares any DERIVE attribute at all). Two real, disclosed findings: (1) a pre-existing primitive-layer gap, not introduced or fixed here — `LayerOn`/`LayerFrozen`/`LayerBlocked` are EXPRESS `LOGICAL` (tri-state) attributes, and this port's `EntityInstance.get()` returns the raw `1`/`0` for TRUE/FALSE (not JS `true`/`false`) since only the write-side `valueToVariant()` special-cases `declaredKind === LOGICAL`, with no symmetric read-side unwrap — confirmed empirically against this exact worktree's own built native addon; `addLayerWithStyle.test.ts` asserts the actual `1`/`0` round-trip rather than masking it (real Python's own test passes only because Python's `bool` is an `int` subclass, so `1 == True`, a language-level coincidence this TS port doesn't get for free). (2) A genuine real-Python-source bug in `unassign_layer.py`, reproduced verbatim rather than silently guarded away: `set(layer.AssignedItems) or set()` looks like a `None`-guard but isn't one (`set(None)` raises `TypeError` before the `or` ever runs, unlike `assign_layer.py`'s own actually-safe `set(layer.AssignedItems or [])`), so real `unassign_layer` genuinely crashes on a layer whose `AssignedItems` was never set — this port's own `EntityInstanceSet.update()` deliberately does not tolerate a `null` iterable itself (only individual `null` elements within one) specifically so this crash is preserved rather than accidentally fixed by JS's own `Set` constructor treating `null` as empty; pinned by a dedicated regression test. Test coverage ported from all of `test/api/layer/test_*.py` (`test_add_layer.py`/`test_add_layer_with_style.py`/`test_assign_layer.py`/`test_unassign_layer.py` — `edit_layer.py`/`remove_layer.py` have no real Python test files at all, confirmed by directory listing; original coverage added instead, matching `../context/editContext.test.ts`/`removeContext.test.ts`'s established shape for the identically-structured Python source), plus new Transaction/undo-redo regression tests for every mutating function and the disclosed-bug regression test above. Independently self-reviewed directly against the real Python source before merge. |
| `api.document` | ✅ | [#79](https://github.com/mikitski/IfcOpenShell/pull/79) | Port of `api/document/` (8 files, ~600 lines): `add_information`/`add_reference`/`assign_document`/`edit_information`/`edit_reference`/`remove_information`/`remove_reference`/`unassign_document`, completing that module. Manages `IfcDocumentInformation`/`IfcDocumentReference` — external project documents (drawings, specifications, certificates, reports) and references into them, assigned to model elements via `IfcRelAssociatesDocument`. Structurally the closest sibling to `api.classification` above (confirmed by reading both side by side, per the task brief), but with one real, disclosed asymmetry: unlike `classification.addReference`/`removeReference`, this module's `assign_document`/`unassign_document` do NOT support non-`IfcRoot` "resource" objects via `IfcExternalReferenceRelationship` at all — real Python's own source carries an unresolved `# TODO: do we need to support non-ifcroot elements like we do in classification.add_reference?` comment on both functions, confirmed directly; ported verbatim, not "fixed" to add that parity. A real, self-contained module confirmed by reading every file: only imports `ifcopenshell.api.owner` (`create_owner_history`/`update_owner_history`, both already landed) and `ifcopenshell.util.element` (already fully ported, including its `REFERENCE_TYPES` table's `IfcDocumentReference`/`IfcDocumentInformation` rows, added in the `api.classification` chunk above — this module is their first genuine caller), plus `ifcopenshell.guid`. Three real, disclosed IFC2X3-vs-IFC4+ schema differences, investigated directly against the real Python source and the generated `.d.ts`s: (1) `IfcDocumentReference` has no `Identification` attribute on IFC2X3 — its equivalent field is `ItemReference` instead, AND it has no `ReferencedDocument` forward link to `IfcDocumentInformation` at all; IFC2X3 links the other way, via a forward `IfcDocumentInformation.DocumentReferences` list attribute instead (the task brief's own predicted "analogous IFC2X3-vs-IFC4+ naming difference," confirmed, but with an extra wrinkle beyond a simple rename). (2) `IfcDocumentInformationRelationship` has no `Name`/`Description` attributes on IFC2X3 at all, shifting `RelatingDocument`/`RelatedDocuments` two positions earlier than on IFC4+ — a NEW finding, not called out by the task brief, requiring `addInformation`'s positional `createEntity` call to branch on schema even though real Python's kwargs-based call doesn't need to. (3) `IfcExternalReferenceRelationship` doesn't exist in IFC2X3 at all — moot for this module specifically, since (per finding above) `assignDocument`/`unassignDocument` never reach that class on any schema. `IfcDocumentInformation`/`IfcRelAssociatesDocument`'s attribute orders verified contiguous and identical across all 3 schemas' generated `.d.ts` files — no DERIVE-interleaving gotcha (neither class declares any DERIVE attribute). `removeInformation`'s `IsPointer`/`IsPointedTo` recursive subdocument-deletion dance (self-recursion through the wrapped exported function, matching `context.removeContext`'s established pattern) is genuinely order-dependent real-Python behavior — a multi-child `IfcDocumentInformationRelationship` is only deleted once the LAST remaining child is removed, relying on `IfcFile.remove`'s own automatic "splice a just-removed entity out of every list attribute referencing it" cascade — verified directly against `test_remove_information.py`'s own `test_removing_all_references_of_an_information` and ported verbatim, not eagerly pruned. Test coverage ported from all of `test/api/document/test_*.py` (`test_add_information.py`/`test_add_reference.py`/`test_assign_document.py`/`test_remove_information.py`/`test_remove_reference.py`/`test_unassign_document.py` — `edit_information.py`/`edit_reference.py` have no real Python test files at all, confirmed by directory listing; original coverage added instead, matching `../classification/editClassification.test.ts`'s established shape), plus original coverage for `assignDocument`'s "no products left to assign" early-return (not exercised anywhere in real Python's own test suite) and new Transaction/undo-redo regression tests for every mutating function, with particular scrutiny on `removeInformation`'s cascading subdocument/reference/rel cleanup. Independently self-reviewed directly against the real Python source before merge; no further bugs found. Ran the full local suite (2016 passed, 5 skipped, 3 pre-existing unrelated failures in `api.unit.addMonetaryUnit`/`editMonetaryUnit` traced to the borrowed multi-schema native addon build's IFC2X3 currency-enum keywords, already tracked in `TODOS.md`, untouched by this chunk). |
| `api.library` | ✅ | [#81](https://github.com/mikitski/IfcOpenShell/pull/81) | Port of `api/library/` (8 files, ~542 lines): `add_library`/`add_reference`/`assign_reference`/`edit_library`/`edit_reference`/`remove_library`/`remove_reference`/`unassign_reference`, completing that module — the third and final sibling in the `classification`/`document`/`library` family (all 3 now fully ported). Manages `IfcLibraryInformation`/`IfcLibraryReference` — external classification/reference libraries (e.g. Uniclass, a manufacturer's product library, a Brickschema dataset, a BACnet system) — assigned to rooted objects via `IfcRelAssociatesLibrary`. A real, self-contained module confirmed by reading every file: only imports `ifcopenshell.api.owner` (`create_owner_history`/`update_owner_history`, both already landed) and `ifcopenshell.util.date`/`ifcopenshell.util.element` (both already fully ported, including `get_referenced_elements`'s `REFERENCE_TYPES` table's `IfcLibraryReference`/`IfcLibraryInformation` rows — added in the `api.classification` chunk above, already exercised once by `api.document`, this module is simply their third caller), plus `ifcopenshell.guid`. Structurally closest to `api.document` overall — confirmed directly, not assumed from either sibling's shape: real Python's own `document.assign_document`/`unassign_document` docstrings explicitly cross-reference this module ("NOTE: reuses the same shape as `library.assign_reference`"/"`library.unassign_reference`"), and `assignReference.ts`/`unassignReference.ts` are indeed near carbon-copies of `assignDocument.ts`/`unassignDocument.ts` (same real-Python `# TODO: do we need to support non-ifcroot elements like we do in classification.add_reference?` comment on both, so neither module supports non-`IfcRoot` "resource" objects via `IfcExternalReferenceRelationship` at all) — except `assignReference` is simpler than `assignDocument`: `reference` is always an `IfcLibraryReference` (never also an `IfcLibraryInformation`), so no `is_a()` dispatch is needed to pick the IFC4+ inverse attribute name, unlike `assignDocument`'s `IfcDocumentReference`-vs-`IfcDocumentInformation` branch. `add_reference`/`edit_reference`/`remove_reference`, by contrast, are each simpler and smaller than either sibling's equivalents — no non-`IfcRoot` support (unlike `classification`) and no multi-parent/subdocument tree (unlike `document`'s `IfcDocumentInformationRelationship` machinery) — confirmed directly against the real Python source for all 8 files, not assumed from either sibling's shape. Three real, disclosed IFC2X3-vs-IFC4+ schema differences, investigated directly against the real Python source and the generated `.d.ts`s, not assumed from either sibling's own findings: (1) `IfcLibraryReference` has only 3 attributes on IFC2X3 (`Location`/`ItemReference`/`Name`) vs. 6 on IFC4+ (`Location`/`Identification`/`Name`/`Description`/`Language`/`ReferencedLibrary`) — no `ReferencedLibrary` forward link at all on IFC2X3, which instead links the other way via a forward `IfcLibraryInformation.LibraryReference` list attribute (the same general shape as `document`'s `Identification`-vs-`ItemReference` finding, but a starker attribute-count gap: 3 vs. 6, not 4 vs. 5). (2) `IfcLibraryInformation.VersionDate` is an `IfcCalendarDate` ENTITY reference on IFC2X3 but a plain `IfcDateTime` STRING on IFC4+ — a genuinely new finding, not called out by either sibling module (neither `IfcClassification` nor `IfcDocumentInformation` has an analogous datetime-typed attribute this port has needed to special-case in an `edit_*` function before); `editLibrary.ts` special-cases `VersionDate` via `util/date.ts`'s `datetime2ifc`, using the project's established `kind: "datetime"` discriminator as the substitute for Python's `isinstance(dt, datetime.datetime)` guard (a plain string/other value is left untouched, exactly matching Python's behavior — pinned by a dedicated regression test). (3) `remove_reference.py`'s own IFC2X3 branch does a direct `file.by_type("IfcRelAssociatesLibrary")` scan filtered by `RelatingLibrary == reference`, NOT `document.removeReference`'s own `file.get_inverse(reference)` call — a real, disclosed asymmetry between the two structurally-similar sibling modules, confirmed directly against both real Python sources (not a translation slip in either port). `IfcLibraryInformation`/`IfcLibraryReference`/`IfcRelAssociatesLibrary`'s attribute orders verified contiguous and identical across all 3 schemas' generated `.d.ts` files wherever both schemas declare the same shape — no DERIVE-interleaving gotcha anywhere in this chunk (none of these 3 classes declare any DERIVE attribute). `remove_library.py`'s own IFC4+ branch walks a THIRD distinct inverse attribute name, `HasLibraryReferences` (a library's own child references — distinct from `REFERENCE_TYPES`'s `LibraryInfoForObjects`, which covers rels associating products directly with the library itself), confirmed directly against the real Python source, dynamically accessed the same way as every other undeclared inverse attribute in this chunk. Test coverage ported from all of `test/api/library/test_*.py` — uniquely among the 3 sibling modules, every one of the 8 functions has a real Python test file (no gaps requiring original coverage for the ported behavior itself), plus original coverage for `editLibrary`'s `VersionDate` pass-through guard (not exercised anywhere in real Python's own test suite, since it only ever passes an actual `datetime.datetime`) and new Transaction/undo-redo regression tests for every mutating function. Ran the full local suite (1245 passed, 31 skipped — IFC2X3/IFC4X3 unavailable in this build's C++ core, matching CI's own `-DSCHEMA_VERSIONS=4` gap, not a regression from this chunk). Independently self-reviewed directly against the real Python source and the generated `.d.ts`s before opening the PR (line-by-line, function-by-function); no further bugs found. |
| `api.constraint` | ✅ | [#82](https://github.com/mikitski/IfcOpenShell/pull/82) | Port of `api/constraint/` (10 files, ~566 lines): `add_metric`/`add_metric_reference`/`add_objective`/`assign_constraint`/`edit_metric`/`edit_objective`/`remove_constraint`/`remove_metric`/`unassign_constraint`, completing that module. Manages `IfcObjective` (a qualitative parametric constraint) and `IfcMetric` (a quantitative benchmark linked to an objective), assigned to products via `IfcRelAssociatesConstraint` — real Python's own module docstring warning reproduced verbatim: "usage of constraints are mostly untested in real life applications." Despite superficially resembling the `classification`/`document`/`library` family, this module's shape is genuinely its own — confirmed directly against the real Python source per the task brief's own explicit warning, not assumed from that family's precedent: (1) `add_metric_reference`'s dotted `"attribute.attribute.attribute"` reference-path parsing (a linked chain of `IfcReference` entities via `InnerReference`, with the outermost one wired onto `metric.ReferencePath`) has no analogue anywhere in that family; (2) `assign_constraint`/`unassign_constraint` look up existing rels via `IfcFile.getInverse(constraint)` (a generic, schema-agnostic "who references this" query), NOT via `util/element.ts`'s `REFERENCE_TYPES`/`getReferencedElements` machinery the `classification`/`document`/`library` family's own `assign*`/`unassign*` functions all share — confirmed directly: neither `IfcObjective` nor `IfcMetric` is a `REFERENCE_TYPES` entry, so `getReferencedElements` is genuinely not relevant here, contrary to what the family resemblance might suggest; (3) `IfcObjective`/`IfcMetric` are not `IfcRoot` subtypes at all (no `GlobalId`, no `OwnerHistory`), so `add_objective`/`add_metric`/`edit_objective`/`edit_metric` have no `owner.create_owner_history`/`update_owner_history` dependency whatsoever, unlike every sibling module's own `add_*`/`edit_*` functions. A real, self-contained module confirmed by reading every file: only imports `ifcopenshell.api.owner` (`assign_constraint`/`unassign_constraint` only, both already landed) and `ifcopenshell.util.element` (`remove_constraint`/`remove_metric`/`unassign_constraint`, for `remove_deep2`, already fully ported). Two real, disclosed IFC2X3-vs-IFC4+ schema limitations, investigated directly against the real Python source and the generated `.d.ts`s: (1) `IfcReference` doesn't exist at all on IFC2X3, and `IfcMetric.ReferencePath` doesn't exist there either (only added on IFC4+) — `add_metric_reference` is IFC4+-only in practice; real Python itself has no IFC2X3 guard, so this port reproduces the resulting throw verbatim (`file.createEntity("IfcReference")`/`metric.set("ReferencePath", ...)` both throw via this port's existing "unknown class"/"no such attribute" primitives). (2) `IfcResourceConstraintRelationship` doesn't exist at all on IFC2X3 either — combined with finding 1, `remove_metric` is effectively unusable on IFC2X3: real Python's own unguarded `metric.ReferencePath`/`file.by_type("IfcResourceConstraintRelationship")` calls both throw there, ported verbatim (not "fixed" with a guard real Python itself never had). One more disclosed finding, narrower than a schema difference: `IfcObjective.BenchmarkValues` appears as a bare, non-array `IfcMetric | null` in `ifc2x3.d.ts` but as `IfcConstraint[] | null` in `ifc4.d.ts`/`ifc4x3.d.ts` — almost certainly a `.d.ts`-generation artifact for LIST attributes declared through IFC2X3's own indirect `IfcMetricValue` defined-type alias, not a genuine scalar attribute; `addMetric.ts` follows the already-landed `util/constraint.ts`'s `getMetrics` (this exact attribute's other real caller, from an earlier Phase 3 chunk), which already reads it as an array unbranched across all 3 schemas — not a new assumption introduced here. None of these 3 findings could be empirically verified against a real IFC2X3 file in this sandbox (only the IFC4 schema plugin is registered in the locally available native addon build, matching `api.library`'s own already-tracked, pre-existing build gap above — not a regression from this chunk). `IfcObjective`/`IfcMetric`/`IfcReference`/`IfcRelAssociatesConstraint`'s attribute orders verified contiguous (and, for `IfcObjective`, landing back on the same final position across the two diverging schema branches) across all 3 schemas' generated `.d.ts` files — no DERIVE-interleaving gotcha anywhere in this chunk (none of these classes declare any DERIVE attribute). One self-caught authoring slip during development, not a native-addon or schema bug: an early draft of `addObjective.ts`'s positional `createEntity` call was one `null` short between `ConstraintGrade`(2) and `ObjectiveQualifier`(9), silently shifting `ObjectiveQualifier`'s value onto position 8 (`LogicalAggregator`, a different enum) and throwing "Unable to find keyword in schema" — root-caused via direct `setByIndex`-level instrumentation against the built native addon (ruling out a suspected V8/native-argument-count bug first, including a false lead from `vite-node`'s own transform, and a red herring from `IfcSIUnit`'s real `Dimensions`-placeholder quirk) before finding the actual off-by-one; fixed and verified before opening the PR, not left in. Test coverage ported from both of `test/api/constraint/test_*.py` (`test_assign_constraint.py`/`test_unassign_constraint.py` — `add_metric.py`/`add_metric_reference.py`/`add_objective.py`/`edit_metric.py`/`edit_objective.py`/`remove_constraint.py`/`remove_metric.py` have no real Python test files at all, confirmed by directory listing; original coverage added instead, matching this project's established shape for untested functions, including a dedicated regression test for each of the 2 disclosed IFC2X3-unusable findings above), plus new Transaction/undo-redo regression tests for every mutating function. Ran the full local suite (1286 passed, 33 skipped — IFC2X3/IFC4X3 unavailable in this build's C++ core, matching CI's own `-DSCHEMA_VERSIONS=4` gap, not a regression from this chunk). Independently self-reviewed directly against the real Python source and the generated `.d.ts`s before opening the PR (line-by-line, function-by-function); no further bugs found. |

## Phase 6.5 — Beta checkpoint ⏸️ npm-publish confirmation required

| Chunk | Status | PR | Notes |
|---|---|---|---|
| Benchmark re-run (no regression) | 🔲 | — | — |
| `npm publish ifcopenshell@beta` | 🔲 | — | ⏸️ stops for explicit user go-ahead |

## Phase 7 — `api` Tier 2 [Lane A]

| Chunk | Status | PR | Notes |
|---|---|---|---|
| `api.material` — `assign_material`/`unassign_material`/`copy_material` (chunk 1 of several; 3 of this module's 26 real files) | ✅ | [#93](https://github.com/mikitski/IfcOpenShell/pull/93) | High-priority chunk: 3 separate already-merged chunks (`api.type.assignType`, `api.root.removeProduct`, `api.root.copyClass`) had each disclosed a real, loud-throw blocker citing this exact module (previously zero TS port of any kind) — landing this chunk retroactively unblocks all three. **`assignMaterial`** dispatches on a `type: MaterialType` parameter across all 7 real Python branches (`IfcMaterial`/`IfcMaterialConstituentSet`/`IfcMaterialLayerSet`/`IfcMaterialLayerSetUsage`/`IfcMaterialProfileSet`/`IfcMaterialProfileSetUsage`/`IfcMaterialList`) — despite the chunk-planning doc's own speculation that building a fresh `*SetUsage` might need still-unported sibling `add_layer`/`add_profile`-style helpers, reading the full 364-line source found NO such call (the usage entity is built directly via `create_entity`); the ONLY actual `ifcopenshell.api.material.*` call this function makes is `unassign_material`, itself in this same chunk's scope — so, unlike `assignType`/`removeProduct` before it, this function has **zero remaining blocked call sites of its own**. Several real, disclosed quirks ported verbatim: `execute()` unconditionally unassigns any pre-existing (even inherited-only) material from every product first; a subtle dispatch-order edge case where a non-`IfcMaterial` `material` under a mismatched, non-`"...Usage"` `type` bypasses the type-specific branch entirely (reachable by no real Python test, but real, load-bearing logic); a genuine DEAD "type → material set" cache bug (`types_to_material_sets` declared, membership-checked, but never actually written to in either `*SetUsage` branch) that a real, currently-passing Python test (`test_assign_element_layer_set_usage_is_different_for_different_layer_set_directions`) actually depends on — "fixing" it would break that test; `update_representation_profile`'s early `return` aborts the WHOLE function (not just the current product) the instant any one product in a batch lacks a representation; a literal upstream typo ("assiged") preserved in two `assert` messages. **`unassignMaterial`** reverses the above, with a real IFC2X3-vs-IFC4+ branch (IFC2X3 has no `IfcMaterialUsageDefinition`/`.AssociatedTo`, needs a second `get_inverse` hop) and a load-bearing operation-ordering subtlety in its `while associations:` set-draining loop (`material_inverses.issubset(associations)` must be checked BEFORE `rel` itself is removed from the working set). **`copyMaterial`** recurses into material-SET items (reusing, not copying, the underlying materials/profiles they reference) and copies psets/styles via already-landed `util.element.copy`/`copyDeep`; found and disclosed (not fixed — out of scope, belongs to an already-merged, separately-reviewed utility) a NEW `util.element.copyDeep`/`copy` limitation: neither function can copy a "simple"/defined-type instance (e.g. `IfcLabel`) at all, so copying a material pset property with an actual populated value throws — pinned as a dedicated "throws" test, tracked in `TODOS.md`. Entity positional-attribute orders verified against all 3 generated `.d.ts`s, including the real `IfcMaterialLayerSetUsage`/`IfcMaterialProfileSetUsage` IFC4+-only `ReferenceExtent` attribute (deliberately left unset, matching real Python) and `IfcMaterialProfileSetUsage`'s total absence from IFC2X3. **Unblocked the 3 waiting call sites**: `api.type.assignType`'s `mapMaterialUsages` now calls the real `assignMaterial` (its own disclosed-throw test replaced with the real, previously-unreachable `test_map_material_usages`/`test_do_not_reassign_material_if_it_was_assigned_previously` assertions); `api.root.removeProduct`'s `IfcRelAssociatesMaterial` branch now calls the real `unassignMaterial` (its own disclosed-throw test replaced with the real `test_removing_all_material_relationships_of_an_element`); `api.root.copyClass`'s `*Set` branch could NOT be wired up at chunk-authoring time — PR #92 (`copyClass`/`reassignClass`) had not yet landed in this worktree's `v0.9.0` base, confirmed by directory listing — but WAS wired up by the orchestrating session's own small follow-up once both PRs merged (its own disclosed-throw test replaced with real Python's `test_copying_material_sets_for_type_elements_only` assertions; see the `api.root` row above). Test coverage: `test_assign_material.py`/`test_unassign_material.py`/`test_copy_material.py` ported as fully as this chunk's own scope allows (fixtures for still-unported sibling `add_material`/`add_material_set`/`add_constituent`/`add_layer`/`add_profile`/`add_list_item` built directly via `file.createEntity`), plus original coverage for the dispatch quirk and Transaction/undo-redo regression tests for every mutating function. Real Python's own test suite never exercises IFC4X3 at all for this module — this port's tests additionally run the shared IFC2X3+ cases against IFC4X3 too (`.d.ts`s confirm identical shapes). Ran the full local suite (3063 passed, 5 skipped, the same 3 pre-existing unrelated failures in `api.unit.addMonetaryUnit`/`editMonetaryUnit` already tracked in `TODOS.md`, confirmed untouched by this chunk) against a locally-built all-3-schemas native addon; `tsc --noEmit`/`biome check` both clean. |
| `api.material` — `add_material`/`add_material_set`/`add_layer`/`add_profile`/`add_constituent`/`add_list_item`/`edit_material`/`edit_assigned_material` (chunk 2 of several; 8 more of this module's 26 real files, 11/26 total so far) | ✅ | [#95](https://github.com/mikitski/IfcOpenShell/pull/95) | Each of this chunk's 8 files was read in full first, and none has a genuine blocked sibling-call dependency (the shared docstring examples reference `add_layer`/`edit_layer`/`assign_material`, but those are documentation only, not real code) — no `TODOS.md` entry needed. **`addMaterial`** ports the `name \|\| "Unnamed"` truthiness fallback (an explicit empty string also falls back, unlike `?? "Unnamed"` would) and a real, disclosed IFC2X3-vs-IFC4+ gap: `Category`/`Description` don't exist on IFC2X3's own `IfcMaterial` declaration at all (only `Name`), so providing either on IFC2X3 throws a native "no such attribute" error, matching real Python's own unguarded `setattr` (its docstring even warns "categories are not available in IFC2X3") — not a bug this port introduces or should silently guard against. **`addMaterialSet`** introduces its own file-local `MaterialSetType` (Python's own separate `MATERIAL_SET_TYPE` literal, deliberately NOT merged into `util/element.ts`'s broader `MaterialType`) and ports a real dispatch quirk verbatim: the `if/elif` chain's un-`else`d tail handles both `IfcMaterialProfileSet`/`IfcMaterialConstituentSet` as one fallback branch, not two. **`addLayer`** ports a real, load-bearing IFC2X3-vs-IFC4+ `if/else` (not merged into one call): IFC2X3's own `IfcMaterialLayer` has no `Name` attribute at all, so passing `Name=name` unconditionally would throw even for a caller who never asked for one — real Python's own two-branch shape avoids this by simply never mentioning `Name` in the IFC2X3 branch's kwargs. **`addProfile`**/**`addConstituent`** are both implicitly IFC4+-only (`IfcMaterialProfile`/`IfcMaterialConstituent` don't exist on IFC2X3 — no schema guard in real Python either, so this port adds none); `addProfile`'s `Name` is always passed at creation (even `null`), while `Material`/`Profile` are each conditionally set afterward only when truthy — ported with that exact asymmetry. **`addListItem`** creates no entity at all (a bare list-append onto `IfcMaterialList.Materials`) and, unlike its siblings, actually works identically on every schema (`IfcMaterialList` itself is schema-uniform) despite existing to serve IFC2X3 callers specifically. **`editMaterial`**/**`editAssignedMaterial`** are confirmed real Python duplicates of each other (byte-for-byte identical `setattr`-loop bodies, differing only in docstring/parameter naming) — ported as two separate trivial functions matching `../group/editGroup.ts`'s established shape, not merged into one. Every `file.createEntity` call's positional attribute order verified against all 3 generated `.d.ts`s, including 3 distinct real schema splits: `IfcMaterial` (`Name` only on IFC2X3 vs. `+Description+Category` on IFC4+), `IfcMaterialLayer` (3 attrs on IFC2X3 vs. 7 on IFC4+, `Name` at a different relative but same absolute-enough position needing an explicit `null` filler), and `IfcMaterialProfile`/`IfcMaterialConstituent` (absent from IFC2X3 entirely). No real Python test file exists for 7 of these 8 files (`test_add_material.py`/`test_add_layer.py`/`test_add_profile.py`/`test_add_constituent.py`/`test_add_list_item.py`/`test_edit_material.py`/`test_edit_assigned_material.py` all confirmed absent by directory listing) — original coverage written directly against each real source's own disclosed behavior instead; `test_add_material_set.py`'s real 4 assertions (`test_add_layer_set`/`test_add_list`/`test_add_profile_set`/`test_add_constituent_set`) were ported as-is, plus a few own additions (default `set_type`, empty-string-name fallback). Real Python's own test suite never exercises IFC4X3 for `add_material_set` — this port's shared IFC2X3+ cases additionally run against IFC4X3 too (`.d.ts`s confirm identical shapes), matching chunk 1's own precedent. Ran the full local suite (3315 passed, 5 skipped, the same 3 pre-existing unrelated failures in `api.unit.addMonetaryUnit`/`editMonetaryUnit` already tracked in `TODOS.md`, confirmed untouched by this chunk) against a locally-built all-3-schemas native addon; `tsc --noEmit`/`biome check` both clean. |
| `api.material` — `remove_material`/`remove_material_set`/`remove_layer`/`remove_profile`/`remove_constituent`/`remove_list_item` (chunk 3 of several; 6 more of this module's 26 real files, 17/26 total so far) | ✅ | [#96](https://github.com/mikitski/IfcOpenShell/pull/96) | Each of this chunk's 6 files was read in full first; only **`removeMaterialSet`** has a genuine sibling `api.material` dependency (`unassignMaterial`, already landed in chunk 1) — called to strip every product's/type's association (and LayerSet/ProfileSet "Usage" wrapper) with the set before it is removed, real Python's own comment being "usage is invalid if it is not associated with some element, so we can remove usages through unassignment". The other 5 files' own docstring examples reference sibling `add_*`/`edit_*` functions, but those are documentation only, not real code inside the `remove_*` functions themselves — no `TODOS.md` entry needed for this chunk either. **`removeMaterial`** ports real Python's own `TODO` verbatim (deleting a set item but never the set itself, which can leave an invalid zero-item set) and a real IFC2X3-vs-IFC4+ branch for `IfcMaterialProperties`: only `IfcExtendedMaterialProperties` (a subtype) has a `Properties`-like attribute (`ExtendedProperties`) on IFC2X3, read via a soft `getattr`-equivalent (`attrOrNull`) that tolerates the base class not declaring that attribute at all. **`removeMaterialSet`** ports `has_usages` being true only for `IfcMaterialLayerSet`/`IfcMaterialProfileSet` (the other two set classes have no "Usage" wrapper entity in the schema) and a real, unguarded `else: raise ValueError(...)` dispatch tail for an unknown material-set class, ported as the same explicit `throw`; also confirmed its own unconditional (non-schema-branched) `IfcMaterialProperties.Properties` read is not an oversight relative to `removeMaterial`'s own branched version — none of the 4 material-SET classes this function accepts is an `IfcMaterial`, so an `IfcMaterialProperties` inverse of a material SET is structurally unreachable on IFC2X3 in the first place. **`removeProfile`** ports a genuine SWIG/Python sequence-iteration-protocol quirk: real Python's `for attribute in profile:` has no true `__iter__`, so it iterates via `profile[i]`/`len(profile)` (every FORWARD attribute's raw value by positional index, NOT inverse attributes, and NOT flattening a list-valued attribute's own elements since `isinstance(list_value, entity_instance)` is always `False`) — ported via the equivalent `attributeCount()`/`getByIndex(i)` loop, deduped by identity into a `Map<number, EntityInstance>` to reproduce Python's `set()` semantics; also ports a real, disclosed asymmetric skip where `should_remove_material`/`should_remove_profile_def` are each checked independently via `continue`, not merged into one combined condition. **`removeListItem`** ports real Python's `materials.pop(material_index)` — `list.pop`'s exact semantics, where a negative index counts from the end and ANY out-of-range index (positive or negative) raises `IndexError` — as an explicit bounds check + `throw new RangeError("pop index out of range")`, since plain JS array/`splice` indexing would otherwise silently no-op rather than throw; matches this project's own established `IndexError`→`RangeError` convention (`api/pset/editPset.ts`). **`removeLayer`**/**`removeConstituent`** are confirmed byte-for-byte the same shape as each other (read both real sources in full) — genuine duplicate logic across two different set-item classes, not a copy/paste mistake; `should_remove_material` only removes the material via `remove_deep2` (i.e. only if it has no other remaining users after this layer/constituent is gone), so a shared material used by multiple layers/constituents/products is left alone even when the flag is set. Test coverage: `test_remove_material.py`/`test_remove_material_set.py` ported in full (both real Python classes run on IFC2X3+IFC4 via multiple inheritance; this port's tests additionally run the shared cases against IFC4X3 too, matching chunks 1/2's own established precedent). No real Python test file exists for `remove_layer.py`/`remove_profile.py`/`remove_constituent.py`/`remove_list_item.py` (all 4 confirmed absent from `src/ifcopenshell-python/test/api/material/`) — original coverage written directly against each real source's own disclosed behavior instead; every schema-specific test (`IfcMaterialProfile`/`IfcMaterialConstituent` don't exist on IFC2X3) is gated via `AVAILABLE_SCHEMAS.filter(...)`, never a hardcoded schema list. One genuine test bug found and fixed during verification (not a correctness bug in the ported `remove*.ts` logic itself, which matches the real Python source line-by-line): `removeMaterial.test.ts`/`removeMaterialSet.test.ts` called `addMaterial(file)` with no settings argument at 9 call sites, which threw at runtime since `addMaterialUsecase` reads `settings.name` unconditionally with no default parameter — every other call site of `addMaterial` in the codebase (chunks 1/2 included) passes at least `{}`; fixed to `addMaterial(file, {})`. Ran the full local suite (3395 passed, 5 skipped, the same 3 pre-existing unrelated failures in `api.unit.addMonetaryUnit`/`editMonetaryUnit` already tracked in `TODOS.md`, confirmed untouched by this chunk) against a locally-built all-3-schemas native addon; `tsc --noEmit`/`biome check` both clean (one formatting nit in `removeMaterial.test.ts` auto-fixed via `biome --write`). |
| `api.material` — `edit_layer`/`edit_constituent`/`reorder_set_item`/`edit_layer_usage`/`edit_profile`/`set_shape_aspect_constituents`/`assign_profile`/`edit_profile_usage` (chunk 4 of 4, **`api.material` module now FULLY COMPLETE, 26/26 files**) | ✅ | [#97](https://github.com/mikitski/IfcOpenShell/pull/97) | Final chunk of `api.material`. Every sibling `api.material` dependency this chunk's own 8 files need (`unassignMaterial`/`removeMaterialSet`/`addMaterialSet`/`addConstituent`/`assignMaterial`, all needed by `set_shape_aspect_constituents` alone) was already landed in an earlier chunk of this same module — no cross-chunk blocker at all. **`editLayer`**/**`editProfile`** share the same "attributes loop, then independently-guarded optional `Material`/`profileDef` swap" shape; **`editConstituent`** instead sets `Material` UNCONDITIONALLY (even `null`/omitted) — a real, disclosed asymmetry against its two siblings, ported verbatim, not "fixed" to match them. **`editLayerUsage`** is a trivial required-`attributes` setter loop (typically used to change `OffsetFromReferenceLine`). **`reorderSetItem`** dispatches across all 4 real material-set classes (`MaterialConstituents`/`MaterialLayers`/`MaterialProfiles`/`Materials`) with a real, unguarded `else: throw` tail for an unrecognised class, and reproduces Python's exact `list.pop`/`list.insert` reorder semantics (not a naive swap) via a small `moveItem` splice helper. **`assignProfile`** swaps an `IfcMaterialProfile.Profile` and propagates it onto every occurrence's own body representation `IfcSweptAreaSolid.SweptArea` via `ToMaterialProfileSet`/`AssociatedTo`; discloses a real, permanently UNREACHABLE dead-code branch (its own `if schema === "IFC2X3"` dispatch, since `IfcMaterialProfile` doesn't exist on IFC2X3 at all — a real caller could never reach this function with a valid argument on that schema in the first place), ported verbatim rather than collapsed away. **`setShapeAspectConstituents`** discloses a genuine upstream BUG: its "reuse an existing matching material constituent set" check reads `is_a("IfcMaterialConstituent")` (a SET-ITEM class, never assignable as a `RelatingMaterial`) where it should almost certainly read `"IfcMaterialConstituentSet"` — always `False` in practice, so this function ALWAYS creates a brand new set from scratch on every call, even called twice in a row with identical `materials`, never reusing one (ported verbatim, with the exact same short-circuiting `&&` that avoids ever hitting a further, real `AttributeError` a naive typo-only fix would immediately reintroduce); also has ONE genuinely unported dependency, `ifcopenshell.api.style.assign_item_style` (`api.style` has no TS port of any kind at all — a brand-new blocked module, same treatment as `api.system`'s own `copyClass.ts` entry), thrown only at the exact item/style match that would need it, never proactively, with everything else (material-set surgery, aspect/style matching) running to completion regardless. **`editProfileUsage`** (223 lines, by far this chunk's largest/most complex file, read multiple times before porting): ports real Python's exact order-of-operations (compute the cardinal-point side effect from the OLD `CardinalPoint` value FIRST, then apply the `attributes` setter loop SECOND) faithfully, including the real silent no-op guard when the profile set has no profile at all; its one blocker is `ifcopenshell.geom`/`util.shape.getX`/`getY` (the same pre-existing geometry-kernel gap `TODOS.md`'s very first two entries already track) needed by `calculate_position()`'s actual triangulation-based cardinal-point-offset math — thrown the instant a profile is found and `CardinalPoint` genuinely changes, deliberately BEFORE the dummy-file/representation-patching work AND before `usage`'s own `attributes` loop runs (matching real Python's own order, so nothing is ever mutated before the throw). Every `file.createEntity`/attribute-access call verified against all 3 generated `.d.ts`s, confirming `IfcMaterialConstituent`/`IfcMaterialProfile`/`IfcMaterialConstituentSet`/`IfcMaterialProfileSet`/`*SetUsage` classes' total absence from IFC2X3 (gating most of this chunk's own test files to IFC4/IFC4X3 only, via `AVAILABLE_SCHEMAS.filter`). Test coverage: only 1 of these 8 files has a real Python test counterpart (`test_edit_profile_usage.py`'s `test_update_cardinal_point`, IFC4/IFC4X3 via multiple inheritance) — since it asserts a real geometric result this port cannot compute, it's pinned as a dedicated "throws" regression test instead (per the disclosed blocker above), alongside real, portable assertions for every branch that doesn't need the geometry kernel; the other 7 files have no Python test counterpart at all (confirmed by directory listing) — original coverage written directly against each real source's own disclosed behavior, including dedicated regression tests for the `setShapeAspectConstituents` reuse-bug and its `api.style` throw. Two new `TODOS.md` entries added (`editProfileUsage`'s `ifcopenshell.geom` gap, `setShapeAspectConstituents`'s `api.style` gap + disclosed bug); searched `TODOS.md` for any stale `assign_profile`/`edit_layer`-not-yet-existing cross-references from earlier chunks — none found (all prior `api.material` blocker entries already reference only `assign_material`/`unassign_material`/`copy_material`, all resolved in chunk 1). Ran the full local suite (1746 passed, 46 skipped, all pre-existing gaps unaffected) against a locally-built IFC4-only native addon (matching CI's own `-DSCHEMA_VERSIONS=4` configuration); `tsc --noEmit`/`biome check` both clean. `api.material` is now FULLY PORTED, 26/26 files. |
| `api.geometry` — `assign_representation`/`map_representation` (chunk 2 of ~29 real files; landed early, out of file-count order) | ✅ | [#94](https://github.com/mikitski/IfcOpenShell/pull/94) | High-priority chunk: 2 already-merged chunks (`api.type.mapTypeRepresentations`, `api.root.reassignClass`'s `switchBetweenClassTypes`) had each disclosed a real, loud-throw blocker citing `assign_representation`/`map_representation` by name — landing this chunk retroactively unblocks both (fully for `mapTypeRepresentations`, partially for `reassignClass`, see below). Verified directly: neither file has any `numpy`/matrix-math dependency (unlike the still-unported `edit_object_placement`) — `assign_representation` only calls `map_representation` (its own sibling, ported alongside), `api.owner.update_owner_history` (landed), and `util.element.get_type`/`get_material` (landed); `map_representation` has no external `ifcopenshell.*` import at all. **`assignRepresentation`** ports a real, non-obvious guard with its exact 4-condition boolean shape: an `IfcProduct` occurrence gets silently redirected onto its own TYPE instead when the type already has `RepresentationMaps`, the new representation isn't itself already `"MappedRepresentation"`, AND the type's own material isn't an `IfcMaterialProfileSet`/`IfcMaterialLayerSet` — real Python's own comment cites GitHub issue #6934 (a Revit-export quirk) as the reason for the material carve-out, pinned by `test_assigning_to_an_instance_with_a_geometric_profile_layer_based_type_only_adds_it_to_the_instance`. The `IfcTypeProduct` branch builds a fresh `IfcRepresentationMap` then cascades a wrapped `IfcMappedItem` onto every real occurrence via the schema-dependent `ObjectTypeOf`(2X3)/`Types`(4+) inverse, the same split `util/element.ts`'s `getTypes` already encodes. **`mapRepresentation`** wraps a representation in a fresh `IfcMappedItem`, reusing the first `IfcRepresentationMap` inverse found (via `get_inverse`) or creating one. Every entity class both files construct (`IfcCartesianPoint`/`IfcDirection`/`IfcAxis2Placement3D`/`IfcRepresentationMap`/`IfcCartesianTransformationOperator3D`/`IfcMappedItem`/`IfcShapeRepresentation`/`IfcProductDefinitionShape`) verified positionally against all 3 generated `.d.ts`s — all identical across schemas, no DERIVE-attribute interleaving. **Wired up both waiting call sites**: `api.type.mapTypeRepresentations` now ports real Python's full two-loop body for real (no throw left at all) — its own disclosed-throw test replaced with the real `test_doing_nothing_if_the_type_has_no_representation_maps`/`test_removing_existing_element_representations_and_mapping_type_representations` assertions, and `assignType.test.ts`'s own analogous throw pin replaced with the real `test_map_representation`/`test_do_not_map_representation_if_type_was_assigned_previously` (the latter genuinely unreachable while blocked, now ported for real). `api.root.reassignClass`'s `switchBetweenClassTypes` blocker is only NARROWED, not fully resolved: re-reading `switch_between_class_types`'s own final `if` verbatim shows the still-unported `edit_object_placement` is reached ONLY `if switch_type == "type_to_occurrence" and representations:`, never for `occurrence_to_type` — so an `occurrence_to_type` switch with representations now fully succeeds via the real `assignRepresentation` (pinned by the real `test_keeping_representations_switching_from_occurrence_class_to_type_class`), while a `type_to_occurrence` switch with representations still throws, now citing only `editObjectPlacement` (pinned by `test_keeping_representations_switching_from_type_class_to_occurrence_class`, still a disclosed-throw test). No real Python test file exists for `map_representation` itself (confirmed by direct search of `test/api/geometry/`) — its own dedicated `mapRepresentation.test.ts` coverage (reuse-vs-create `IfcRepresentationMap` branches, copied `ContextOfItems`/`RepresentationIdentifier`, Transaction/undo-redo) is written directly against the 64-line real Python source instead. Ran the full local suite (1652 passed, 46 skipped, all pre-existing gaps unaffected) against a locally-built IFC4-only native addon; `tsc --noEmit`/`biome check` both clean. `TODOS.md`'s `mapTypeRepresentations`/`reassignClass` blocker entries updated to RESOLVED/narrowed accordingly. |
| `api.feature` | 🔲 | — | — |
| `api.nest` | 🔲 | — | — |
| `api.style` — `add_style`/`remove_style`/`remove_surface_style`/`remove_styled_representation`/`edit_presentation_style`/`unassign_material_style`/`unassign_representation_styles` (chunk 1 of 2; 7 of this module's 13 real files) | ✅ | [#98](https://github.com/mikitski/IfcOpenShell/pull/98) | High-priority chunk: `api.material.setShapeAspectConstituents` (already-merged chunk 4 of `api.material`) had disclosed a real, loud-throw blocker citing this exact module's `assign_item_style` by name (previously zero TS port of any kind). `assign_item_style` itself is a chunk 2 file (alongside `add_surface_style`/`edit_surface_style`/`add_surface_textures`/`assign_material_style`/`assign_representation_styles`), so that specific blocker remains open until chunk 2 lands — this chunk could not retroactively unblock it, unlike the `api.material` chunk 1 precedent. Every sibling `api.style` dependency this chunk's own 7 files need (`unassignRepresentationStyles`, needed by `unassignMaterialStyle`) was already landed in this SAME chunk — no cross-chunk ordering blocker. **`addStyle`** unconditionally forces `IfcSurfaceStyle.Side = "BOTH"` (real Python's own Revit-compatibility comment: "Name is filled out because Revit treats this incorrectly as the material name"), not exposed as a settings option. **`removeStyle`** ports real Python's recursive `Usecase` class as plain functions threading `file` through explicitly (matching `../material/unassignMaterial.ts`'s own established precedent for a multi-method Python class), including a real, disclosed mutual `IfcFillAreaStyle`/`IfcFillAreaStyleHatching` removal recursion with two deliberately DISTINCT `removeDeep2` call shapes (one with `alsoConsider`, one without) — unifying them would risk silently changing which entities each call is allowed to also delete. **`removeSurfaceStyle`** ports a real IFC2X3-vs-IFC4+ schema branch for texture cleanup (`IfcTextureCoordinate`/`IfcTextureCoordinateGenerator.Maps`/`IfcSurfaceTexture.IsMappedBy` don't exist on IFC2X3 at all — confirmed against `ifc2x3.d.ts`) and Python's `for attribute in style:` sub-entity scan as an explicit `attributeCount()`/`getByIndex()` loop. **`removeStyledRepresentation`** discloses a real asymmetry, ported verbatim: an orphaned styled item's `IfcPresentationStyleAssignment` wrapper IS cleaned up, but a bare `IfcPresentationStyle`/`IfcSurfaceStyle` it (or a sibling item) references never is — matching this function's own documented "doesn't remove the underlying styles" contract. **`editPresentationStyle`** is a trivial `attributes` setter loop, matching `../material/editMaterial.ts`'s identical shape. **`unassignRepresentationStyles`** ports the `IfcPresentationStyleAssignment`-unwrap-then-unconditional-unassign two-step shape verbatim; a real, disclosed docstring/return-value mismatch (real Python's own early `return []` vs. its documented `:return: None`) is not reproduced since no caller anywhere uses the return value. **`unassignMaterialStyle`** has ONE genuinely unported, out-of-module dependency: `ifcopenshell.util.element.get_shape_aspects` — confirmed absent from `util/element.ts` (that file's own header comment lists it as out of scope across all 3 of its own already-landed chunks; re-reading the real 36-line source directly shows the original blocking rationale attributed to it there may already be stale, since it doesn't actually call `util.representation` at all) — thrown loudly only at the exact point it would be needed (a named material constituent AND a real element using it both found), never proactively, with the function's own unconditional first-loop mutation always running to completion regardless. Confirmed schema differences beyond the above: `IfcPresentationStyleAssignment` exists on IFC2X3/IFC4 but was REMOVED in IFC4X3 (confirmed absent from `ifc4x3.d.ts` entirely — `IfcStyledItem.Styles` narrows accordingly). One new `TODOS.md` entry added (`unassignMaterialStyle`'s `getShapeAspects` gap). Test coverage: `test_remove_style.py`/`test_remove_surface_style.py` ported in full (using `api.project.createFile` for a genuinely blank file, matching real Python's own bootstrap fixture, needed for their `len(list(file)) == 0` assertions); `test_unassign_material_style.py`/`test_unassign_representation_styles.py` ported with fixtures built directly (reproducing the exact entity shape the still-unported chunk-2 `assign_material_style`/`assign_representation_styles` would produce, identical technique to `../material/removeMaterial.test.ts`'s own established precedent), and the one real assertion needing `getShapeAspects` replaced with a "throws the disclosed blocked error" regression test (matching `../material/setShapeAspectConstituents.test.ts`'s own precedent) instead. No real Python test file exists for `add_style.py`/`remove_styled_representation.py`/`edit_presentation_style.py` (confirmed by directory listing) — original coverage written directly against each real source's own disclosed behavior instead. Every schema-specific test gated via `AVAILABLE_SCHEMAS.filter(...)`, never a hardcoded schema list. Ran the full local suite (69 new tests, all passing) against a locally-built all-3-schemas native addon; `tsc --noEmit`/`biome check` both clean. Noted, out of scope for this chunk: running the full existing suite against an all-3-schemas addon (rather than CI's own IFC4-only) surfaced 10 pre-existing failures in already-merged, unrelated `api.material`/`api.unit` test files (`editLayer`/`editLayerUsage`/`reorderSetItem`/`addMonetaryUnit`/`editMonetaryUnit`, all IFC2X3-specific — some already tracked in `TODOS.md`'s `ZWL`/`DOLLARYDOO` entry, others newly surfaced) — confirmed unrelated to and untouched by this chunk, flagged for a future session rather than fixed here. |
| `api.style` — `add_surface_style`/`edit_surface_style`/`add_surface_textures`/`assign_item_style`/`assign_material_style`/`assign_representation_styles` (chunk 2 of 2, **`api.style` module now FULLY COMPLETE, 13/13 files**) | ✅ | [#99](https://github.com/mikitski/IfcOpenShell/pull/99) | Completes `api.style`. **Resolves the high-priority blocker**: `api.material.setShapeAspectConstituents` (chunk 4 of `api.material`) had disclosed a real, loud-throw blocker citing `assign_item_style` by name — this chunk lands `assignItemStyle.ts` and wires the real call back into `setShapeAspectConstituents.ts`, replacing its throw; `setShapeAspectConstituents.test.ts`'s own "throws the disclosed blocked error" regression is replaced with real `IfcStyledItem`/`Styles` assertions, and `TODOS.md`'s matching entry is marked RESOLVED (its separate, still-open `is_a("IfcMaterialConstituent")` reuse-check bug is untouched). Every sibling `api.style` dependency this chunk's own 6 files need (`editSurfaceStyle`/`removeSurfaceStyle` for `addSurfaceStyle`; `assignRepresentationStyles` for `assignMaterialStyle`) was already landed either in this same chunk or chunk 1 — no cross-chunk ordering blocker. **`addSurfaceStyle`** enforces two real invariants ported verbatim: shading/rendering are mutually exclusive (an `IfcSurfaceStyleRendering` IS-A `IfcSurfaceStyleShading` in EXPRESS, so adding one removes the other), and at most one item per class survives per style. **`editSurfaceStyle`** resolves each attribute's declared EXPRESS type at runtime (`declaration().as_entity().all_attributes()` + `parameter_type`/`named_type`/`aggregation_type`, reusing `util/schema.ts`'s `isEnumMember` precedent) to dispatch across 3 special-cased handlers (`IfcColourRgb`/`SpecularHighlight`-by-name/`IfcColourOrFactor`); discloses a real asymmetry (`editColourRgb` always overwrites `Name`, `editColourOrFactor` never touches it) — **and hits a genuine, already-tracked primitive-layer gap**: `TODOS.md`'s "`EntityInstance.setByIndex`/`IfcFile.createEntity` cannot write an initial value into a freshly created simple/defined-type instance" entry (previously found 4 times across `util.migrator`/`util.cost`/`api.owner.addApplication`/`api.pset.editPset`) blocks creating a standalone, addressable `IfcNormalisedRatioMeasure`/`IfcSpecularExponent`/`IfcSpecularRoughness` — confirmed empirically against the locally-built native addon (even a *zero*-arg `file.createEntity("IfcNormalisedRatioMeasure")` gets `id() === 0`, so no workaround exists without fixing the primitive layer itself); the numeric-factor branch of `editColourOrFactor` and both non-`null` branches of `editSpecularHighlight` throw the disclosed error only at that exact point (the real "remove the old real-id value" step still runs first), pinned by 3 dedicated "throws" regression tests (2 real Python test cases could not even be ported as fixtures at all, since their own setup needs the identical blocked call — dropped rather than left broken); `TODOS.md` updated with a 5th consequence entry. **`addSurfaceTextures`** ports the full `textures=` (list-of-dicts) path faithfully (real Python's own IFC2X3 early-return preserved, `IfcImageTexture` having an entirely different attribute shape there — confirmed against `ifc2x3.d.ts`); the `material` (Blender node-tree) parameter is a **permanent, non-"port later" scope boundary** (no TS/Node representation of `bpy.types.Material` exists at all) — throws only when supplied, documented in its own dedicated `TODOS.md` entry distinct from every other "not yet ported" one in the file; matches real Python's own exact order of operations (a `code-review` finding, fixed before merge): the IFC2X3 early-return runs first, UNCONDITIONALLY (`material` is never inspected on that schema, so it never throws there), the full `textures` loop always runs to completion second (any textures supplied alongside `material` are still created for real), and only then is `material` checked and thrown on. **`assignItemStyle`** (the chunk's namesake unblocker) ports its 3-way schema branch (no-existing-item / IFC4X3-direct-overwrite / pre-IFC4X3-assignment-reuse) verbatim, including the final "collapse to exactly one effective style" normalization on the pre-IFC4X3 path; no real Python test file exists for it (confirmed by directory listing) — original coverage written directly against the real 134-line source. **`assignRepresentationStyles`** — confirmed a real, pre-existing ASYMMETRY with chunk 1's `unassignRepresentationStyles` (assign side also matches `IfcTopologyRepresentation`/`IfcTopologicalRepresentationItem`, unassign side doesn't — real Python's own difference, re-verified by re-reading `unassign_representation_styles.py` directly, not a bug either port introduced), pinned by the real `test_assign_style_to_topology_representation`. **`assignMaterialStyle`** (247 lines, this chunk's largest, read multiple times): discloses a real, **TEST-CONFIRMED** upstream bug in `create_styled_item`'s reuse branch — its own `reuse_item.is_a("IfcPresentationStyleAssignment")` guard is permanently unreachable (`reuse_item` is always the `IfcStyledItem` being reused, never an assignment, provable from its own two call sites), so reusing an existing styled item always writes the RAW, unwrapped style directly, even on IFC2X3, silently orphaning the assignment this same function just created — confirmed not just by static analysis but by real Python's own shipped `test_assign_material_style.py::TestAssignMaterialStyleIFC2X3::test_run`, whose `else: # IfcPresentationStyleAssignment` comment is immediately followed by an assertion checking the bare, unwrapped style, contradicting its own comment; ported verbatim, pinned by this chunk's own `assignMaterialStyle.test.ts`. Also hits the EXACT SAME `ifcopenshell.util.element.get_shape_aspects` gap chunk 1's `unassignMaterialStyle` already disclosed (its own symmetric "handle material constituents and shape aspects" tail) — `TODOS.md`'s existing entry updated to cover both call sites rather than duplicated; pinned by a "throws the disclosed blocked error" regression matching `unassignMaterialStyle.test.ts`'s own precedent. Every `file.createEntity`/attribute-access call verified against all 3 generated `.d.ts`s, confirming (beyond chunk 1's own findings) `IfcSurfaceStyleShading.Transparency` doesn't exist on IFC2X3 (added in IFC4) and `IfcImageTexture` has a wholly different attribute shape there (`TextureType`/`UrlReference` vs. `Mode`/`URLReference`). Test coverage: `test_add_surface_style.py`/`test_add_surface_textures.py`/`test_assign_material_style.py`/`test_assign_representation_styles.py` ported in full (or as "throws" regressions where genuinely blocked, per above); `test_edit_surface_style.py` ported except `test_editing_texture_style` (covered instead, more thoroughly, by `addSurfaceTextures.test.ts`) and the 2 blocked-fixture cases noted above; `assignItemStyle.test.ts` is original coverage (no Python counterpart). Every schema-specific test gated via `AVAILABLE_SCHEMAS.filter(...)`, never a hardcoded schema list; no test passes `category`/`description` to `addMaterial` on IFC2X3 (the already-tracked `addMaterial`/IFC2X3 gap from the `api.material` chunk 4 finding). Ran the full local suite (3633 passed, 5 skipped, the same ~10 pre-existing failures chunk 1 already found and confirmed unrelated — all in already-merged `api.material`/`api.unit` IFC2X3-specific tests, untouched by this chunk) against a locally-built all-3-schemas native addon; `tsc --noEmit`/`biome check` both clean. **`api.style` is now FULLY PORTED, 13/13 files.** |
| `api.system` | 🔲 | — | — |
| `api.resource` | 🔲 | — | — |
| `api.profile` | 🔲 | — | — |
| `api.cost` | 🔲 | — | — |

## Phase 8 — `api` Tier 3 [Lane A]

| Chunk | Status | PR | Notes |
|---|---|---|---|
| `api.sequence` | 🔲 | — | — |
| `api.structural` | 🔲 | — | — |
| `api.georeference` | 🔲 | — | — |
| `api.grid` | 🔲 | — | — |
| `api.boundary` | 🔲 | — | — |
| `api.drawing` | 🔲 | — | — |
| `api.control` | 🔲 | — | — |
| `api.pset_template` | 🔲 | — | — |

## Phase 9 — `api` Tier 4, non-kernel [Lane D — needs Phase 4 + Phase 6]

| Chunk | Status | PR | Notes |
|---|---|---|---|
| `api.geometry` — relationship-CRUD functions | 🔲 | — | — |
| `api.geometry` — parametric shape-builder functions | 🔲 | — | — |
| `api.alignment` (non-kernel) + `api.cogo` | 🔲 | — | — |

## Phase 10 — v1 completeness pass

| Chunk | Status | PR | Notes |
|---|---|---|---|
| `validate.py` port | 🔲 | — | — |
| Full parity audit (Python test tree ↔ TS test tree) | 🔲 | — | — |
| Niche `util` modules: `brick`, `fm`, `profiler` | 🔲 | — | — |
| Derived-attribute support (EXPRESS rules) | 🔲 | — | — |

**v1 done when every row above is ✅.**

---

## Post-v1 (out of scope for this loop unless redirected)

- Phase G1 — Geometry kernel binding
- Phase B1 — Browser/WASM target

## Blockers / escalations

- **2026-09-04 — GitHub push access — RESOLVED.** User fixed the PAT (it previously lacked repo
  access). Confirmed working via a real push. No longer blocking.
- **2026-09-04 — `gh` CLI broken in this sandbox — WORKED AROUND, not resolved.** `gh` (both
  `gh auth status` and any `gh api`/`gh pr *` call) fails with a client-side TLS certificate
  verification error (`x509: OSStatus -26276`), independent of token validity. `git` and raw `curl`
  (using the token from `git credential fill`) work fine over the same network path. Every `gh`
  operation in this orchestration loop (CI status checks, PR merge, branch deletion) is being
  substituted with the curl+credential-helper equivalent. Not investigated further — no need to fix
  the underlying `gh` install unless it becomes a blocker for something curl can't do.
- **2026-09-04 — Planning docs accidentally bundled into the Phase 0 merge commit — noted, not
  fixed.** The orchestrator's initial planning-docs commit (`ee5756693`) was made directly to local
  `v0.9.0` but never pushed to `origin` before the Phase 0 worktree was branched from it. When PR #1
  squash-merged, GitHub computed the diff against `origin/v0.9.0`'s actual (older) tip, so the merge
  commit `8931001035` — titled "Add ifcopenshell-ts Phase 0 scaffolding" — also carries the full
  `planning/ifcopenshell-ts/` doc tree (~3840 lines) that was really a separate, earlier piece of
  work with its own review process. Content is correct and complete either way; only the commit
  history/attribution is muddled. Not unwound via history rewrite (would mean force-pushing a
  shared branch, treated as out of bounds without being asked) — local `v0.9.0` was synced to match
  `origin` and remaining orchestration-tracking edits (this file, `TODOS.md`) were committed
  separately on top. No action needed going forward; future PRs won't repeat this since `v0.9.0` is
  now fully in sync.

## Timeline

- 2026-09-03/04: Planning docs written and reviewed via `/plan-eng-review` — all findings resolved,
  10 architecture/testing/performance gaps closed, 2 delivery checkpoints added.
- 2026-09-04: Implementation loop spec finalized (worktree isolation, per-module PR granularity,
  serial-then-parallel-lanes sequencing, npm-publish confirmation gate). This tracker created.
- 2026-09-04: Phase 0 dispatched, hit and recovered from a GitHub push-access credential issue, 3
  rounds of real CI failures (missing CMake flags for OCCT/CGAL, missing cmake-js N-API package.json,
  vcpkg header-only Boost gaps, macOS Intel runner scarcity) — all diagnosed and fixed by the
  implementation agent using a self-added PR-comment diagnostic mechanism (this sandbox can't reach
  GitHub Actions' log-download host directly). CI went fully green across all 6 OS×arch combos.
  PR #1 reviewed against design docs and squash-merged to `v0.9.0` as `8931001035`.
